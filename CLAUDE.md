# CLAUDE.md

Operating notes for Claude when working on Stratum.

## Mission

Stratum is aiming to be a first-class peer of ZFS and btrfs — not a demo, not a hobby project, a production-grade COW filesystem that a POSIX-compatible OS can be built on. Beyond peer status, Stratum is intended to pioneer in a chosen set of areas where the state of the art is ripe for advance:

1. **Formally verified sync + crash-safety protocol.** TLA+ (or similar) specification of the three-phase sync, allocator invariants, and AEAD nonce machinery, proved correct under arbitrary write reordering. No mainline filesystem has this.
2. **Post-quantum encryption as default.** ML-KEM-768 + XChaCha20-Poly1305 hybrid wrap keys. Stratum ships PQ-ready out of the box.
3. **Content-defined extent boundaries.** Rolling-hash chunking at the extent layer (shift-resistant dedup for real-world data patterns — VMs, container images, backups). No production filesystem integrates this.
4. **Lock-free metadata path.** The Bε-tree's message-buffer model is an unusually good fit for lock-free operation. Pair with MVCC readers for zero-contention reads.
5. **Merkle-rooted metadata integrity.** Tamper-evident semantics stronger than per-block csums: any offline edit to any metadata block is cryptographically detectable.
6. **Tiered storage with learned migration.** Beyond heuristic hot/cold tiering — access-pattern models for policy decisions.
7. **io_uring-native, zero-copy write path.** Designed for 2026+ NVMe workloads, not retrofitted from POSIX block I/O.

These are long-horizon commitments. The existing code is a **foundation, not the destination**. Components will be preserved where they still serve the vision (Bε-tree core, crypto primitives, xxHash helpers) and rewritten where they don't (superblock layout, allocator, namespace model, block-device abstraction). Salvage rate of the existing ~20 KLOC is expected to be 30–50%. That's fine. The cost of preserving code against architectural constraints it doesn't fit is higher than the cost of rewriting it right.

## Design-first policy (Phase 0)

Architectural decisions compound. A wrong choice in the concurrency model, storage pool layout, or namespace structure becomes irrevocable once thousands of lines of code depend on it. **Phase 0 — active as of 2026-04-19 — pauses implementation of new features until major architectural decisions are specified in writing.**

Phase 0 outputs (all go into `docs/` — written, reviewed, versioned):

- `docs/VISION.md` — target workloads, scale targets, property ranking (perf / reliability / security / concurrency / portability). Identifies the non-goals.
- `docs/COMPARISON.md` — feature matrix vs ZFS, btrfs, bcachefs. Three columns: match, lead, deliberately-not-doing. Annotated with *why* for each.
- `docs/NOVEL.md` — the committed novel angles from the mission list. For each: concrete scope, what "done" looks like, dependencies on other decisions.
- `docs/ARCHITECTURE.md` — the foundational decisions:
    - Concurrency model (MVCC + lock-free readers / fine-grained writer locks / something else)
    - Storage pool model (device roster, redundancy domains, nonce construction under multi-device)
    - Namespace model (subvolumes, datasets, property inheritance, per-dataset encryption keys)
    - Allocator model (tree-embedded vs side channel; relation to sync)
    - Superblock / quorum model (per-device uberblock trees, commit ordering across devices)
    - Block-device abstraction (io_uring-native, DAX-compatible, zero-copy affordances)
    - Integrity model (Merkle root placement, verification cadence)
    - Crypto model (per-dataset keys, PQ wrap, key rotation, rekey)
- `docs/ROADMAP-V2.md` — the phased plan that builds on the above. Replaces the prior SOTA roadmap.

Only after `ARCHITECTURE.md` is signed off does implementation resume — likely from a mostly-fresh tree in a sibling directory (`stratum-v2/` or similar), with salvageable components ported one at a time with their own justification.

**Process notes during Phase 0:**
- Every design decision gets a written rationale. Assume we'll need to justify it in a year when the context has faded.
- Assume every decision gets challenged. The mission is ambitious enough that "we chose X because it was easier" isn't a legitimate answer for foundational choices.
- Design sessions produce documents, not code. No implementation pressure during Phase 0.
- Load-bearing invariants identified during design become candidates for formal specification (TLA+ / Alloy / similar). The spec is the source of truth; code is an implementation of the spec.

## Spec-first policy (applies to every new feature, not just Phase 0)

**If a feature touches a load-bearing invariant — concurrency, commit ordering, nonce uniqueness, quorum, redundancy, crypto key derivation, cache coherence, torn-write recovery — the TLA+ (or similar) model comes BEFORE the implementation.** Write the spec, let TLC chew on it, and let the invariant violations (if any) surface at the spec level where they cost minutes, not at runtime where they cost commits.

Concrete pattern that's repeatedly paid off on this project:
1. Propose the feature in prose (problem + shape).
2. **Model the mechanism in TLA+** — state, actions, invariants. TLC with small bounds (typically Devices≤3, Commits≤3, Retries≤1).
3. Iterate until TLC is green under the invariants you'd want the impl to uphold. If a bug shows up, fix the design BEFORE writing code.
4. Implement against the model. Cross-reference each impl step to the corresponding spec action in comments. Keep the SPEC-TO-CODE mapping current.
5. When the impl surfaces a new mechanism the spec didn't cover, extend the spec FIRST, then update the impl.

Historical receipts:
- `quorum.tla` (P5-0) caught two bugs before a line of P5-2 code was written: orphan-gen mount acceptance (fixed via `OrphansNotAuthoritative`) and over-strict `LiveCoordMonotonic` that would fail in the transient between-writes state.
- `quorum.tla` with `IdempotentRetry=FALSE` + `MaxRetries≥2` demonstrated the R14 P1 content-divergence bug at the spec level — the invariant fired before anyone had coded the offending path.
- Skipping this step for P5-3b/c left the per-tree-gen divergence bug (fold of alloc_commit's R7c P2-5 short-circuit + the roots-object value layout) for a test to catch at runtime, not the model. An extension of `quorum.tla` that captures per-device tree gens + idempotent-commit would have caught it before the first compile.

**If you cannot articulate the invariant formally, you don't understand it well enough to implement it.**

Features that clearly benefit (not exhaustive): multi-device commit, quorum protocols, key rotation, scrub orchestration, refcount/snapshot graph mutations, MVCC readers vs writers, anything that persists a reservation-then-commit or multi-phase write, anything that uses crypto nonces over a shared key, anything with more than one device state machine in play.

Features that usually don't (pure computation, test helpers, config parsing, CLI glue): skip the spec; just write + test. Use judgment.

## Audit-triggering changes

**Applies to**: any change to the current codebase during Phase 0 (e.g. bug fixes to the foundation) AND to Stratum v2 code once implementation resumes. The policy survives the redesign — the invariants are filesystem invariants, not stratum-v1 invariants.

Stratum v1 went through 15 rounds of adversarial soundness audits (R0–R14, commits `bb39db8` → `405c4fb`) that landed ~60 corruption-class fixes. The invariants those audits established are load-bearing and non-obvious — many will carry forward to v2. **Any change to the surfaces below MUST spawn a focused soundness audit against the changed code before merge** — not as ceremony, but because each round has surfaced a previously-invisible bug, and the pattern is that regressions in these areas are not caught by the test suite.

**Trigger list** — changes to any of these require an audit round:

| Surface | Files | Why |
|---|---|---|
| Crypto nonce construction / key handling | `src/crypto/crypto.c`, `include/stratum/crypto.h` | AEAD nonce uniqueness is the single most expensive invariant; past rounds found nonce-reuse across mount, across rollback, across UINT64_MAX wrap |
| Sync / rollback / mount-bump flow | `src/fs/fs.c::stm_fs_sync`, `stm_fs_open_impl`, `stm_fs_gen_bump_disk`, `src/snap/snap.c::stm_snap_rollback` | Invariant `disk ss_gen > fs->gen` is maintained by a delicate three-phase dance (see `docs/STRATUM.md` §7) |
| Snapshot index (v2) | `v2/src/snapshot/snapshot.c`, `v2/include/stratum/snapshot.h` | (v2-only) Snapshot create/delete/lookup/hold + chain ordering invariants from `v2/specs/snapshot.tla` (`ChainExtentTxgOrdered`, `OrphansNotAuthoritative`). Trust boundaries: (1) **R29 P3-1 saturation**: refuse on `next_id == UINT64_MAX` or `current_txg == UINT64_MAX`; (2) **R40 P2-1 racing-creator**: `extent_txg < prev's extent_txg` returns STM_EINVAL — without it, the in-process commit window could persist a snap with stale extent_txg, which the next mount's `sp_validate_shadow` would reject as STM_ECORRUPT (unmountable pool); (3) **R99 P2-1 carry (P9-CTL-1d-actions-snapshot-create)**: `stm_snap_name_chars_valid` refuses control bytes (< 0x20 + 0x7F) — closes the line-injection attack vector on every present and future surface that displays snapshot names in line-oriented format. UTF-8 multi-byte sequences (≥ 0x80) accepted unchanged; (4) **dead-list ownership transfer**: `stm_snapshot_delete`'s `out_freed_paddrs` AND `out_freed_cold_hashes` ownership transfers to caller; caller MUST reclaim each paddr via `stm_alloc_free` (per-device routed through `stm_paddr_device`) AND each cold hash via `stm_cas_deref` (iterating the buffer in `STM_CAS_HASH_LEN`-byte strides) before the next sync_commit OR blocks leak from tracking. Both buffers may legitimately be returned NULL with count 0 (clean delete with no overwrites); `free()` is well-defined on NULL. On non-OK return both pairs are zero/NULL. Future snapshot-mutation chunks (rollback, hold/release, recv side-effects) MUST consume both buffer-pairs end-to-end (R106 P3-2 carry — first /ctl/ surface to handle the pair was P9-CTL-1d-actions-snapshot-delete); (5) **txg monotonicity**: `current_txg` only ever advances. Future snapshot-mutation chunks (rollback, hold/release, recv side-effects) MUST preserve every invariant above and re-audit. |
| Btree write paths | `src/btree/btree.c::stm_btree_write_node`, `split_root`, `split_child`, `stm_btree_flush`, `src/btree/msg.c` | Torn-write recovery, pivot pre-reserve, and the two-phase flush commit are easy to break |
| Superblock layout / validation | `src/fs/fs.c` mount SB parsing, `include/stratum/super.h` | SB is plaintext + unkeyed xxHash3; every counter field needs adversarial-tamper defense |
| 9P wire handling (v1) | `src/p9/p9.c` | Every `body` pointer read needs a `body_len` bound; silent Rread with count=0 hides errors |
| 9P codec (v2 / janus) | `v2/src/p9/server.c`, `v2/src/p9/wire.h`, `v2/include/stratum/p9.h` | (v2-only) Generic vops-based 9P2000 codec used by janus's `/keys/` synthetic FS. Inherits R11-R14 audit lessons: bounds-checking on every wire read, fid leak avoidance on failed Tattach, partial-Twalk doesn't bind newfid, mode-gating on read/write. |
| 9P2000.L codec (v2 / lp9) | `v2/src/lp9/server.c`, `v2/include/stratum/lp9.h` | (P9-CTL-2a; v2-only) Generic vops-based 9P2000.L server, sibling to `stm_p9_server` (which speaks 9P2000) but on the .L wire. v1.0 wire-handles Tversion/Tattach/Twalk/Tlopen/Tread/Twrite/Tclunk/Tflush/Tgetattr/Treaddir; optional ops (Tlcreate/Tmkdir/Tunlinkat/Tsetattr/Tfsync/Tsymlink/Treadlink) return Rlerror(ENOSYS) when their vops slot is NULL. Trust-boundary discipline lifted verbatim from `v2/src/9p/server.c` (the FS-bound .L server): every Txx parser bounds-checks `body_len` before consuming fields; every Rxx writer bounds-checks `resp_cap` before serialising; count fields in Tread/Twrite/Treaddir clamp against BOTH `msize - hdr - 4` AND `resp_cap - hdr - 4` so a small response buffer doesn't fail the request — it just emits less. h_read enforces `f->is_open` (R74-class lesson on .L server's stricter open-state contract vs. 9P2000 server's lax behavior); h_lopen refuses re-open of an already-open fid, refuses O_DIRECTORY on non-dir → ENOTDIR, refuses non-RDONLY on dir → EISDIR. First consumer is /ctl/ as of P9-CTL-2b (`v2/src/ctl/synfs.c`); slate daemon is the next planned consumer. Future consumers MUST inherit the same discipline when they add new ops: bounds-check body_len, clamp counts, validate fid state. R111 client-side caller-cap-bound lesson applies symmetrically here on the server side — `got > count` after vops->read returns is clamped before serialising. |
| 9P2000.L filesystem server (v2) | `v2/src/9p/server.c`, `v2/src/9p/wire.h`, `v2/include/stratum/9p.h` | (P9-9P-1; v2-only) Filesystem-bound 9P2000.L server that takes a live `stm_fs *` and dispatches every op to `stm_fs_*`. Composes against `v2/specs/fid.tla` — every Tread/Twrite/Tgetattr/Tsetattr/Tlock/... handler MUST verify the fid's cached_gen against the inode's current `si_gen` via `verify_fid_fresh` BEFORE forwarding to stm_fs (the IOReject gate from fid.tla); every Twalk / Tattach binding MUST snapshot current `si_gen` at bind time (the WalkBindsWithCurrentGen invariant). Five buggy configs enumerate the runtime-gate failure modes the reviewer must explicitly rule out — staleness gate skipped (confused-deputy attack), bind-time gen-snapshot wrong, Tclunk leaks fid, Tdetach leaks fids, IOReject fires falsely. Every fid that has held byte-range locks must call `stm_fs_release_lock_owner(owner_id)` at clunk + at Tdetach (composes against locks.tla::ReleaseOwner). |
| slate daemon (v2) | `v2/src/slate/slate.c`, `v2/include/stratum/slate.h`, `v2/src/cmd/stratum-slate/main.c`, `v2/specs/slate.tla` | (P9-SLATE-1 + P9-SLATE-2 + P9-SLATE-3a + P9-SLATE-3b + P9-SLATE-3c-ascend + P9-SLATE-3c-selection + P9-SLATE-4-confirm + P9-SLATE-4b + P9-SLATE-5a + P9-SLATE-5b; v2-only) Slate is the Plan-9-shaped TUI architected as a synthetic filesystem (docs/SLATE-DESIGN.md). The daemon owns all UI state; clients (terminal renderer, scripts, AI agents, future Halcyon panes) interact with it purely by reading/writing files in a 9P tree served over a Unix socket. Cumulative schema: SLATE-1 — /version, /status, /event, /redraw, /log/{tail,append}; SLATE-2 — /connection/{socket,connected,attach}, /panels/{left,right}/{path,entries}; SLATE-3a — /panels/{left,right}/{cursor,action} (cursor RW, action W); SLATE-3b — `key Enter` descend verb + walk refactor (panel_entries_render clones WORK→CWD before lopen so per-entry walks resolve from CWD instead of root, enabling nested-cwd correctness); SLATE-3c-ascend — `key Backspace` parent-dir verb (pure path manipulation; no backend op); SLATE-3c-selection — /panels/X/selection RW (comma-separated entry indices, bitset state, reset on any cwd change); SLATE-4-confirm — /dialogs subtree (single-dialog stack, confirm kind: kind/title/body/options/result; programmatic open API; result-write to dismiss; monotonic non-reusable dialog ids encoded in qid low bits). Composes against `v2/specs/slate.tla` (VersionMonotonic + EventFIFO + ReadConsistent + DispatchProgress + ConnectionAtomic + PanelPathConsistent; 4 buggy configs trip the right invariants). **Per user decision 2026-05-07: future Slate chunks (SLATE-3+) skip formal-model verification — spec-first applies only to FS invariant-bearing components.** Concurrency model is fundamentally different from /ctl/'s SERIAL-per-server: slate **MUST** accept connections concurrently (one JOINABLE pthread per accepted connection per R114 P2-1; worker pool drained at shutdown) because /redraw is a blocking-read primitive — a renderer holding /redraw must NOT block another connection's /event write. Trust boundaries: (1) wire framing — same 4-byte LE size header + body discipline as stratumd's serve_ctl_client; per-connection bounded by msize. (2) Per-connection peer credentials — SO_PEERCRED on Linux / getpeereid() on macOS/BSD; refuse-by-default on resolution failure (R95 P2-2 carry); opt-in `--allow-unauth` for testing. (3) Mutex-cv discipline (load-bearing concurrency) — every /version mutation goes through `s->mu`; /redraw blocking-read does cond_wait on `s->cv`; every state-mutating action (dispatch_event, set_status, log/append write, attach, disconnect) MUST `pthread_cond_broadcast(&s->cv)` so blocked readers wake. Without the broadcast, /redraw would never wake → renderers stuck. (4) Stop discipline — `stm_slate_stop` sets a stop flag and broadcasts cv so blocked /redraw readers return immediately; daemon shutdown calls this BEFORE worker join (otherwise blocked readers hold their pthreads alive past process exit-attempt). (5) /event input bounds — STM_SLATE_EVENT_LINE_MAX (256 bytes) + zero-byte refusal (R101 P2-2 carry; same posture as /ctl/'s writable kinds). (6) Per-fid materialized body bounded at STM_SLATE_BODY_MAX (4 KiB); snprintf-then-check pattern. (7) Log ring buffer — STM_SLATE_LOG_LINES (100) most recent lines, STM_SLATE_LOG_LINE_MAX (320 bytes) per line; oldest dropped on overflow (no growth past cap). (8) Signal-mask discipline (R113 P1-1 carry from stratumd) — every per-connection worker pthread is spawned with SIGINT/SIGTERM/SIGHUP/SIGQUIT BLOCKED so signals route to the accept thread; broken posture = signal-routing deadlock at shutdown. (9) FIFO event dispatch — events processed in arrival order (slate.tla::EventFIFO); future writable kinds (panels/X/action, dialogs/.../result, editor/action — SLATE-3..5) MUST go through the same mu-guarded dispatch path so version bumps stay atomic and FIFO holds across all mutation surfaces. (10) **SLATE-2 backend lifecycle**: `stm_slate` holds an optional `stm_9p_client *backend` for the attached stratumd. ConnectionAtomic invariant: `connected == (socket_len > 0)`; PanelPathConsistent: connected ⇒ panel paths = "/", disconnected ⇒ panel paths = "". A SECOND mutex `backend_mu` serialises every libstratum-9p call against the held client (the lib is one-op-at-a-time per connection — see include/stratum/9p_client.h §Concurrency); without it, two slate connections both reading /panels/X/entries would corrupt the wire framing. backend_mu also pins the backend pointer for the duration of an op so Disconnect can't tear it down concurrently. **Lock ordering is backend_mu → mu**, never the reverse — Attach takes backend_mu, then briefly s->mu to swap state, then closes the prior client outside s->mu but UNDER backend_mu. Future writable kinds that interact with the backend (e.g., SLATE-3's panels/X/action descend-dir) MUST adopt the same lock-ordering discipline; never call into stm_9p_* without holding backend_mu. (11) **SLATE-2 attach trust posture**: /connection/attach accepts ARBITRARY socket paths at v2.0 — writing arbitrary paths doesn't exfiltrate data but a malicious local writer COULD redirect the panel to a different dataset. v1.1 may add per-uid ACL on attach (forward-noted; SLATE-DESIGN §7.1). Path bounded at STM_SLATE_SOCKET_MAX (1023 bytes); embedded NUL refused. Empty body = disconnect verb (NOT zero-byte refusal — clause 5's R101 P2-2 carry exempts /connection/attach because zero-len IS the documented disconnect verb). (12) **SLATE-2 panel-entries materialisation**: /panels/X/entries does its backend op (Twalk + Tlopen + Treaddir + per-entry Twalk + Tgetattr + Tclunk) at vops_lopen, OUTSIDE s->mu but UNDER backend_mu; result is cached in per-fid bulk_buf and served on subsequent reads — matches /log/tail's R114 P1-1 bulk_buf pattern. STM_SLATE_ENTRIES_MAX (200) caps body at ~64 KiB. **R115 P1-1 sanitization (load-bearing trust boundary)**: every entry name is sanitized at the line-render step — bytes < 0x20 OR == 0x7F replaced with '?' before snprintf into the line-oriented body. The ORIGINAL name is still used for the per-entry Twalk so the real backend file is resolved; only the rendered display gets sanitized. UTF-8 multi-byte sequences (≥ 0x80) pass through unchanged. This is the R99 P2-1 doctrine class extended to the entries-line surface — `fs_validate_dirent_name` only rejects '/' + '\\0' (POSIX permits anything else), so the safety boundary lives at the slate display layer. Future writable kinds that emit user-controlled strings into line-oriented bodies (e.g., SLATE-4 dialogs/.../body, SLATE-5 editor/active) MUST adopt the same posture. SLATE-3 will introduce `/panels/X/cursor` + `/panels/X/action` (writable); the action handler MUST inherit (a) backend_mu discipline for any descend-dir op, (b) version bump under s->mu, (c) cursor revalidation against the panel's current entries snapshot. (13) **walk_to_cwd path-depth handling** (R117 P3-3 closure): `walk_to_cwd` parses cwd components into a heap-allocated buffer (bounded by ~131 KB worst case at STM_SLATE_PATH_MAX = 1023). For ≤ STM_9P_MAX_WALK (16) components a single Twalk binds out_fid; for deeper paths the function ping-pongs between two scratch fids (SLATE_BACKEND_WALK_A_FID = 204, WALK_B_FID = 205), walking STM_9P_MAX_WALK components per iteration into the alternating scratch, with the LAST batch walking into out_fid (caller's intended destination). Each iteration clunks the previous source fid before the next walk so the scratch fid pool stays at exactly two through the entire walk. The per-entry walk in panel_entries_render is fixed via SLATE-3b's CWD_FID clone (clause 15 (a)) — every per-entry Twalk issues from CWD (the panel's current cwd), not from ROOT. Both pieces of clause 13's original forward-note are now closed. (14) **SLATE-3a panel cursor + action**: /panels/X/cursor is the FIRST RW kind on slate (mode 0644 — readable AND writable on the same fid). vops_lopen now uses an accmode-vs-mode-bits gate (instead of a per-kind writable flag) so RDONLY/WRONLY/RDWR opens are validated against KIND_META[k].mode (0444 ⇒ readable, 0222 ⇒ writable). Read-capable opens materialise a body at lopen; write-only opens skip materialisation. Cursor write parses decimal — every byte must be a digit, leading-`-` refused, uint32_t overflow refused, empty-body refused, oversized refused. Cursor reset to 0 on attach + disconnect (alongside path reset). /panels/X/action is the FIRST writable verb-dispatch surface on slate; v1 supports `key Up` / `key Down` (cursor adjustment with clamp-at-0 for Up + clamp-at-UINT32_MAX for Down — though Down clamping is academic at v1 since the renderer enforces real bounds). Unknown verbs return STM_ENOTSUPPORTED so renderers know the action wasn't dispatched. SLATE-3b+ will add `key Enter` (descend into highlighted dir — composes with the deeper-cwd refactor in clause 13), `key F3` (view as file), and mouse verbs. Version bumps on cursor write AND on action verb that actually moved cursor; no-move actions (key Up at cursor=0) do NOT bump version (matches "version changes only via real state mutation" doctrine). (15) **SLATE-3b descend trust boundaries** (key Enter verb + nested-cwd refactor): (a) `panel_entries_render` clones WORK_FID → CWD_FID via Twalk(WORK, CWD, n_names=0) BEFORE Tlopen on WORK; per-entry Twalks issue from CWD_FID so nested cwds resolve correctly. CWD_FID stays UNOPENED so `Twalk(CWD, ENT, [name])` is unambiguously valid in 9P2000.L (walking from an opened fid is server-defined). All error paths clunk CWD before unlocking backend_mu. (b) **`descend_panel` name-injection refusal** (R117 P1-1, R115 P1-1 doctrine carry to write-site): if the cursor's entry name contains a byte < 0x20 OR == 0x7F, descent is REFUSED with STM_EINVAL — sanitization-on-storage would diverge from the real backend name (panel.path is ALSO used as a key for subsequent walk_to_cwd lookups), so refusal is the only consistent posture. UTF-8 multi-byte sequences (≥ 0x80) pass through. Future writable kinds that store user-controlled strings AND use them as backend-op keys MUST adopt the same refusal posture; sanitization-on-display only suffices when the storage isn't a key. (c) **Cursor cap parity** (R117 P3-1): descent refuses cursors ≥ STM_SLATE_ENTRIES_MAX (200) so the backend readdir scan terminates promptly — parity with panel_entries_render's truncation cap. A cursor beyond the rendered cap can't correspond to a user-visible entry. Bounds the duration backend_mu is held under malicious cursor writes. (d) **Action verb dispatch table** (R104 + R116 P3-3 doctrine carry): action verbs are now table-driven (`action_verb_row[]`) — SLATE-3c+'s additional verbs (key F3 view, mouse, "ascend") just append a row. (e) walk_to_cwd's depth cap is now lifted via iterative re-walks (clause 13). (f) Forward-note: there's no "ascend"/parent-dir verb yet; once descended, the only way back to "/" is disconnect+reattach. SLATE-3c will add the companion verb. (16) **SLATE-3c-ascend trust boundaries** (key Backspace verb): (a) ascend is pure path-truncate, NO backend op required — relies on R117 P1-1's refusal posture at descend write-site to keep stored panel.path always sanitized (no control bytes), so ascend never has to revalidate stored bytes; (b) version bump only on real path change (root no-op skips per R116 P3-2 doctrine); (c) cursor reset to 0 on every successful ascend (parity with descend); (d) defense-in-depth path_len bound check at entry (R118 P3-1) — guards against a future writable kind that bypasses the STM_SLATE_PATH_MAX cap; (e) future verbs that mutate panel.path (e.g., `goto` to absolute path) MUST EITHER (i) re-validate stored bytes against the R115 P1-1 / R117 P1-1 refusal class, OR (ii) operate ONLY on shrinkage of a previously-validated path (ascend's posture). (17) **SLATE-3c-selection trust boundaries** (/panels/X/selection RW): (a) bitset of size SLATE_SELECTION_BYTES = (STM_SLATE_ENTRIES_MAX+7)/8 = 25 bytes per panel; bit (idx % 8) of byte (idx / 8) set iff entry idx is selected; (b) **strict parser** (parse_selection_bits) — comma-separated decimal indices only, no whitespace, no leading zero (except literal "0"), no leading/trailing/double comma, indices ≥ STM_SLATE_ENTRIES_MAX (200) refused (R117 P3-1 cursor-cap-vs-render-cap parity carry); on any parse error the caller's bitset is NOT modified (parse-into-scratch-then-memcpy pattern); (c) **reset-on-cwd-change invariant** (load-bearing) — selection indices reference the current panel's visible entries; cwd change invalidates the index space, so memset(selection, 0) fires at every cwd-changing transition: attach_state_swap_locked, disconnect_state_swap_locked, descend_panel, ascend_panel. A future writable kind that mutates panel.path WITHOUT clearing selection would surface stale-index bits as "selected" against the wrong directory; (d) version bump only on real change (memcmp old vs new bitset; R116 P3-2 doctrine carry); (e) materialize emits sorted comma-separated indices + trailing newline, OR just "\n" when empty — clients reading must distinguish "1 byte" (empty) from "0 bytes" (which slate doesn't emit for this kind); (f) cursor write does NOT clear selection — cursor and selection are independent navigation/membership concerns. (18) Forward-note: SLATE-3c does NOT yet implement key F3 / mouse verbs, `goto` to absolute path, /dialogs/.../* (SLATE-4), /editor/* (SLATE-5), or /admin views (SLATE-7 gated on /ctl/-on-stratumd). The synfs schema must stay backward-compatible as those land — clients that hard-code v1.0 / v1.1 / v1.2 / v1.3 / v1.4 paths must continue working. (19) Slate-wide offset-ignored Twrite posture: every writable kind on slate (event, log/append, conn/attach, panel/X/cursor, panel/X/action, panel/X/selection, dialogs/X/result) IGNORES the Twrite `offset` parameter and treats every Twrite as a complete payload. POSIX append semantics (offset > 0) would mismatch the synthetic-FS state-replacement model. SLATE-4b+ writable kinds (dialogs/X/input, editor/content) MUST adopt the same posture explicitly, or this clause needs uplift to a struct-comment level constraint with explicit refusal of offset > 0. (20) **SLATE-4-confirm trust boundaries** (/dialogs subtree): (a) dialog ids are MONOTONIC and NEVER reused (R29 P3-1 doctrine carry — saturation at UINT64_MAX returns STM_EOVERFLOW rather than wrap); id 0 reserved for "no active dialog" sentinel; (b) qid encoding: kind in high byte, dialog id in lower 56 bits — `qid_of_dialog(kind, id)` constructs, `qid_dialog_id(qid)` extracts. Static kinds (DIALOGS_DIR, DIALOG_STACK) have id=0; dynamic kinds (DIALOG_DIR + leaves) carry the id; (c) **stale-id discipline** (load-bearing) — every dialog walk + materialize re-checks `s->dialog.active && s->dialog.id == did` under s->mu and returns STM_ENOENT on mismatch (mirrors POSIX inode-gen-stamp semantics from fid.tla). Without this, a dismissed-then-reopened dialog could leak old state to clients holding stale fids. The lp9 server's per-fid qid binding plus this re-check provides confused-deputy resistance; (d) **strict input validation** (R115 P1-1 / R117 P1-1 doctrine carry) — title and options are SINGLE-LINE (refuse all bytes < 0x20 + == 0x7F); body permits embedded '\n' (multi-line) but refuses all OTHER control bytes; result write must be a token-equal match against options (no prefix matching, no whitespace). UTF-8 multi-byte (≥ 0x80) passes through; (e) **SLATE-4-confirm v1.0 single-dialog stack — superseded by SLATE-4b multi-dialog stack** (clause 22 below). (f) /dialogs/<id>/result is the FIRST dynamic-id writable kind on slate; future writable dialog kinds (e.g., the SLATE-4b input field) MUST inherit the stale-id re-check + the validate-against-options-style strict input refusal. (21) Forward-note: SLATE-4-confirm + SLATE-4b do NOT yet implement key F3 view, mouse verbs, /editor/* (SLATE-5), or /admin views (SLATE-7). The synfs schema must stay backward-compatible as those land — clients that hard-code v1.0..v1.6 paths must continue working. (22) **SLATE-4b multi-dialog stack + input kind trust boundaries**: (a) the slate state holds an array `s->dialogs[STM_SLATE_DIALOG_MAX_ACTIVE]` (4 slots at v1.0); each slot is FREE (active=false) or ACTIVE (active=true). Slot lookup by id is a O(N) linear scan via `dialog_slot_for_id_locked`. Slots become FREE again on result-write OR programmatic cancel (`stm_slate_dialog_cancel`). (b) **DialogStackLIFO invariant** (informal carry from SLATE-DESIGN §11): only the topmost dialog accepts result writes — the "top" is the active slot with the highest id (since ids are allocated monotonically, top = most-recently-opened). Result-write to a non-top dialog returns STM_EBUSY; renderers MUST dismiss the topmost first. The `dialog_top_id_locked` helper is the source-of-truth for "top". (c) **/dialogs/stack output**: comma-separated active ids in ASCENDING order (newest last per SLATE-DESIGN §3) with trailing newline; empty = just "\n". `dialog_collect_sorted_locked` does insertion-sort (N=4, trivial) into a caller-provided buffer. (d) **/dialogs readdir output**: emits "stack" + one decimal-name dirent per active id in ascending order. The id snapshot is taken under mu and then the emit callbacks run OUTSIDE mu (callbacks may take other locks for buffering — same posture as /pools readdir). (e) **input kind**: new `slate_dialog_kind` enum value `INPUT`; new SLATE_KIND_DIALOG_INPUT (= 33; SLATE_KIND_MAX = 34); /dialogs/<id>/input is mode 0644 RW, materialised on RDONLY/RDWR opens, write path validates len ≤ STM_SLATE_DIALOG_INPUT_MAX (256) + control-byte refusal. The `input` field on the dialog directory is emitted at walk + readdir ONLY for slots where `kind == INPUT` — confirm-kind dialogs do NOT expose /input (walk returns ENOENT, readdir omits the entry). vops_write defense-in-depth re-checks slot.kind == INPUT before storing (a malicious client constructing the qid directly can't bypass the walk gate). (f) **last_dismiss completion record** (single-record, mu-protected): at result-write time the daemon snapshots (id, kind, result, input) into `s->last_dismiss`; the consumer queries via `stm_slate_dialog_consume(id)` which (under mu) compares id, copies result + input into caller buffers, clears the record, and returns STM_OK on match / STM_ENOENT on mismatch. **v1.0 limitation**: rapid back-to-back dismisses overwrite each other's records; consumers should consume promptly after detecting dismiss via /redraw + /version. v1.1 may add a per-id consumption queue; documented in slate.h's `stm_slate_dialog_consume` doc. (g) **stale-id discipline** at every entry point — vops_walk (DIALOGS_DIR + DIALOG_DIR), vops_readdir (DIALOG_DIR), vops_lopen (DIALOG_KIND/TITLE/BODY/OPTIONS/INPUT), vops_write (DIALOG_RESULT/DIALOG_INPUT) — re-checks `dialog_slot_for_id_locked(s, did) >= 0` under s->mu and returns STM_ENOENT on mismatch. The R29 P3-1 doctrine carry. (h) **R116 P3-2 doctrine**: input-write only bumps version on REAL change (memcmp old vs new under mu); zero-length post-strip ("\n" alone) is treated as "clear the field" and counts as a change iff the field was non-empty. (i) **R29 P3-1 saturation**: dialog_open_locked refuses with STM_EOVERFLOW if next_dialog_id == UINT64_MAX (carries the SLATE-4-confirm posture; never wraps). (j) **Programmatic cancel** (`stm_slate_dialog_cancel(s, id)`): clears the slot, bumps version, but does NOT write to last_dismiss — consumer that polls last_dismiss after a programmatic cancel sees ENOENT (correct: there's no result to convey). (k) Per user policy 2026-05-07, slate doesn't carry a formal-model spec extension for SLATE-4b — the multi-dialog invariants (DialogStackLIFO + slot-pool-allocation + monotonic-non-reuse) are enforced in code only, with comprehensive unit + e2e socket tests covering each. (23) **SLATE-5a editor scaffold + open verb trust boundaries**: (a) /editor subtree adds 7 kinds (EDITOR_DIR, EDITOR_ACTIVE, EDITOR_FILENAME, EDITOR_CONTENT, EDITOR_CURSOR, EDITOR_MODIFIED, EDITOR_ACTION; SLATE_KIND_MAX = 41). All read-only at v5a (mode 0444); SLATE-5b will lift content + cursor to RW (mode 0644) and add save/quit/revert/save-and-quit verbs at /editor/action. v5a's /editor/action accepts NO verbs — every write returns STM_ENOTSUPPORTED (with R101 P2-2 zero-byte refusal taking priority). (b) **Backend lifecycle for editor open** (composes against clause 10): `stm_slate_editor_open(s, path, path_len)` follows the lock-ordering discipline backend_mu → mu. Walk root → path via the attached backend (using walk_to_cwd from clause 13; supports paths > STM_9P_MAX_WALK depth via iterative re-walk); Tgetattr to verify regular file (mode & 0170000 == 0100000); Tlopen for read; Tread in a doubling-heap-buffer loop bounded at STM_SLATE_EDITOR_CONTENT_MAX (1 MiB) until EOF; Tclunk WORK_FID; commit new editor state (filename + content + cursor=0,0 + modified=false) under s->mu; bump version + broadcast cv. (c) **getattr-size-as-hint posture** (load-bearing for synthetic-FS interop): synthetic-FS leaves (e.g., slate's own /version) report size=0 from Tgetattr because their bodies materialize at Tlopen time. editor_open uses the getattr size only as a defensive cap probe + initial heap-allocation hint; the actual content size is determined by reading until Tread returns 0 (EOF). The cap-overrun probe (extra 1-byte Tread after hitting MAX) refuses with STM_ERANGE rather than truncating — slate doesn't silently drop bytes. (d) **NUL-termination invariant** (load-bearing trust boundary, R123 lesson): `path` arrives at editor_open as a (ptr, len) pair, often a slice of a network read buffer (e.g., the Twrite body for /event "editor open <path>"). The slice is NOT NUL-terminated within the buffer; adjacent buffer memory may contain unrelated bytes. editor_open MUST copy `path` into a NUL-terminated stack buffer BEFORE forwarding to walk_to_cwd (which parses up to '\0'). Failure to do so reads past path_len into adjacent memory and produces walks against attacker-influenced paths. (Validation already enforces no embedded NULs, so the copy + path[len]='\0' is unambiguous.) Future paths-as-keys at slate that go through walk_to_cwd MUST adopt the same posture. (e) **Path validation** (R117 P1-1 doctrine carry — storage-as-key): control bytes < 0x20 OR == 0x7F refused; embedded NUL refused; non-absolute (no leading '/') refused; oversize (> STM_SLATE_EDITOR_FILENAME_MAX = STM_SLATE_PATH_MAX = 1023) refused. UTF-8 multi-byte (≥ 0x80) passes through. Future paths-as-keys at slate MUST adopt the same validation. (f) **/event verb routing** (NEW writable surface at /event for backend-touching verbs): `stm_slate_submit_event` first logs the event line (FIFO doctrine: every line gets logged), THEN routes recognized verbs ("editor open <path>", "editor close") to the corresponding public APIs. The event line is logged regardless of verb success/failure; verb errors propagate as the function's return code so callers reading via Twrite see Rlerror. v5a only handles "editor open" + "editor close" — future verbs ("editor save", panel-targeted verbs at SLATE-3 level) MUST be routed via the same prefix-match dispatcher AND the prefix MUST include a trailing space (or exact match) so e.g. "editor closed" doesn't match "editor close". The verb dispatch happens AFTER the log so failed verbs still surface in /log/tail (debugging surface). (g) **No editor sanitization on /editor/content** (deliberate): editor content is whatever the file contained. The editor IS a text-editing UI; control bytes and binary data are part of the file the user is editing. Sanitization happens at the renderer layer (which decides whether to display, hex-dump, refuse, etc). Slate's responsibility is to faithfully transport the bytes. v5a is read-only on /content; SLATE-5b lifts to RW with the same posture. (h) **Single editor session at v1.0**: `s->editor_*` fields are unitary (no array). Opening a new file while another editor is active drops the prior buffer (modified-state lost; no save). Per SLATE-DESIGN §10 forward-note, multi-pane editor for v1.1+. (i) **`stm_slate_editor_close` is no-op when inactive** (R116 P3-2 doctrine carry): no version bump on inactive close — version changes only via real state mutation. Likewise, `stm_slate_editor_open` of a path that walks to itself doesn't bump version on identical content (currently NOT implemented; identical-path/content reload always bumps; future enhancement). (j) **SLATE-5b shipped at next commit**: /editor/content + /editor/cursor RW (mode 0644). /editor/action accepts verbs save / quit / revert / save-and-quit (unknown verbs return STM_ENOTSUPPORTED; zero-byte EINVAL via R101 P2-2). New `editor_original` heap-allocated baseline copied at open + at save; modified-tracking via `editor_recompute_modified_locked` (memcmp content vs original; len-aware). Save flow: walk_to_cwd → Tlopen WRONLY|TRUNC → Twrite chunked at iounit → Tfsync → Tclunk; on success replace editor_original with content snapshot. Save's partial-write failure leaves the file partially overwritten on the backend AND modified=true so the user knows; v1.1 may switch to atomic temp-and-rename. Revert delegates to editor_open(filename) which re-walks + re-reads; resets cursor to 0,0 (matches editor_open's contract). Save-and-quit chains save then close. Cursor parser refuses empty body, missing comma, empty row/col, non-digit, oversize. Content write is a complete-payload-replace (clause 19 offset-ignored carry); R116 P3-2 doctrine — bump version only on real change (memcmp vs current). NO control-byte sanitization on /content (clause 23(g) carry — editor IS a text-editing UI; bytes flow through unchanged). (24) **Cross-connection fid-uniqueness constraint** (load-bearing — discovered during P9-SLATE-6): `s->sessions[STM_SLATE_MAX_SESSIONS]` is keyed by `fid` ALONE (`session_get_locked` matches the first slot whose `active && fid == requested_fid`). Slate accepts CONCURRENT connections (clause 9). The lp9 server's per-connection fid table is independent, so two connections can both bind fid 101 → both lopens allocate slate sessions with `fid==101`; subsequent reads on the second connection collide on the first session and surface as Rlerror with mapped EBACKEND→EIO at the client. Pre-existing /ctl/ comment block at `v2/src/ctl/synfs.c` §"Why the mutex isn't enough" (lines 70–98) describes the same constraint as a forward-note — but slate's concurrent-accept regime makes it observable in practice. **Workaround for clients**: use globally-unique fid ranges per connection. The C-side socket tests dodge it via 100/101 vs 200/201 (`test_slate_socket.c`); the Rust client (`v2/tools/stratum-slate-tty/src/slate.rs::SlateClient::dial`) mints unique fid bases from a process-wide `AtomicU32` counter (16-stride per instance). Future slate clients MUST adopt the same posture OR slate must be fixed to extend the session key to `(server_idx, fid)` (the same fix /ctl/ §83 forward-notes); fixing slate requires a vops API change to pass the lp9_server pointer to vops callbacks — out of scope for SLATE-6, forward-noted as a separate chunk (P9-SLATE-FIX-SESSIONS). (25) **SWISS-4q-supervise auto-reconnect** (`ddd688a`; on the slate-client-side helpers in slate.c's panel paths): `is_conn_broken_status(rc)` predicate covers STM_EBACKEND / EIO / EPROTOCOL. `log_panel_event(s, fmt, ...)` writes a diag line into /log/tail via the standard event-FIFO. `panel_reconnect_locked(s, panel_idx)` is called from every error branch of `panel_entries_render`: caller holds backend_mu; snapshots socket path under s->mu; dials a fresh `stm_9p_client *` into a temporary; on success swaps it into panel.backend under s->mu briefly; closes the old client OUTSIDE s->mu but UNDER backend_mu. Retry posture is `goto retry` with max-1-attempts so a persistent-broken socket fails fast rather than infinite-loops. **Retry-from-walk_to_cwd discipline** (load-bearing): a fresh dial binds ONLY ROOT_FID; WORK / CWD / ENT / WALK_A / WALK_B are unbound on the new client — retry MUST restart from walk_to_cwd (clause 13). Future error paths in panel state mutation (e.g., descend/ascend if they migrate to the auto-reconnect pattern) MUST inherit this. The C-side test_slate_socket harness has a smoke test (R128 audit forward-noted: harness limitation — detached per-conn pthreads outlive their slate fixture, so log-content assertions can't deterministically observe the reconnect; manual repro on real terminal-focus-switching remains the validation path). Lock order (slate): `panel.backend_mu` outer → `s->mu` inner. |
| stratum monolith binary (v2) | `v2/tools/stratum/{Cargo.toml,build.rs,src/{main,embed,landing,tui,slate,ui,ffi}.rs}`, `v2/include/stratum/cmds.h`, `v2/src/cmd/*/run.c` | (SWISS-1; v2-only) Stratum's deployment shape: single statically-linked binary `stratum` that bundles every cmd-line tool plus the FAR-Commander TUI (SLATE-DESIGN.md §12). Subcommand dispatch — `stratum serve|slate|mkfs|fs` FFI-calls the same C entry points the standalone binaries use (`stm_cmd_<name>_main` in `cmds.h`, exported by `libstm_cmd_<name>.a`); `stratum tui` runs the renderer; `stratum tui --vol VOL` auto-spawns daemons by re-execing `current_exe` for `serve` + `slate`; no-arg `stratum` shows the Veracrypt-shaped landing screen. Trust boundaries the reviewer must check: (1) **FFI argv translation** — every `ffi_dispatch` call MUST preserve the original tool name as `argv[0]` so the C-side usage messages remain consistent (R125 doctrine carry — each tool's error messages are the source of truth for its flags; the swiss-army does NOT re-interpret them). The CString backing memory MUST stay alive for the duration of the call. (2) **Re-exec child spawn** (`embed.rs::run`): the embedded `tui --vol` mode spawns stratumd + slate as child processes via `Command::new(current_exe()).args(...)`. Each child gets its own process address space (no signal-handler conflict per R125 doctrine carry); shutdown is SIGTERM with 1s grace then SIGKILL. The session dir under `$XDG_RUNTIME_DIR/stratum-<pid>/` (Linux) or `$TMPDIR/stratum-<pid>/` (macOS) is per-pid, removed on shutdown — but on crash, leaks (forward-noted to SWISS-N: PID-stamped cleanup script, OR move to `mktemp -d`). (3) **Pre-attach via SlateClient** — before entering the TUI loop, `embed.rs::attach_slate` writes the stratumd socket path to slate's `/connection/attach`. This bumps slate's version + makes panel state immediately readable for external clients (the user's "Claude can drive slate" workflow per `--print-env-to`). The pre-attach uses the same SlateClient that R125 audited; unique-fid-base discipline preserved. (4) **Headless mode signal handling** (`embed.rs::run_headless`): blocks on SIGINT/SIGTERM via `pthread_sigmask(SIG_BLOCK)` + `sigwait`. The mask is set by the *current* (Rust main) thread; child processes have already inherited the unblocked default mask before this point. Future async-process management via signalfd would be cleaner on Linux; macOS sticks with sigwait. (5) **`--print-env-to` trust posture**: writes `STRATUM_SLATE_SOCK=…` and `STRATUMD_SOCK=…` in shell-source-able format. Any local user with read access to the file can dial slate or stratumd. v2.0 trusts everyone with filesystem access to the env file (mode is whatever `fs::write` gives — typically 0644). v1.1 may add explicit chmod 0600 + a note that the file should land in a per-user dir. (6) **Refactored cmd entry points** (`v2/src/cmd/*/run.c`): each daemon's `main()` body factored into `stm_cmd_<name>_main(argc, argv)` exposed via `cmds.h`; the standalone binary's `main.c` is a one-line wrapper. Tests linking the underlying libs (`stm_stratumd`, `stm_slate`) are unaffected — the run-main logic lives in `stm_cmd_<name>` libs which are NOT pulled into tests. (7) **Subcommand routing collision**: each subcommand dispatcher passes `args[2..]` to the FFI entry point with the original tool's `argv[0]` reconstructed — no leakage of `stratum` parent argv. (8) **Build — static link discipline** (`build.rs`): auto-discovers every `libstm_*.a` + `libblake3.a` + `libxxhash.a` under `v2/build/src/**` + `v2/build/third_party/`. Linux uses `--start-group/--end-group` to handle circular static-lib deps; macOS auto-resolves. `libsodium` linked dynamically via pkg-config (mirrors cmake). The `stm_testlib` is filtered out — production binary doesn't want it. Future Rust crates that link the umbrella SHOULD reuse this build.rs as a template. (9) Forward-notes: SWISS-1 ships subcommand dispatch + headless mode + landing + auto-spawn; SWISS-2 lands the volume-map visualization (donut + snapshot timeline + integrity bar — the F2 view per SLATE-DESIGN §12.5). The slate-tty crate (`v2/tools/stratum-slate-tty/`) is functionally superseded by `stratum tui` but kept in tree until SWISS-2 stabilizes the chrome (forward-noted archive in SWISS-N). |
| stratum TUI worker threads + spinner + intra-file polling | `v2/tools/stratum/src/tui.rs` — `CopyWorker { handle, dst_name, dst_poll_path }`, `DeleteWorker { handle, item_name }`, `spawn_copy_worker`, `advance_copy_batch`, `advance_delete_batch`, `clear_panel_selection`; `v2/tools/stratum/src/ui.rs` — `BatchKind { Copy, Delete }`, `CopyProgress.kind`, `spinner_glyph(elapsed_secs) -> char`, `draw_copy_progress` conditional render; static `COPY_BYTES_DONE: AtomicU64`, static `INTRA_FILE_BYTES: AtomicU64` | (SWISS-4q-worker + SWISS-4q-worker-bytes; v2-only) Per-item batch ops (copy + delete) run on dedicated `thread::spawn` workers so the main UI loop doesn't block. Trust boundaries: (1) **Atomicity** — COPY_BYTES_DONE migrated from thread_local Cell to static AtomicU64; worker writes via `fetch_add(Relaxed)`; main loop reads via `load(Relaxed)`. INTRA_FILE_BYTES is the host-fs dst's "in-flight bytes" — set by the main loop's metadata stat at ~100ms tick (~10 μs cost) when a copy worker is running; the worker doesn't touch it. (2) **Memory ordering**: worker's `handle.is_finished()` observation by the main loop must happen-before the main loop's promotion of INTRA_FILE_BYTES → COPY_BYTES_DONE. The Rust `JoinHandle::is_finished` returning true synchronises-with the worker's exit. (3) **Bytes accounting rule** (load-bearing — the 2× blip fix): host-fs dst SKIPS `bump_bytes` inside perform_copy_one; main loop promotes the final intra → COPY_BYTES_DONE at worker-complete (then zeros intra). Stratum-fs dst BUMPS inside perform_copy_one — no polling available (stratum stat is a subprocess, too expensive). Rule lives in the `match (src_meta, dst_meta)` block comments. (4) **Selection-clear posture** (the other half of the 2.8 GB amplification chain): every selection-consuming verb that doesn't change cwd MUST `clear_panel_selection(client, focus)` at BatchTick::Done — writes `"\n"` to `/panels/<focus>/selection`. Cwd-changing verbs (descend / ascend) clear selection naturally via slate's reset-on-cwd-change invariant (CLAUDE.md slate row clause 17(c)). Failure to clear means stale indices grab unrelated files on the next batch (the original 2.8 GB user-reported bug). (5) **Spinner derivation**: `spinner_glyph(elapsed_secs)` returns one of 10 braille chars (`⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏`) at 12 fps via `(elapsed_secs * 12.0) as usize % 10`. Deterministic per (start_time, current_time) tuple — no atomic counter. (6) **Worker lifecycle on panic**: workers don't `join()` on drop — process exit reclaims their threads via OS cleanup. If the main loop panics mid-batch, the worker may still run to completion (writing to disk after the UI dies). v1.0 accepts this; SWISS-4q-worker-cancel forward-notes proper cancel-via-subprocess-kill. (7) **dst_poll_path None for stratum-fs dst**: spinner-only, no % rendering; renderer branches on `progress.bytes_total > 0` AND `dst_poll_path.is_some()`. |
| stratum TUI view-mode dispatch (Files / F2View) | `v2/tools/stratum/src/ui.rs::ViewMode + F2State + F2Pane + F2Focus`, `ui::render` (F2View branch with menu/content split + dialog overlay defense), `v2/tools/stratum/src/tui.rs::handle_key + handle_f2view_key + handle_f2view_menu_key + handle_f2view_content_key + handle_snapgraph_content_key`, `v2/tools/stratum/src/volmap.rs::VolumeMapPoller`, `v2/tools/stratum/src/swiss5_view.rs::render` (Map pane), `v2/tools/stratum/src/snapgraph.rs::SnapshotGraphPoller`, `v2/tools/stratum/src/swiss6_view.rs::render` (Snapshot Graph pane), `v2/tools/stratum/src/swiss7_view.rs::render` (Integrity pane), `v2/tools/stratum/src/ctl.rs::CtlClient` | (SWISS-5..8a / R129; v2-only) `ViewMode::Files / F2View` selects between the dual-pane file browser and the unified "Volume View" screen. F2 toggles between the two; Esc inside F2View also closes back to Files. F2View renders a vertical-split layout: left menu pane (~22 cols) listing every available inspection surface (Map / Snapshot Graph / Integrity / Encryption / Inspect / Metrics), right content pane showing the selected surface. Tab cycles keyboard focus between Menu and Content; Up/Down moves the menu cursor when focus=Menu OR forwards to the per-pane handler when focus=Content. **SWISS-8i: F2View redraws on poller ticks**: bug discovered while probing user-reported "F2 still says Awaiting first refresh". The pollers were running fine + writing fresh state to their `Arc<RwLock<...>>`, but the main UI loop's inner `event::poll(100ms)` wait only broke on key/resize/redraw_rx signals. None of those fire just because a background poller wrote state, so the renderer slept indefinitely on a stale frame. Fix: extend the inner-loop break condition to include `view_mode == ViewMode::F2View` so every 100 ms tick falls through, causes a redraw, and picks up the poller's latest snapshot. **SWISS-8j: lp9 Tfsync as no-op for synchronous-write surfaces**: `stratum fs <mutating>` issues a post-op Tfsync on ROOT_FID to make CLI writes durable. /ctl/'s lp9 server didn't implement Tfsync — fell through the dispatch default to `Rlerror(ENOSYS)` — so the CLI exited with `EXIT_IO` and the message `post-op sync failed: status=-205` after every snapshot create / delete / scrub-trigger, despite the write succeeding (admin verbs commit synchronously inside the Twrite handler). Fix: add a `h_fsync` handler to `v2/src/lp9/server.c`. If `vops->fsync != NULL`, dispatch to the backend. If NULL (the /ctl/ posture), return `Rfsync` no-op — semantically correct because the surface is already synchronous. Matches Linux fsync-on-synthetic-FS (procfs / sysfs) behavior. Future /ctl/ mutators (rollback, v1.2 ghost mount, etc.) inherit the no-op via the same path. **SWISS-8g: async /ctl/ admin jobs (no UI freeze)**: snapshot create + delete + scrub-trigger previously blocked the UI on `child.wait_with_output()`; if stratumd took > 1 s (snap create flushes dirty buffer per SWISS-4q-flush-snapshot, can be slow), the TUI froze. Now: `CtlJob { rx, success_msg, _join }` in `run_ui` carries the background `std::thread::spawn` + mpsc channel. The main loop polls `advance_ctl_job(&mut local_dialog, &mut ctl_job)` on every tick BEFORE rendering, and breaks out of the inner poll loop while a job is in flight so it ticks at 100 ms cadence. The `spawn_ctl_job(ctl_sock, p9_path, body, success_msg)` helper centralizes the subprocess pattern (`stratum fs -s … write …` + stdin pipe + wait + status parse) — all three current admin writes (snapshot-create, snapshot-delete, scrub-trigger) and any future writable kinds MUST go through it. While the job runs, `local_dialog` shows an `info_dialog` "Working…" message; on completion the dialog is replaced with success (info_dialog) or error (error_dialog with first stderr line). **SWISS-8h: F8 scrub-trigger in Integrity pane**: F8 with focus=Content + pane=Integrity opens a Confirm dialog ("Start a pool scrub now?" — default selection No). On Yes, `ConfirmAction::ScrubTrigger { pool_uuid, verb: "start" }` flows into `submit_confirm` which dispatches `spawn_ctl_job` against `/pools/<uuid>/scrub-trigger` with body "start". v1.0 wires only "start"; v1.1 may add "pause" / "resume" / "abort" via the same path (just a different verb body). The pool UUID is pulled from the in-memory `VolumeMapState.pool_uuid` at the moment F8 fires — `volmap_state: Option<&VolumeMapState>` is threaded through `handle_key → handle_f2view_key → handle_f2view_content_key → handle_integrity_content_key`. If no pool is attached yet (volmap state has no pool_uuid), the dispatch shows an error_dialog instead of issuing the verb. **SWISS-8d: F2 pollers discover mid-session /ctl/ sockets**: previously `SpawnCtx.stratumd_ctl_sock` was a fixed `Option<PathBuf>` set at launch time, and `spawn_stratumd` (called when the user opens a .stm mid-session via Enter-on-yellow) didn't pass `--ctl-listen`. Result: the F2 view's pollers were either never spawned (no `--vol` at launch) OR keyed against a stale path (volume opened later). Now: (a) `SpawnCtx.stratumd_ctl_sock: Arc<RwLock<Option<PathBuf>>>` is mutable + cloned into both pollers via `stratumd_ctl_sock_handle()`; (b) every `spawn_stratumd_inner` allocates a sibling `stratumd-ctl-{n}.sock` path, passes `--ctl-listen`, waits up to 2 s for the socket to bind, then calls `set_ctl_sock(path)` which the pollers observe on their next 1 s tick; (c) the pollers idle (no error in state) when path is None and reconnect when a desired path appears or changes; (d) `current_ctl_sock()` accessor for one-shot reads (used by F9 create/delete subprocess paths). "Last attached wins" — mid-session open of a second .stm replaces the F2 view's data source with the new daemon. Pollers spawn unconditionally at `run_ui` entry whenever a SpawnCtx is attached, so even no-`--vol` launches show the F2 view as soon as the user opens a volume. **SWISS-8c: F9 snapshot dialog port** (v1 graphics): F9 in Files mode opens `LocalDialogKind::SnapshotList { snaps, cursor, dataset_id }` — a port of v1's `tui/src/ui.rs::draw_snap_dialog`. 70×20 centered modal, double-line cyan border, " Snapshots " title, header row (`ID Gen Name`), scrollable cursor-highlighted list, footer hint `N:new  R/Enter:rollback  D:delete  Esc:close`. The dialog populates from a snapshot of `SnapshotGraphState.snaps` filtered to `dataset_id=1` (v1.0 hardcoded; v1.1 may add per-panel routing). Verbs: **N** opens the existing `SnapInput` sub-dialog for create; **D** opens a `Confirm("Delete snapshot X?")` dialog whose `on_pick = ConfirmAction::DeleteSnapshot { dataset_id, snap_id, name }`; **R/Enter** opens an info_dialog explaining rollback is gated on v1.1c (which is multi-chunk: spec + C API + /ctl/ kind + TUI + audit). **R115 P1-1 sanitization** carries to the rendered name column via `snapgraph::sanitize_name`. The delete path writes the decimal `snap_id` to `/ctl/datasets/<id>/delete-snapshot` (admin kind 22) via the existing `stratum fs write` subprocess pattern (mirrors the create path). When `/ctl/` is missing, the dialog still opens with an empty list + the v1 "(no snapshots — press N to create)" hint; N then fails with a clear error rather than the prior raw "/ctl/ socket missing" error message. **Shift+F<n> shortcuts (SWISS-8b)** pre-select a specific pane on F2View entry. The Files-mode F(n) handler intercepts Shift+F3/F5/F6/F8 BEFORE the slate forward-noop, sets `f2_state = F2State::with_pane(...)`, and toggles into F2View. Same R129 P2-1 modal-active gate as the plain F2 toggle. Mapping: Shift+F3 → Snapshot Graph (Snaps); Shift+F5 → Integrity (Health); Shift+F6 → Encryption (Encryp, placeholder); Shift+F8 → Metrics (placeholder). Shift+F2 stays as host-mount per SWISS-4b; Shift+F7 stays as mkfs per SWISS-4c. Shift+F1/F4/F9 are still forwarded to slate as "key Shift-F<n>" for forward-compat. The Shift-F-key footer in Files mode shows the new labels (`S3 Snaps` / `S5 Health` / `S6 Encryp` / `S8 Metrics`) alongside the existing `S2 Host` and `S7 MkVol`. The previous F4-from-VolumeMap (SnapshotGraph drill-down) and F5-from-VolumeMap (Integrity drill-down) plain bindings are REMOVED — those surfaces are reached via the menu or Shift+F<n> shortcuts now. F4 in Files mode goes back to its original meaning (editor); F5 in Files mode goes back to its original meaning (copy). Trust boundaries: (1) **F2 toggle gate** (R129 P2-1 doctrine — load-bearing): F2 toggle REFUSES when ANY modal is active (`local_dialog.is_some() || editor.is_some() || copy_batch.is_some() || delete_batch.is_some()`). Without this gate, a user could enter VolumeMap with an active confirm-delete dialog whose modal UI is then invisible (the VolumeMap branch doesn't render `local_dialog` overlay by default; defense-in-depth at clause 3 covers async modal sources). (2) **In-VolumeMap key gate** (R129 P2-2 doctrine — load-bearing): in VolumeMap mode, `handle_key` returns `Action::Ignore` for EVERY key that isn't quit (Ctrl-Q / Ctrl-C / F10 / Esc) or F2 (handled above as the mode-toggle). Without this gate, file-browser verbs (F8 delete, spacebar select, F5 copy, F3 view, F7 mkdir, Enter descend, Tab focus-toggle, ...) still fire while the user thinks they're in the dashboard. (3) **Defense-in-depth dialog overlay in VolumeMap branch**: `ui::render`'s VolumeMap branch ALSO renders the dialog + copy-progress overlays so future async modal sources (e.g., poller error surfaced as a notification) are visible in VolumeMap mode. (4) **VolumeMapPoller lifecycle**: 1 Hz background thread holds its OWN libstratum-9p connection (one-op-at-a-time-per-connection per `9p_client.h §Concurrency`). State surfaced through `Arc<RwLock<VolumeMapState>>`. Poller spawn happens in `run_ui` only when `spawn.stratumd_ctl_sock.is_some()` (`stratum tui` without `--vol` skips poll spawn). Drop is not pthread_join'd — process exit reclaims (same posture as the SLATE-6 redraw thread; v1.1 should make it graceful). (5) **Per-instance unique fid bases on CtlClient::dial** (R125 / clause 24 of slate row doctrine carry): `SlateClient`-style discipline — process-wide `AtomicU32` counter, stride 16. /ctl/'s session table key from `(uid, fid)` makes this less load-bearing on `stratumd` than on slate, but the discipline applies for forward-compat with any single-fid-keyed surface. (6) **Read overlay branch ordering**: `ui::render` `match view_mode` is a hard either-or (no overlay between branches); VolumeMap branch must NOT call into the Files branch's draw helpers because the layout split is incompatible (no panel rects allocated). Future view modes inherit the same hard-either-or posture AND inherit clauses (1)–(3) — every new mode MUST be gated against modal activity at the toggle AND must drop file-browser verbs in handler. (7) **SnapshotGraph view + F4 alternate-binding** (SWISS-6; v1.0 read-only): `ViewMode::SnapshotGraph` is reached by drilling down from VolumeMap via F4 (the "alternate-binding" — F4 in Files mode still opens the editor; F4 in VolumeMap mode opens the snapshot lineage view). F2 / Esc from SnapshotGraph returns to VolumeMap (the "back" stop). Same R129 P2-1 toggle gate applies: F4-into-SnapshotGraph refuses when any modal is active. Same R129 P2-2 in-mode key gate applies: in SnapshotGraph, only quit verbs (Ctrl-Q / Ctrl-C / F10), back verbs (F2 / Esc), and cursor navigation (Up/Down/PgUp/PgDn/Home/End) fire; every other key drops via `Action::Ignore`. `SnapshotGraphPoller` (`snapgraph.rs`) parallels `VolumeMapPoller`: 1 Hz background thread, own libstratum-9p connection, `Arc<RwLock<SnapshotGraphState>>`, OS-reclaim-on-process-exit Drop posture. State comes from `/ctl/datasets/<id>/snapshots/<sid>` (S5-PRE-C kind 28) — NO new C-side surface; lineage chain computed in `SnapshotGraphState::lineage_depth` (capped at MAX_LINEAGE_DEPTH = 1000 as defense-in-depth against pathological chains). `MAX_SNAPS_RENDERED = 10_000` caps render cost on pathologically-snapshotted volumes; beyond the cap the state's `truncated` flag fires and the renderer shows a marker. Cursor (`snapgraph_cursor: u32` in `run_ui`) is clamped on every draw tick against `state.filtered_count(filter)` (SWISS-6 v1.1a) so a snap deletion OR a filter narrowing between ticks doesn't dangling-index. R115 P1-1 name-display sanitization carries to `snapgraph::sanitize_name` (UTF-8 multi-byte ≥ 0x80 passes through; ASCII control < 0x20 + 0x7F replaced with '?'). **(7a) SWISS-6 v1.1a — per-dataset filter cycle (F3)**: `snapgraph_filter: Option<u64>` in `run_ui`; `None` = "All", `Some(id)` = restrict to that dataset. F3 in SnapshotGraph dispatches `snapgraph_state.next_filter(current)` which cycles `None → dataset_ids[0] → dataset_ids[1] → ... → None` (dataset_ids sorted ascending). Cursor resets to 0 on every cycle so the user lands on the first visible snap after a filter change (parity with the entry-into-SnapshotGraph reset). Filter resets to `None` on every mode transition (F2 toggle, F4 entry from VolumeMap, Esc back to VolumeMap) for predictable UX. **Stale-filter-id discipline** (load-bearing — R29 P3-1 doctrine carry): the draw tick checks `sg.filtered_count(Some(fid)) == 0 && !sg.snaps.is_empty()` and drops the filter to `None`, so a between-ticks dataset deletion can't strand the user on an empty filter. `swiss6_view::render` takes `filter: Option<u64>` and computes `visible: Vec<usize>` (indices into the full snaps vec) once per draw; cursor indexes into `visible`, NOT into `snaps`; `lineage_depth` still walks the FULL chain so cross-filter parent visualization is preserved (though in practice `prev_snap_id` always references the same dataset per `snapshot.tla::ChainExtentTxgOrdered`). Future writable verbs in SnapshotGraph (v1.1c F8 rollback) MUST adopt the same allow-list extension posture — each new key dispatched via explicit `match key.code` arm; never via fall-through. **(7b) SWISS-6 v1.1b — diff between two marked snaps (Spacebar mark + F5 diff)**: `snapgraph_marks: Vec<u64>` in `run_ui` holds snapshot_ids (NOT indices — references survive snapshot add/delete between ticks); capped at 2 entries. Spacebar in SnapshotGraph toggles the mark on cursor's snap: in marks → remove; not in marks AND len < 2 → push; not in marks AND len == 2 → refuse (user must unmark first). F5 in SnapshotGraph: if `marks.len() != 2` → open `info_dialog` with "Diff requires exactly 2 marked snapshots"; else look up both snap_ids in the current state, call `snapgraph::diff_summary(a, b)`, surface as `info_dialog` (multi-line `LocalDialogKind::Error` with `is_error=false`, dismiss-only). **State-lookup gap**: if a marked snap was deleted between the mark and the F5, the lookup returns None for one side and an info_dialog explains; marks are then cleared. **Marks reset discipline** (load-bearing): marks clear on EVERY transition — F2 toggle, F4 entry, Esc back, AND F3 filter cycle. The F3 case is important: a filter narrowing can hide a marked snap, which would render confusingly (header reads "Marks: 2/2" but the user sees zero ✓ glyphs). The marks-clear-on-filter-cycle keeps header + visible-set coherent. `swiss6_view::snap_row_line` takes `marked: bool` and emits a 2-char leading marker block: `cursor_marker + mark_marker` (each independently '▶'/' ' for cursor, '✓'/' ' for mark); marked-not-cursor rows get a yellow-bold style; marked-AND-cursor rows use the cursor-highlight style (cursor takes precedence). `diff_summary(a, b)` is order-independent (normalizes to older.snapshot_id first) and produces a ≤ 60-col-wide line-oriented body suitable for the 70-col `info_dialog` popup: header, dataset relationship, txg deltas (created/extent with signed_delta helper), lineage relationship (both roots / shared parent / immediate-parent / parents-differ), hold-count delta, flags XOR with changed-bits hex. R115 P1-1 sanitization carries into diff_summary's name fields. Forward-noted v1.1c: F8 rollback + double-confirm. Forward-noted v1.2: F6 mount-snap-RO-as-ghost-pane (requires new fs.h API; gated on a fresh audit). **(8) SWISS-7 — Integrity pane (F5 alternate-binding from VolumeMap; v1.0 read-only)**: `ViewMode::Integrity` is reached by drilling down from VolumeMap via F5 (the "alternate-binding" — F5 in Files mode still copies; F5 in VolumeMap mode opens the Integrity pane). F2 / Esc from Integrity returns to VolumeMap (the "back" stop). **Reuses `VolumeMapPoller` intentionally** — Integrity is a re-projection of the same /ctl/ surfaces (`/pools/<uuid>/status` + `/pools/<uuid>/scrub` + `/pools/<uuid>/devices/<id>/status`) the F2 view consumes, just with different emphasis (health verdict + scrub counters + per-device color-coded table rather than capacity dashboard). No new poller. Same R129 P2-1 toggle gate: F5-into-Integrity refuses when any modal is active. Same R129 P2-2 in-mode key gate: only quit verbs (Ctrl-Q / Ctrl-C / F10) and back verbs (F2 / Esc) fire; ALL other keys drop via `Action::Ignore` (no cursor, no selection at v1.0 — Integrity is a pure status display). ui::render renders dialog + copy-progress overlays in the Integrity branch too as defense-in-depth (parity with VolumeMap + SnapshotGraph). **`compute_verdict` (swiss7_view.rs)**: 4-state — Healthy (all online + no errors), Degraded (any device degraded/evacuating OR scrub failed > 0), Faulted (any device offline/removed/faulted OR fs wedged OR scrub unrepairable > 0), Unknown (pool not attached). Verdict drives the top-bar status color. Per-device row uses Unicode marker glyph + state color: `●` green (online), `◐` yellow (degraded), `↻` yellow (evacuating), `○` dim (offline), `✗` dim (removed) or red (faulted). Future v1.1 verbs (e.g., F8 trigger-scrub via /ctl/pools/<uuid>/scrub-trigger admin write — kind 20, already exists C-side) MUST inherit the allow-list extension posture: explicit `match key.code` arm, no fall-through, admin-gate check client-side BEFORE issuing the /ctl/ write (the C-side gate is the authoritative check; the client-side gate is UX so the user sees "permission denied" before the round-trip). Forward-noted v1.1: F8 trigger-scrub double-confirm; toggleable scrub-pause/resume verbs. |
| stratum-slate-tty Rust renderer (v2 — superseded by `stratum`) | `v2/tools/stratum-slate-tty/src/{ffi,slate,ui,main}.rs`, `v2/tools/stratum-slate-tty/build.rs`, `v2/tools/stratum-slate-tty/Cargo.toml` | (P9-SLATE-6; v2-only) Terminal renderer that dials slate's Unix socket and renders the synfs as a FAR-Commander dual-pane TUI (visual aesthetic mirrors v2/tui/src/ui.rs — double-line borders, blue/dark-gray active/inactive scheme, F-key bar, header columns Name|Modified|Size). Two libstratum-9p connections per process: one for the main thread (state fetch + /event verb writes) and one for the /redraw long-poll thread, since libstratum-9p is one-op-at-a-time per connection (see include/stratum/9p_client.h §Concurrency). Trust boundaries the reviewer must check: (1) **Per-instance unique fid ranges** (load-bearing carry from CLAUDE.md slate row clause 24): `SlateClient::dial` mints a unique fid base from a process-wide `AtomicU32` counter (stride 16). Slate's session table is keyed by fid alone across connections (the slate-side bug forward-noted as P9-SLATE-FIX-SESSIONS); without unique fids per client, the second connection's reads collide on the first's session and surface as STM_EIO. (2) **Caller-cap-bound on every server-supplied count** (R111 doctrine carry): `read_until_eof` caps at READ_CAP (4 MiB) with explicit overflow check; `write_full` clamps at min(iounit, 64 KiB) per chunk. (3) **/event verb routing**: keyboard events translate to `key Up`/`key Down`/`key Enter`/`key Backspace` writes on `/panels/<focus>/action` (NOT `/event` — `/event` is reserved for backend-touching verbs like "editor open"). F1-F9 also forwarded as `key F<N>` for forward-compat with future slate verbs (returns ENOTSUPPORTED at v1.0; renderer ignores). F10 + Ctrl-Q + Ctrl-C are LOCAL quit (no /event write). Tab is LOCAL focus toggle (no /event write — focus is renderer-local at SLATE-3a). (4) **/redraw thread shutdown discipline**: redraw thread holds its OWN libstratum-9p connection. On Ctrl-Q the main thread sets a stop AtomicBool and lets the redraw thread observe it on its next wake. The thread isn't pthread_join'd — process exit closes its connection and OS reclaims its resources; v1.1 should make this graceful. (5) **No editor sanitization on display** (carries clause 23(g) of slate row): /editor/content bytes are displayed as-is via ratatui's Paragraph; control bytes / binary content render as the terminal's mojibake. The renderer is the right layer to add hex-dump or refusal in v1.1. (6) **No interactive dialog dismissal at v1.0**: /dialogs/<id>/result is forward-noted; v1.0 just displays "dialog active" overlay and tells the user to dismiss from another client. (7) **Smoke test harness** (`tests/smoke.rs`): spawns slate via `--allow-unauth`, exercises read + write + concurrent /redraw blocking with proper unique-fid discipline. Six tests pass deterministically with cargo's default parallelism (each test mints a unique socket + unique fid base). Future renderers (Halcyon graphical pane, scriptable monitors) MUST inherit the unique-fid posture OR wait for P9-SLATE-FIX-SESSIONS. |
| stratumd Unix-socket transport (v2) | `v2/src/cmd/stratumd/serve.c`, `v2/src/cmd/stratumd/main.c`, `v2/include/stratum/stratumd.h` | (P9-9P-4; v2-only; P9-CTL-2c added second-listener for /ctl/; P9.5-PARALLEL-1 made /ctl/ CONCURRENT-accept) Unix-socket daemon transport for the 9P server. Up to TWO listen sockets: FS (mandatory) + /ctl/ (opt-in via `ctl_socket_path`). Each accepted connection on FS gets a fresh `stm_9p_server` — one fid namespace per connection per CLAUDE.md / 9p.h doctrine; each accepted connection on /ctl/ gets a fresh `stm_lp9_server` PAIRED with a fresh `stm_ctl_conn` (per-conn caller credentials + per-conn sessions[]; see /ctl/ row clause 13). Each socket has its own CONCURRENT accept loop on a dedicated pthread (FS on the calling thread of run(); /ctl/ on a worker thread spawned by run()). EACH ACCEPT spawns a DETACHED pthread to serve that one client (SWISS-4g pattern, extended to /ctl/ at P9.5-PARALLEL-1); the accept loop never blocks on serve. Pre-P9.5 /ctl/ was one-connection-at-a-time per socket; that workaround is retired. Trust boundaries the reviewer must check: (1) wire framing — read 4-byte LE size header, reject sizes outside [STM_9P_HDR_SIZE, msize_max] (FS) / [STM_LP9_HDR_SIZE, msize_max] (CTL), read body, dispatch; out-of-range size disconnects; (2) peer credentials — SO_PEERCRED on Linux, getpeereid() on macOS/BSD, refuse-by-default on resolution failure (R95 P2-2; opt-in fallback via `allow_unauthenticated_peer`); (3) fs lifecycle — mount/unmount happens in `stm_stratumd_run`, NOT per-connection; concurrent serving against one `stm_fs *` is the regime under which R94 P2-1 (h_reflink stat-after-mutation) and the same-shape patterns at h_lcreate / h_mkdir / h_renameat become observable. The /ctl/ thread also reads stm_fs (via stm_ctl's getattr_at + materializers) concurrently with the FS thread; this is safe under the public stm_fs API's thread-safety contract but extends the R94 P2-1 race surface to /ctl/'s read paths. Reviewer of any concurrent-accept upgrade MUST address that whole stat-after-mutation class. (4) /ctl/ lifecycle ordering (P9-CTL-2c → P9.5-PARALLEL-1): stm_ctl is created AFTER fs mount and AFTER /ctl/ socket bind succeeds, but BEFORE the accept-loop pthread starts; tear-down is reverse — main thread shutdown(2) the /ctl/ listen fd to unblock accept(), pthread_join the accept-loop worker, THEN stm_ctl_destroy (which now BLOCKS on `worker_cv` until every detached per-connection worker has called `stm_ctl_conn_destroy` — the LifecycleNoUAF invariant from `v2/specs/ctl_conn.tla`), THEN stm_scrub_close, THEN unmount fs. The wait-on-worker_cv replaces the pre-P9.5 implicit serial-accept guarantee that "no in-flight worker exists once accept-loop returned"; under concurrent accept the loop returns but detached workers may still be inside vops dispatch. Signal-mask discipline (R113 P1-1): BOTH the accept-loop pthreads AND every per-connection detached worker pthread MUST be spawned with SIGINT/SIGTERM/SIGHUP/SIGQUIT BLOCKED so process-directed signals route to the calling thread (which observes stop_flag → drives shutdown). Pattern: pthread_sigmask(SIG_BLOCK, &block_set) on the calling thread BEFORE pthread_create, pthread_sigmask(SIG_SETMASK, &prev) AFTER, OR the worker installs the block as its first action. Without this, POSIX may deliver the signal to ANY unmasked thread; if it lands on a worker, the worker exits but the accept loop blocks forever in accept() → daemon hangs. Future multi-thread extensions (slate concurrent accept, pluggable auth backends) MUST establish the same convention: main thread receives signals; worker threads have signals masked. (5) per-connection caller stamping (P9-CTL-2c → P9.5-PARALLEL-1): `stm_ctl_conn_create(ctl, peer_uid, peer_gid, &cn)` is called for each /ctl/ accept and the resulting `cn` is passed as the vops ctx to `stm_lp9_server_create`. Caller credentials are IMMUTABLE for the conn's lifetime — no cross-connection state aliasing. `stm_ctl_set_admin_uid(ctl, geteuid())` is called ONCE at startup, BEFORE any worker pthread spawn (the pthread_create barrier provides the happens-before for worker reads of `c->admin_uid`). (6) /ctl/ per-conn sessions lifecycle (R101 P1-1 → P9.5-PARALLEL-1): sessions[] are per-`stm_ctl_conn` (see /ctl/ trigger row clause 7); `stm_ctl_conn_destroy` at connection close frees the sessions[] under the per-conn `mu` AND decrements `stm_ctl::worker_count` under `worker_mu` AND broadcasts `worker_cv` for the destroy-drain waiter. The pre-P9.5 `stm_ctl_drop_all_sessions(c)` is a no-op stub; no inter-connection drain is needed. (7) /ctl/ full attach (SWISS-5 / S5-PRE-A — `b3f234b`): stratumd attaches stm_fs to stm_ctl AND attaches a pool + scrub pair. Sequence after `stm_ctl_create` and BEFORE `stm_ctl_set_admin_uid` (preserving the R97 P2-2 attach-before-pthread_create timing barrier): `stm_ctl_attach_pool(ctl, stm_fs_pool(fs))` → `stm_scrub_create(stm_fs_sync(fs), &scrub)` → `stm_sync_scrub_install_production_cb(stm_fs_sync(fs), scrub)` → `stm_ctl_attach_scrub(ctl, scrub)`. The scrub is daemon-owned, NOT fs-owned (fs has no scrub field; stratumd creates one via the sync getter). Public getters `stm_fs_pool(fs)` and `stm_fs_sync(fs)` are pure read-only accessors returning pointers set at mount time + never reassigned. **Lifecycle ordering on shutdown** (load-bearing — R26 P3-4 doctrine carry from ctl.h "Borrowed references" + scrub.h): servers (FS + /ctl/) shutdown FIRST → `stm_ctl_destroy(ctl)` → `stm_scrub_close(scrub)` → `stm_fs_unmount(fs)` (closes sync). The scrub_close MUST come BETWEEN ctl_destroy and fs_unmount — earlier would leave /ctl/'s scrub pointer dangling; later would tear down sync before scrub releases its sync references. Future chunks that attach additional subsystems to /ctl/ MUST inherit the same shutdown discipline: drop-server-references-first, then-tear-down-subsystem, then-fs-unmount. With S5-PRE-A, /pools/<uuid>/* and /pools/<uuid>/scrub render correctly via stratumd, and /pools/<uuid>/metrics/prometheus gains the full gauge set (pool roster + scrub counters + per-device gauges). |
| FUSE callbacks | `src/cmd/fuse.c` | Same concerns as 9P: every callback is a trust boundary from kernel VFS. Single-threaded loop assumed — multi-threading would need locking. |
| Btree node cache | `src/btree/cache.c`, `stm_btree_read_node`, `stm_btree_write_node` | Cache correctness depends on invalidate-on-write + COW-fresh-paddr invariant. Any future path that writes a node without going through `stm_btree_write_node` breaks the contract silently. |
| Extent write ordering | `src/fs/fs.c::stm_fs_write`, `extent_write_data`, `extent_read_data`; (v2 extension) `v2/src/fs/fs.c::stm_fs_unlink_anon`, `stm_fs_rename` (overwrite branch), `fs_unlink_inode_and_dirent`, `stm_fs_copy_file_range`, `stm_fs_migrate_to_cold`, `stm_fs_promote_to_hot`, `stm_fs_create_snapshot`, `fs_pre_inode_free_cleanup_locked`, `fs_post_inode_free_reclaim_locked` | Insert-before-free, ebuf tail-zero, partial-extent old-lookup are all audit-derived. **SWISS-4q P2 — extent reclaim on unlink** (`d80958a`; v2-only): stm_fs_unlink + stm_fs_rmdir call `stm_sync_truncate` BEFORE inode-and-dirent removal, then double-commit. Works AROUND R50 P2-1's `free_gen < committed_gen` strict-less-than predicate: without the double-commit, freed paddrs at the same gen as the unlink's commit would violate the predicate. The double-commit advances free_gen one tick past committed_gen at unlink time, then re-commits with the inode/dirent removal. Forward-note: a future R50-relaxation chunk could remove the double-commit but must re-prove allocator safety. Until then, do NOT remove the double-commit from stm_fs_unlink / stm_fs_rmdir. **SWISS-4q P0 — MSIZE 1→8 MiB + cmd_write/read 8 MiB heap** (`75e1a9b`; v2-only): MSIZE bump in 9P server + client + cmd/fs/run.c. iounit propagation correctly. Heap allocations in cmd_write/read sized 8 MiB to match. Affects how libstratum-9p chunks Twrite — verify against the new ceiling. **R128 P1 — extracted inode-free helpers** (`48e6c4c`; v2-only): `fs_pre_inode_free_cleanup_locked(fs, ds, ino, cv)` drops the dirty_buffer entry + truncates extents BEFORE the inode free; returns `bool` indicating whether a truncate was actually performed (R130 gate). `fs_post_inode_free_reclaim_locked(fs)` performs the post-truncate double-commit (the R50 P2-1 step). Wired into 3 inode-freeing paths: `stm_fs_unlink_anon` (R128 P1-2; O_TMPFILE orphan free), `stm_fs_rename` overwrite branch (R128 P1-1), `fs_unlink_inode_and_dirent` (refactored from inline). Future inode-freeing paths MUST use both helpers as a pair — pre-cleanup before the inode tree mutation, post-reclaim AFTER (gated on the pre-cleanup's bool return AND on the freed/fs_free==OK signal). **R128 P2 — pre-flush extent-tree-mutation sites** (`48e6c4c`; v2-only): `stm_fs_copy_file_range`, `stm_fs_migrate_to_cold`, `stm_fs_promote_to_hot`, `stm_fs_create_snapshot` all drain the writeback buffer via `fs_flush_all_locked` or `fs_flush_ino_locked` BEFORE touching the extent tree. snapshot create's drain folds the SWISS-4q-flush-snapshot task into R128's close. Future ops that mutate the extent tree directly (without going through stm_sync_write_extent) MUST pre-flush the relevant inode or all inodes. **R130 perf gate** (`48e6c4c`; v2-only): the post-reclaim double-commit fires only when the pre-cleanup performed an actual truncate — directories, symlinks, fresh-anon-then-unlinked, and inline-only files skip the ~200 ms commit-pair. Without this gate, ctest -j4 timed out 4/51 suites; test_fs went from 60s solo to 17 minutes parallel. Do NOT relax the gate without crash-safety review. |
| Writeback aggregation | `v2/src/dirty_buffer/dirty_buffer.c`, `v2/include/stratum/dirty_buffer.h`, `v2/src/fs/fs.c::fs_write_regular_locked` (EXTENT branch), `v2/src/fs/fs.c::fs_read_regular_locked` (EXTENT branch + overlay), `v2/src/fs/fs.c::fs_flush_ino_locked` + `fs_flush_all_locked` + `fs_flush_drain_cb` + `fs_write_extent_aligned_locked` | (SWISS-4q-flush; v2-only) Per-inode plaintext buffer that absorbs sub-1-MiB writes; drains as fewer larger extents at flush time. Composes against `v2/specs/writeback.tla` v1 (44.5M states). Caps: STM_FLUSH_INODE_CAP_BYTES = 8 MiB per-inode, STM_FLUSH_GLOBAL_CAP_BYTES = 256 MiB total, STM_FLUSH_DIRECT_THRESHOLD_BYTES = 1 MiB (≥ this bypasses the buffer). Trust boundaries: (1) **Lock order**: `fs->lock` outer → `dbuf->mu` middle (256-bucket hash table key (dataset_id, ino) hashed via xxHash-style avalanche mod 256) → `sync->lock` inner. Drain callback runs UNDER dbuf->mu + fs->lock; sync->lock briefly taken for the extent write. No path takes (sync, dbuf) in reverse. (2) **Drain pop-on-success** (R128-class doctrine; the original 2.8 GB amplification bug): each successful callback range is removed from the buffer BEFORE the next iteration. Failed range stays for retry. Reverting to "leave all on failure" reintroduces the amplification — succeeded-then-failed ranges get duplicated as extents on retry, doubling each cycle. Unit tests `dbuf_drain_ino_pops_succeeded_keeps_failed` + `dbuf_drain_retry_does_not_double_emit` enforce this. (3) **AEAD nonce uniqueness**: flush emits one extent per range; each gets allocator-fresh paddr via stm_sync_write_extent → allocator's NoReuseInSameGen invariant (R50 P2-1). Two ranges flushed in the same drain MUST allocate different paddrs. (4) **Read overlay correctness**: `fs_read_regular_locked` overlays buffer on top of extent reads — returns buffered bytes for the buffered portion AND extent bytes for the unbuffered portion. Sub-block alignment requires `fs_write_extent_aligned_locked`'s RMW dance to preserve unbuffered head/tail bytes when a drain emits an extent that doesn't span a full block. (5) **Buffer-overlap-as-replacement** (v1 limitation): insert REPLACES entire overlapping range (no prefix/suffix split). Today's fallthrough via the extent layer covers correctness; writeback.tla v2 + impl could split. (6) **Drain discipline**: any op touching the extent tree directly MUST pre-flush the inode (`fs_flush_ino_locked`) or all inodes (`fs_flush_all_locked`). Today's pre-flush sites: stm_fs_commit, stm_fs_unmount, stm_fs_fallocate, stm_fs_truncate, stm_fs_reflink, **stm_fs_create_snapshot + stm_fs_copy_file_range + stm_fs_migrate_to_cold + stm_fs_promote_to_hot** (added at R128 P2 close — `48e6c4c`). The compound-op race-class audit at P9.5-PARALLEL-2 may surface additional handlers that need explicit pre-flush; those updates fold here at PARALLEL-2 close. (7) **Cap accounting**: total_bytes is the sum of per-bucket bytes; each insert + drain mutates both. Underflow on free / double-count on insert / integer overflow are all P0 bugs. Bounded retry-dance at write-time: when insert would exceed inode cap, flush this inode → retry; if global cap is over, flush all → retry; on third try, hard ENOSPC. (8) **Buffer + in-memory inode tree both lost pre-sync_commit**: crash recovery doctrine. Spec says CrashErasesBuffer is deferred to writeback.tla v2 but the C impl IS required to uphold this — the doctrine `Flush is all-or-nothing per call; Crash reverts committed to durable_disk` ships in v1. |
| Public API contracts | `include/stratum/fs.h`, `stm_fs_open_ro` vs `stm_fs_open` | RO vs RW contract depends on runtime guards; new accessors can create bypass surfaces |
| Inode allocator | `v2/src/inode/inode.c`, `v2/include/stratum/inode.h` | (P8-POSIX-1; v2-only — no v1 analog) The (ino, si_gen) tuple-uniqueness-across-time invariant from `v2/specs/inode.tla` underpins stale-fid detection in 9P (ARCH §11.3.2), per-file derived keys (§7.3.3), and NFS file handles. A buggy reuse path that doesn't bump gen exposes confused-deputy attacks. The two `inode_*_buggy.cfg` configs enumerate the failure modes the reviewer must rule out. [R69 P3-9: row uses explicit v2/ prefix since this surface only exists in v2.] |
| Dirent layer | `v2/src/dirent/dirent.c`, `v2/include/stratum/dirent.h` | (P8-POSIX-2; v2-only) Open-addressing chain integrity from `v2/specs/dirent.tla`. Three buggy configs (`dirent_unlink_uses_empty_buggy.cfg`, `dirent_create_overwrites_no_probe_buggy.cfg`, `dirent_lookup_stops_on_tombstone_buggy.cfg`) enumerate the canonical chain-integrity failure modes — silent loss of colliding-name reachability via missing-tombstone, silent overwrite of colliding occupant via no-probe-walk, and read-side analog of unlink-uses-empty. Writer-side guards must mirror decoder-side guards (R71 P1-1 lesson — same posture as the inode allocator). |
| Xattr layer | `v2/src/xattr/xattr.c`, `v2/include/stratum/xattr.h` | (P8-POSIX-6; v2-only) Open-addressing chain integrity from `v2/specs/xattr.tla` — structurally isomorphic to dirent.tla's write-side, keyed at `ino` instead of `dir_ino`. Three buggy configs (`xattr_unlink_uses_empty_buggy.cfg`, `xattr_create_overwrites_no_probe_buggy.cfg`, `xattr_lookup_stops_on_tombstone_buggy.cfg`) enumerate the canonical failure modes. Writer-side guards must mirror decoder-side guards on BOTH `name_len` AND `value_len` (R71 P1-1 + R77 P1-1 lesson — the OOB-read shape from inline data extends to xattr value records, so symmetric bounds must hold at both ≤ STM_XATTR_NAME_MAX (255) and ≤ STM_XATTR_VALUE_MAX (64 KiB) at every trust boundary). Tree-root introductions bind into Merkle chain in lockstep — `xattr_csum` is the 10th input to `compute_merkle_root`. |
| 9P client library (v2) | `v2/src/9p_client/9p_client.c`, `v2/include/stratum/9p_client.h` | (P9-LIB-1; v2-only) Synchronous 9P2000.L client per ARCH §10.2 ("libstratum-9p is the stable public ABI; all language bindings wrap it"). Public API at v2.0 (cumulative through P9-LIB-1d): read-side — dial+Tversion+Tattach handshake, stm_9p_walk, stm_9p_lopen, stm_9p_read, stm_9p_clunk, stm_9p_getattr, stm_9p_readdir; write-side — stm_9p_write (1b), stm_9p_lcreate / stm_9p_mkdir / stm_9p_unlinkat (1c), stm_9p_setattr / stm_9p_renameat / stm_9p_symlink / stm_9p_readlink / stm_9p_fsync (1d); stm_9p_link (1d-link). Tag auto-allocation (monotonic counter, STM_EOVERFLOW on NOTAG hit). Linux-ecode → stm_status mapping closed under err_map. Trust boundaries: (1) wire framing — every Rxx reply size is bound-checked against negotiated msize before parse, out-of-range disconnects with STM_EBACKEND; mirrors stratumd's `read_msg` / `write_msg` exactly. (2) Rlerror parsing — Linux ecode field mapped to stm_status via err_map(); unknown ecodes collapse to STM_EBACKEND so callers see a closed status set. (3) tag allocation — STM_EOVERFLOW once next_tag hits STM_9P_NOTAG; sync client can't disambiguate wrap-collisions cheaply, so the policy is "reconnect or fail". (4) tag-mismatch on reply — STM_EBACKEND + connection-poisoned posture: c->poisoned set by check_reply, every public op short-circuits at op_entry_check (R111 P3 F-11). (5) per-Rxx body-length validation — every parser checks body_len with strict equality (R111 P3 F-10): truncated replies + extra trailing bytes both surface as STM_EBACKEND. (6) caller-cap bound on every server-supplied count used as a write target (R111 P0 F-1 lesson): nwqid in Rwalk capped at caller's n_names, written in Rwrite capped at caller's count, tlen in Rreadlink capped at caller's buf_cap. The doctrine is doctrine carry: every NEW op MUST run op_entry_check at entry, validate names/targets at the lib boundary (saves a round-trip + stable status), use strict body-length equality on the reply, and bound any server-supplied count against the caller's cap before writing. (7) buffer pre-allocation — c->buf sized to msize at dial time; per-op malloc/free is avoided. Concurrency: NOT thread-safe across concurrent calls on the same client (one-op-at-a-time sync model); callers wanting concurrency open multiple clients. Hard-link surface complete: Tlink server handler + stm_9p_link client primitive shipped at P9-LIB-1d-link (alongside the new public API stm_fs_link_by_ino — POSIX link(2) by source inode, mirroring stm_fs_link's R74 P3-2 STM_EPERM-on-dir + R81 P3-1 ctime-stamp-after-success + R74 P3-3 rollback infallibility). Cross-dataset link refused server-side with STM_EXDEV. P9-LIB-2 async API extends with pipelined Txx + reply matching by tag; the bound-check + ecode-map discipline must carry forward. 9P2000-dialect support (for /ctl/ over the wire once /ctl/-on-stratumd lands) is forward-noted: the dial path must accept the version string as caller-configurable. |
| /ctl/ synthetic FS (v2) | `v2/src/ctl/synfs.c`, `v2/include/stratum/ctl.h` | (P9-CTL-1; P9-CTL-2b migrated codec from `stm_p9_server` (9P2000) to `stm_lp9_server` (.L) — KIND_META[] + qid encoding + materialize functions + admin gate + session lifecycle + audit-log doctrine ALL preserved verbatim; only the wire codec + vops adapter shape changed; v2-only) Operator-state synthetic filesystem (ARCH §14.3) served as a vops backend for the generic `stm_lp9_server`. Read paths report subsystem state; write paths trigger admin actions. Trust boundaries: (1) every read path must NOT leak secret bytes into the response (no DEKs, no on-disk structure that could enable offline attack); /ctl/state surfaces only stm_fs_stats counters which are non-secret. (2) every write path must validate caller uid against an operator policy — at v2.0 there are NO write paths (everything ENOACCES); future sub-chunks (e.g. `/pools/<name>/scrub`) MUST gate on uid before triggering the action. (3) per-fid materialized body buffer is bounded at STM_CTL_BODY_MAX (1 KiB); snprintf-then-check pattern guards against future field additions silently overflowing. (4) qid_path encoding reserves 56 bits — sub-chunks MUST keep the kind:8 high-byte invariant so a single qid can route across kind branches. (5) admin-gate trust boundary (P9-CTL-1d-uid → P9.5-PARALLEL-1): `admin_uid` is set ONCE at stratumd startup via `stm_ctl_set_admin_uid` (BEFORE any worker pthread spawn — R97 P2-2 carry); `caller_uid` is bound per-connection at `stm_ctl_conn_create(ctl, peer_uid, peer_gid, &cn)` and IMMUTABLE for the conn's lifetime. `ctl_caller_is_admin(cn)` predicates ANY admin-required kind, reading `cn->caller_uid` + `cn->ctl->admin_uid` (both immutable post-establishment; no lock needed). The default unset-caller AND default unset-admin_uid both fail-closed (uid 0 only is admin). Future sub-chunks adding admin-required kinds MUST set the `admin_required` bit in KIND_META[] AND the gate fires at vops_lopen (Tlopen) AND at vops_walk through admin-required dirs (R100 P2-1: walk-through must return ENOENT for non-admin so Tgetattr-after-walk cannot leak the file's mode bits — same posture as POSIX path-traversal through a mode-0500 dir). The pre-P9.5 `stm_ctl_set_caller(c, uid, gid)` API is preserved as a test-only stash (single-threaded harness migration window — see ctl.h docstring); production code goes through `stm_ctl_conn_create` exclusively. (6) writable-kind discipline (P9-CTL-1d-events): `KIND_ADMIN_CLEAR_EVENTS` is the FIRST writable kind — admin-gated, write-only mode 0200, vops_write specializes on it. Future writable kinds MUST inherit (a) admin gate at vops_lopen, (b) defense-in-depth admin re-check at vops_write, (c) zero-byte Twrite refusal (R101 P2-2 — `len == 0` returns STM_EINVAL since zero-byte writes are too easy to trigger accidentally), (d) consistent KIND_META[] mode-bit + vops_write/lopen dispatch. (7) per-conn sessions lifecycle (P9-CTL-1d-events R101 P1-1 → P9.5-PARALLEL-1): sessions[] now live on `stm_ctl_conn`, NOT on shared `stm_ctl`. Each conn's sessions die with the conn at `stm_ctl_conn_destroy` (called per-connection from stratumd's serve loop AFTER stm_lp9_server_destroy + as the conn's worker_count decrement). Cross-conn fid aliasing is impossible — each `stm_ctl_conn` has an independent sessions[] table keyed by fid alone, and only its own vops dispatch touches it. The pre-P9.5 `stm_ctl_drop_all_sessions(c)` API is preserved as a no-op stub for out-of-tree linkage; the "drop_all between sequential connections" idiom is obsolete. Tests that still reference it (e.g., `stratumd_ctl_session_drain_between_connections`) verify the same outcome — fid slots don't survive a conn close — but via the per-conn destruction path now. (8) reader-vs-clear snapshot invalidation (R101 P2-1 → P9.5-PARALLEL-1): /admin/clear-events bumps `stm_ctl::event_gen` under `event_mu` AFTER zeroing `event_buf`. Every /events Tlopen captures `s->snapshot_event_gen` under `event_mu`; vops_read for /events compares `s->snapshot_event_gen != cn->ctl->event_gen` and returns EOF (count=0) on mismatch — no zero-padded frankenstein bytes. The gen-bump replaces the pre-P9.5 cross-conn sessions[] walk (which is impossible under concurrent regime — a writer can't enumerate sibling conns' sessions[] tables without a registry). Future writable kinds that mutate a buffer surfaced by a snapshot-at-Tlopen reader MUST adopt the same gen-bump + per-session-snapshot pattern. (9) admin-only diagnostic dir trees (P9-CTL-1d-debug-alloc): /debug/ is the second admin-only dir subtree after /admin/. Every NEW admin-only dir tree MUST (a) flag `admin_required=true` in KIND_META[] for the dir kind itself AND for every leaf child kind, (b) add an explicit `case KIND_X_DIR:` to vops_walk with `if (!ctl_caller_is_admin(c)) return STM_ENOENT;` at the head — clause 5's R100 P2-1 walk-through gate scales to N trees. The /debug/ surface composes against fs+sync (not pool) — `stm_fs_alloc_stats_get` is the trust-boundary wrapper that resolves stm_sync_alloc + calls stm_alloc_stats_get under fs->lock with stm_fs_stats_get's wedged-OK posture (diagnostics MUST work when fs is wedged). Future /debug/ sub-chunks (tree-walk, extent-map, integrity-verify) introduce their own thin stm_fs read-side wrappers following the same posture. (10) bulk-format kinds + label injection (P9-CTL-1e): /pools/<uuid>/metrics/prometheus is the FIRST kind whose worst-case body exceeds STM_CTL_BODY_MAX (1 KiB). The per-fid `bulk_buf` field in `ctl_session` is heap-allocated at vops_lopen's materializer call, sized via realloc-doubling growth bounded by STM_CTL_METRICS_MAX (64 KiB), and freed at vops_clunk → session_free_locked. Future bulk-format kinds (e.g., debug tree-walk dumps, extent-map dumps) MUST (a) set s->bulk_buf + s->bulk_len in their materializer; (b) NOT touch s->buf for these kinds (the bulk_buf branch in vops_read pre-empts the bounded-body branch); (c) preserve the free + memset discipline in session_free_locked (free runs BEFORE the memset that clears the slot pointer — the comment is load-bearing; reverse the order and the alloc leaks). The Prometheus exposition format is line-oriented ASCII; label values bounded to a fixed allowlist (UUIDs are 36-char hex; enum-name strings filtered through device_*_name / scrub_state_name with the R98 P3-4 trailing-"unknown" tamper-resilience). NO user-supplied strings flow into Prometheus labels at v2.0 (no dataset names, no snapshot names) — defers the Prometheus label-escape rule. R99 P2-1 line-injection class extends to label-value-injection in the exposition format: when per-dataset metrics are added, the dataset name MUST be sanitized OR labels keyed by dataset_id only. /metrics/ is world-readable mode 0444 (no admin gate — exposition is public per ARCH §14.8.1); body content adapts to fs/scrub attachment but is always renderable when c->pool is attached. The materializer takes each subsystem's lock independently (no cross-subsystem lock held concurrently); pool roster snapshot under stm_pool_lock_shared, fs gauges via stm_fs_stats_get + stm_fs_dataset_count, scrub status via stm_scrub_status_get. Forward-note: OTLP exposition (binary protobuf) is deferred to a sidecar translator; per-dataset op counters + latency histograms deferred until instrumentation hot-path work. (11) **S5-PRE-A — full subsystem attach** (SWISS-5 pre-chunk; `b3f234b`): stratumd calls `stm_ctl_attach_pool(ctl, stm_fs_pool(fs))` AND creates a sibling scrub via `stm_scrub_create(stm_fs_sync(fs), ...)` + `stm_sync_scrub_install_production_cb(...)` + `stm_ctl_attach_scrub(ctl, scrub)`, in that order, AFTER `stm_ctl_create` and BEFORE `stm_ctl_set_admin_uid` (preserving the R97 P2-2 attach-before-pthread_create timing barrier). The scrub is daemon-owned (stratumd creates it; stm_fs has no scrub field). Public getters `stm_fs_pool(fs)` + `stm_fs_sync(fs)` are pure read-only accessors. With this clause, /pools/<uuid>/* (scrub + metrics + devices/) render correctly via stratumd, and /pools/<uuid>/metrics/prometheus gains the full gauge set (pool roster + scrub counters + per-device gauges). Future chunks attaching additional subsystems MUST inherit the same posture: attach AFTER ctl_create + BEFORE caller-stamping or pthread spawn. Shutdown ordering carries to stratumd: servers → ctl_destroy → scrub_close → fs_unmount (load-bearing; scrub.h "Borrowed references" + ctl.h R26 P3-4). (12) **S5-PRE-C — /datasets/<id>/snapshots/ read-only listing** (SWISS-5 pre-chunk; `b3f234b`): new kinds `KIND_DATASET_SNAPSHOTS_DIR` (27) + `KIND_DATASET_SNAPSHOT_INFO` (28); KIND_MAX = 29. New qid helpers `qid_of_snap(kind, snap_id)` + `qid_snap_id(q)` encode the snap_id in the LOW 56 bits of qid path (high byte stays kind:8, preserving clause 4's invariant). The SNAP_ID_QID_MASK = `0x00FFFFFFFFFFFFFFull`; saturation is refused (snap_ids near 2^56 will refuse at the qid encoding before they break the kind bits — extremely unreachable in practice). `snap_collect_ctx` + `snap_collect_cb` filter the snapshot tree by parent dataset_id (`stm_snapshot_iter` walks all snaps; the cb's accumulator only retains snaps whose dataset_id matches the parent dir's). The materializer surfaces an admin-readable line per snap: `snapshot-id`, `dataset-id`, `name`, `created-txg`, `extent-txg`, `prev-snap-id`, `hold-count`, `flags`. **Stale-id discipline** (R29 P3-1 doctrine carry — load-bearing): every walk + lopen + getattr re-checks the snap_id exists against the current snapshot index under the appropriate lock (a snap can be deleted between Tgetattr probe and Tlopen materialize; the second op must return ENOENT, not stale state). The kind is admin-readable but NOT writable at v2.0 — no writable kinds added. R99 P2-1 line-injection class is closed inside `stm_snap_name_chars_valid` (snapshot create refuses control bytes; this listing surface inherits the closure). Future snapshot-mutating writable kinds attached to /datasets/<id>/snapshots/<sid>/ (e.g., delete-from-list, hold-from-list) MUST inherit the same stale-id discipline at each entry point AND adopt the writable-kind doctrine from clause 6 (admin gate at lopen + defense-in-depth at write + zero-byte refusal + KIND_META[].mode + per-conn sessions die-with-conn per clause 7). (13) **P9.5-PARALLEL-1 — stm_ctl_conn per-connection state isolation** (`22fa907`): `stm_ctl` is now PROCESS-WIDE SHARED state (immutable fs/pool/scrub/admin_uid post-startup + audit log `event_buf`+`event_gen` under `event_mu` + worker-count refcount under `worker_mu`/`worker_cv`). `stm_ctl_conn` (forward-decl in ctl.h) is PER-CONNECTION state (immutable `caller_uid`/`caller_gid` + per-conn `sessions[STM_CTL_MAX_SESSIONS=64]` + per-conn `mu`). Every vops_* callback takes ctx as `stm_ctl_conn *`; transports MUST pass `cn` (not `ctl`) to `stm_lp9_server_create`. **Lock order (load-bearing)**: `cn->mu` (per-conn sessions; OUTER) → `event_mu` (event_buf + event_gen; INNER, NESTED on snapshot-capture paths in /events Tlopen + Tread). `worker_mu` is taken alone at conn create / destroy + at shutdown drain. Subsystem-internal locks (fs->lock, pool::lock, scrub::lock, snapshot index mutex) sit underneath `cn->mu` (lower lock-order layer; no callbacks into /ctl/, no cycles reachable). Future writable kinds + future logged paths nested inside sessions[]-touching critical sections MUST take cn->mu before event_mu (the established lock order). **Lifecycle refcount** (LifecycleNoUAF in `v2/specs/ctl_conn.tla`): every `stm_ctl_conn_create` increments `worker_count`; every `stm_ctl_conn_destroy` decrements + broadcasts `worker_cv`. `stm_ctl_destroy` waits on the cv until count == 0 — bounds detached /ctl/ workers at daemon shutdown so they can't UAF the shared ctl. **Spec composition**: `v2/specs/ctl_conn.tla` (3 buggy configs trip CallerScopedPerConn / NoClunkSpillover / LifecycleNoUAF). **Test gates** (P9.5-PARALLEL-1f, task #961): `tests/test_ctl_concurrent.c` (5 in-process tests — caller isolation + per-conn fid namespace independence + cross-conn event_gen invalidation + 4-way no-collision + create-input validation), `tests/test_ctl_conn_lifecycle.c` (6 tests — destroy-blocks-on-cv + N short conns + NULL safety + caller_uid accessor), `v2/tools/stratum/tests/concurrent_ctl.rs` e2e (2 tests — two-client + three-client wall-time bounds for the concurrent-accept payoff). **R131 audit (close commit)**: 0 P0/P1; 3 P2 doc-drift items closed inline (ctl.h Concurrency docstring rewritten, stm_ctl_set_caller docstring updated, synfs.c lock-order doctrine block extended with the `cn->mu → event_mu` nesting). **SWISS-8k retired**: TUI pollers in `v2/tools/stratum/src/{volmap,snapgraph}.rs` hold long-lived /ctl/ connections again; the pre-22fa907 disconnect-between-ticks workaround is gone. **Outstanding sub-tasks**: #958 (TLC verify ctl_conn.tla, tooling-gated). (14) **P9.5-PARALLEL-2 — compound-op multi-subsystem read contract** (`1a4e174`): the /ctl/ surface guarantees **per-subsystem linearizable + cross-subsystem eventually-consistent** reads. Each individual subsystem accessor (`stm_fs_stats_get`, `stm_pool_lock_shared`, `stm_scrub_status_get`, `stm_snapshot_iter`, `stm_fs_dataset_count`, `stm_fs_alloc_stats_get`, `stm_fs_effective_dataset_property`) takes its OWN internal mutex and returns a value that reflects that subsystem's committed state at SOME instant during the call. A /ctl/ materializer that consults N>1 subsystems serially (e.g., `materialize_pool_metrics_prometheus` at synfs.c:1611 reads pool + fs + scrub; `materialize_dataset_properties` at synfs.c:1152 calls `stm_fs_effective_dataset_property` 5 times in succession) sees per-subsystem values that may have advanced between calls. The composite output is NOT guaranteed to reflect a single global transaction generation. This is the documented contract per the R110 P3-5 lock-posture doctrine (synfs.c lines 1510-1535) and the materialize_dataset_properties freshness-tradeoff comment (synfs.c lines 1163-1168). **The audit verified** (R132 close + `v2/docs/p9.5-parallel-2-design.md` §2.1): every public stm_fs_* mutating function holds fs->lock from entry to final unlock — NO release-and-reacquire mid-body. R128/R130 pre-flush wiring is in place on every extent-mutating compound op (`stm_fs_commit`, `stm_fs_unlink`/`stm_fs_rmdir`, `stm_fs_unlink_anon`, `stm_fs_truncate`, `stm_fs_rename` overwrite, `stm_fs_create_snapshot`, `stm_fs_copy_file_range`, `stm_fs_migrate_to_cold`, `stm_fs_promote_to_hot`). Writable /ctl/ kinds (admin verbs: `scrub-trigger`, `create-snapshot`, `delete-snapshot`, `{hold,release}-snapshot`) take fs->lock via the public stm_fs_* APIs and serialize with each other AND with FS-side ops; ONLY /ctl/ READ paths run concurrently with FS-side ops (and they take per-subsystem locks, not fs->lock). **Spec composition**: `v2/specs/compound_ops.tla` with 4 invariants (WriterCompoundOpAtomicVsWriter, PerSubsystemLinearizable, NoNegativeCounters, MultiSubReadIsBracketed) + 2 buggy configs (writer-releases-lock-mid → trips atomicity; counter-underflow → trips no-negative). **Test gate** (P9.5-PARALLEL-2e, task #968): `tests/test_compound_ops_concurrent.c` — pthread harness with writer (snapshot create+delete loop) racing reader (stats_get + dataset_count + snapshot_iter loop) for 200 iterations; asserts no deadlock, no torn allocator balance (`data_total == allocated + free + pending` holds every iteration), no STM_ECORRUPT from reader. **Future writable kinds** that need cross-subsystem atomicity (e.g., a future rollback verb that mutates snapshot index + dataset table + allocator state in one logical step) MUST EITHER (a) compose against an extended `compound_ops.tla` spec that captures the new semantic linkage, OR (b) inherit the eventual-consistency contract and document the visible intermediate-state class. PARALLEL-3 (per-inode fs->lock) will refine the WriterCompoundOpAtomicVsWriter invariant ("at most one writer per inode" or similar) and MUST preserve the per-subsystem linearizability + cross-subsystem eventual-consistency surface that PARALLEL-2 documents. **R132 audit close**: see commit message for the close commit. |

**How to run an audit round:**

1. Spawn an Opus 4.7 soundness-prosecutor agent (general-purpose subagent, `run_in_background: true`).
2. In the prompt, include `memory/audit_r15_closed_list.md` contents as the "already fixed — do not re-report" preamble.
3. Scope the prompt to the surface you changed — e.g. "re-audit the changes in `src/snap/snap.c` commits X..Y for nonce-uniqueness / torn-write / wedge-state regressions."
4. Wait for the completion notification. Fix any P0/P1/P2 findings before merge.

## Invariants that must hold

See `docs/STRATUM.md` §22 "Soundness invariants" for the full list. The load-bearing ones:

- **Nonce uniqueness**: `disk ss_gen > fs->gen` at all times post-mount. Every write encrypts under `(paddr, write_gen=fs->gen)` and those pairs must be globally unique across the volume's lifetime.
- **Sync is three-phase**: reservation (G+1, pre-flush root) → flush at gen G → final (G+2, post-flush root). `fs->gen` advances by 1 per sync. Any refactor must preserve the invariant across every torn-write-or-crash boundary.
- **Rollback-bump before allocator swap**: if the bump write fails, `old_alloc` must still hold live refcounts for pre-rollback paddrs so they don't get reused at the same gen.
- **Wedged / read-only containment**: `fs->wedged` and `fs->read_only` are runtime-enforced at every public API entry. New public APIs must call `STM_FS_GUARD_READ` / `STM_FS_GUARD_WRITE`.
- **Counter clamps at mount**: `ss_gen`, `ss_next_ino`, `ss_next_snap_id` all rejected if near UINT64_MAX; walk-derived raises from on-disk tree contents.

## Technical reference upkeep (v2 only)

The v2 tree carries a comprehensive as-built reference at `v2/docs/REFERENCE.md` (top-level index + maintenance policy) and `v2/docs/reference/00..11.md` (per-subsystem deep dives). Every chunk that lands in v2/ MUST update the relevant reference section in the **same PR / commit** that lands the code.  Concretely:

- **New module / new public API** → add or extend the corresponding `reference/NN-*.md` section.
- **Change to a documented invariant or contract** → update the section, and (per the spec-first policy above) update the spec FIRST.
- **New TLA+ spec or buggy config** → update `reference/10-specs.md`'s catalog.
- **New term / acronym / state name** → add to `reference/11-glossary.md`.
- **`STM_UB_VERSION` bump or feature-flag change** → update `reference/00-overview.md`'s UB-versioning table.
- **Snapshot drift** (test count, spec count, tip hash in `REFERENCE.md`) — refresh in the close commit.

The reference is intentionally distinct from `docs/ARCHITECTURE.md` (design intent, including unimplemented work) and `docs/ROADMAP-V2.md` (phased plan): it documents what EXISTS in the tree right now, with `file:line` citations.  When the three disagree, the spec wins, the reference is corrected, and ARCHITECTURE.md gets an issue noted for cross-check.

If a chunk's diff doesn't include a reference-doc update, the reviewer should ask whether it was intentional (e.g., pure bug fix that doesn't change documented surface) or an oversight.

**Phase 9 deferment** (R96 P3-8): the per-subsystem `reference/NN-*.md` catalog last had entries for the post-P7 (Phase 7) modules. Phase 8 (POSIX surface — `inode/`, `dirent/`, `xattr/`, `locks/`) and Phase 9 (9P-server stack — `9p/`, `cmd/stratumd/`, `ctl/`) chunks have shipped without per-subsystem reference docs; the per-chunk detail lives in `phase8-status.md` / `phase9-status.md` instead. The REFERENCE.md Snapshot section is updated per-chunk with a tip line summary. Backfilling the per-subsystem catalog is a chunk-of-its-own scheduled before Phase 9 close. Until that chunk lands, Phase 8/9 chunks update phase status docs + Snapshot tip; new per-subsystem `reference/NN-*.md` files are NOT required as a precondition for chunk merge.

## Regression testing

- Every audit finding that can be made to fail without the fix should land a regression test. See `tests/test_fs.c::test_fs_sync_two_phase_gen_invariant`, `test_fs_mount_bump_encrypted`, `test_fs_write_gap_is_zeroed*`, `tests/test_snap.c::test_snap_rollback_bumps_gen_encrypted`, `tests/test_p9.c::test_p9_wire_validation`, `test_p9_fid_cache_and_dup` for the pattern.
- Before committing, run all 11 C suites + 10 Rust integration tests: `cmake --build build && for t in types key block node btree fs compress crypto snap p9 alloc; do build/tests/test_$t; done && (cd tui && cargo test --test integration --release)`.

## Clangd false positives

The CMake build is the source of truth. Clangd emits many "file not found" / "undeclared identifier" diagnostics in `src/` and `include/` because it doesn't see the cmake-generated compile commands for this tree. Ignore those and trust `cmake --build build`.

## Session-state files

- Built binaries deploy to `/Users/northkillpd/projects/dist/{stratum,stratum-tui}` and must be re-signed after deploy: `codesign --force --sign - <path>`.
- Tasks tracked via the in-conversation task list; one task per audit finding, per-round re-audit task as a checkpoint.
