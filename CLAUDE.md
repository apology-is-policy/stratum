# CLAUDE.md

Operating notes for Claude when working on Stratum.

## Mission

Stratum is aiming to be a first-class peer of ZFS and btrfs — not a demo, not a hobby project, a production-grade COW filesystem that a POSIX-compatible OS can be built on. Beyond peer status, Stratum is intended to pioneer in a chosen set of areas where the state of the art is ripe for advance:

1. **Formally verified sync + crash-safety protocol.** TLA+ (or similar) specification of the three-phase sync, allocator invariants, and AEAD nonce machinery, proved correct under arbitrary write reordering. No mainline filesystem has this.
2. **Post-quantum encryption as default.** ML-KEM-768 + XChaCha20-Poly1305 hybrid wrap keys. Stratum ships PQ-ready out of the box.
3. **Content-defined extent boundaries.** Rolling-hash chunking at the extent layer (shift-resistant dedup for real-world data patterns — VMs, container images, backups). No production filesystem integrates this.
4. **Lock-free metadata path.** The Bε-tree's message-buffer model is an unusually good fit for lock-free operation. Pair with MVCC readers for zero-contention reads.
5. **Merkle-rooted metadata integrity.** Tamper-evident semantics stronger than per-block csums: any offline edit to any metadata block is cryptographically detectable.
6. **Tiered storage with learned migration.** Beyond heuristic hot/cold tiering — access-pattern models for policy decisions.
7. **io_uring-native, zero-copy write path.** Designed for 2026+ NVMe workloads, not retrofitted from POSIX block I/O.

These are long-horizon commitments. The existing code is a **foundation, not the destination**. Components will be preserved where they still serve the vision (Bε-tree core, crypto primitives, xxHash helpers) and rewritten where they don't (superblock layout, allocator, namespace model, block-device abstraction). Salvage rate of the existing ~20 KLOC is expected to be 30–50%. That's fine. The cost of preserving code against architectural constraints it doesn't fit is higher than the cost of rewriting it right.

## Design-first policy (Phase 0)

Architectural decisions compound. A wrong choice in the concurrency model, storage pool layout, or namespace structure becomes irrevocable once thousands of lines of code depend on it. **Phase 0 — active as of 2026-04-19 — pauses implementation of new features until major architectural decisions are specified in writing.**

Phase 0 outputs (all go into `docs/` — written, reviewed, versioned):

- `docs/VISION.md` — target workloads, scale targets, property ranking (perf / reliability / security / concurrency / portability). Identifies the non-goals.
- `docs/COMPARISON.md` — feature matrix vs ZFS, btrfs, bcachefs. Three columns: match, lead, deliberately-not-doing. Annotated with *why* for each.
- `docs/NOVEL.md` — the committed novel angles from the mission list. For each: concrete scope, what "done" looks like, dependencies on other decisions.
- `docs/ARCHITECTURE.md` — the foundational decisions:
    - Concurrency model (MVCC + lock-free readers / fine-grained writer locks / something else)
    - Storage pool model (device roster, redundancy domains, nonce construction under multi-device)
    - Namespace model (subvolumes, datasets, property inheritance, per-dataset encryption keys)
    - Allocator model (tree-embedded vs side channel; relation to sync)
    - Superblock / quorum model (per-device uberblock trees, commit ordering across devices)
    - Block-device abstraction (io_uring-native, DAX-compatible, zero-copy affordances)
    - Integrity model (Merkle root placement, verification cadence)
    - Crypto model (per-dataset keys, PQ wrap, key rotation, rekey)
- `docs/ROADMAP-V2.md` — the phased plan that builds on the above. Replaces the prior SOTA roadmap.

Only after `ARCHITECTURE.md` is signed off does implementation resume — likely from a mostly-fresh tree in a sibling directory (`stratum-v2/` or similar), with salvageable components ported one at a time with their own justification.

**Process notes during Phase 0:**
- Every design decision gets a written rationale. Assume we'll need to justify it in a year when the context has faded.
- Assume every decision gets challenged. The mission is ambitious enough that "we chose X because it was easier" isn't a legitimate answer for foundational choices.
- Design sessions produce documents, not code. No implementation pressure during Phase 0.
- Load-bearing invariants identified during design become candidates for formal specification (TLA+ / Alloy / similar). The spec is the source of truth; code is an implementation of the spec.

## Spec-first policy (applies to every new feature, not just Phase 0)

**If a feature touches a load-bearing invariant — concurrency, commit ordering, nonce uniqueness, quorum, redundancy, crypto key derivation, cache coherence, torn-write recovery — the TLA+ (or similar) model comes BEFORE the implementation.** Write the spec, let TLC chew on it, and let the invariant violations (if any) surface at the spec level where they cost minutes, not at runtime where they cost commits.

Concrete pattern that's repeatedly paid off on this project:
1. Propose the feature in prose (problem + shape).
2. **Model the mechanism in TLA+** — state, actions, invariants. TLC with small bounds (typically Devices≤3, Commits≤3, Retries≤1).
3. Iterate until TLC is green under the invariants you'd want the impl to uphold. If a bug shows up, fix the design BEFORE writing code.
4. Implement against the model. Cross-reference each impl step to the corresponding spec action in comments. Keep the SPEC-TO-CODE mapping current.
5. When the impl surfaces a new mechanism the spec didn't cover, extend the spec FIRST, then update the impl.

Historical receipts:
- `quorum.tla` (P5-0) caught two bugs before a line of P5-2 code was written: orphan-gen mount acceptance (fixed via `OrphansNotAuthoritative`) and over-strict `LiveCoordMonotonic` that would fail in the transient between-writes state.
- `quorum.tla` with `IdempotentRetry=FALSE` + `MaxRetries≥2` demonstrated the R14 P1 content-divergence bug at the spec level — the invariant fired before anyone had coded the offending path.
- Skipping this step for P5-3b/c left the per-tree-gen divergence bug (fold of alloc_commit's R7c P2-5 short-circuit + the roots-object value layout) for a test to catch at runtime, not the model. An extension of `quorum.tla` that captures per-device tree gens + idempotent-commit would have caught it before the first compile.

**If you cannot articulate the invariant formally, you don't understand it well enough to implement it.**

Features that clearly benefit (not exhaustive): multi-device commit, quorum protocols, key rotation, scrub orchestration, refcount/snapshot graph mutations, MVCC readers vs writers, anything that persists a reservation-then-commit or multi-phase write, anything that uses crypto nonces over a shared key, anything with more than one device state machine in play.

Features that usually don't (pure computation, test helpers, config parsing, CLI glue): skip the spec; just write + test. Use judgment.

## Audit-triggering changes

**Applies to**: any change to the current codebase during Phase 0 (e.g. bug fixes to the foundation) AND to Stratum v2 code once implementation resumes. The policy survives the redesign — the invariants are filesystem invariants, not stratum-v1 invariants.

Stratum v1 went through 15 rounds of adversarial soundness audits (R0–R14, commits `bb39db8` → `405c4fb`) that landed ~60 corruption-class fixes. The invariants those audits established are load-bearing and non-obvious — many will carry forward to v2. **Any change to the surfaces below MUST spawn a focused soundness audit against the changed code before merge** — not as ceremony, but because each round has surfaced a previously-invisible bug, and the pattern is that regressions in these areas are not caught by the test suite.

**Trigger list** — changes to any of these require an audit round:

| Surface | Files | Why |
|---|---|---|
| Crypto nonce construction / key handling | `src/crypto/crypto.c`, `include/stratum/crypto.h` | AEAD nonce uniqueness is the single most expensive invariant; past rounds found nonce-reuse across mount, across rollback, across UINT64_MAX wrap |
| Sync / rollback / mount-bump flow | `src/fs/fs.c::stm_fs_sync`, `stm_fs_open_impl`, `stm_fs_gen_bump_disk`, `src/snap/snap.c::stm_snap_rollback` | Invariant `disk ss_gen > fs->gen` is maintained by a delicate three-phase dance (see `docs/STRATUM.md` §7) |
| Snapshot index (v2) | `v2/src/snapshot/snapshot.c`, `v2/include/stratum/snapshot.h` | (v2-only) Snapshot create/delete/lookup/hold + chain ordering invariants from `v2/specs/snapshot.tla` (`ChainExtentTxgOrdered`, `OrphansNotAuthoritative`). Trust boundaries: (1) **R29 P3-1 saturation**: refuse on `next_id == UINT64_MAX` or `current_txg == UINT64_MAX`; (2) **R40 P2-1 racing-creator**: `extent_txg < prev's extent_txg` returns STM_EINVAL — without it, the in-process commit window could persist a snap with stale extent_txg, which the next mount's `sp_validate_shadow` would reject as STM_ECORRUPT (unmountable pool); (3) **R99 P2-1 carry (P9-CTL-1d-actions-snapshot-create)**: `stm_snap_name_chars_valid` refuses control bytes (< 0x20 + 0x7F) — closes the line-injection attack vector on every present and future surface that displays snapshot names in line-oriented format. UTF-8 multi-byte sequences (≥ 0x80) accepted unchanged; (4) **dead-list ownership transfer**: `stm_snapshot_delete`'s `out_freed_paddrs` AND `out_freed_cold_hashes` ownership transfers to caller; caller MUST reclaim each paddr via `stm_alloc_free` (per-device routed through `stm_paddr_device`) AND each cold hash via `stm_cas_deref` (iterating the buffer in `STM_CAS_HASH_LEN`-byte strides) before the next sync_commit OR blocks leak from tracking. Both buffers may legitimately be returned NULL with count 0 (clean delete with no overwrites); `free()` is well-defined on NULL. On non-OK return both pairs are zero/NULL. Future snapshot-mutation chunks (rollback, hold/release, recv side-effects) MUST consume both buffer-pairs end-to-end (R106 P3-2 carry — first /ctl/ surface to handle the pair was P9-CTL-1d-actions-snapshot-delete); (5) **txg monotonicity**: `current_txg` only ever advances. Future snapshot-mutation chunks (rollback, hold/release, recv side-effects) MUST preserve every invariant above and re-audit. |
| Btree write paths | `src/btree/btree.c::stm_btree_write_node`, `split_root`, `split_child`, `stm_btree_flush`, `src/btree/msg.c` | Torn-write recovery, pivot pre-reserve, and the two-phase flush commit are easy to break |
| Superblock layout / validation | `src/fs/fs.c` mount SB parsing, `include/stratum/super.h` | SB is plaintext + unkeyed xxHash3; every counter field needs adversarial-tamper defense |
| 9P wire handling (v1) | `src/p9/p9.c` | Every `body` pointer read needs a `body_len` bound; silent Rread with count=0 hides errors |
| 9P codec (v2 / janus) | `v2/src/p9/server.c`, `v2/src/p9/wire.h`, `v2/include/stratum/p9.h` | (v2-only) Generic vops-based 9P2000 codec used by janus's `/keys/` synthetic FS. Inherits R11-R14 audit lessons: bounds-checking on every wire read, fid leak avoidance on failed Tattach, partial-Twalk doesn't bind newfid, mode-gating on read/write. |
| 9P2000.L codec (v2 / lp9) | `v2/src/lp9/server.c`, `v2/include/stratum/lp9.h` | (P9-CTL-2a; v2-only) Generic vops-based 9P2000.L server, sibling to `stm_p9_server` (which speaks 9P2000) but on the .L wire. v1.0 wire-handles Tversion/Tattach/Twalk/Tlopen/Tread/Twrite/Tclunk/Tflush/Tgetattr/Treaddir; optional ops (Tlcreate/Tmkdir/Tunlinkat/Tsetattr/Tfsync/Tsymlink/Treadlink) return Rlerror(ENOSYS) when their vops slot is NULL. Trust-boundary discipline lifted verbatim from `v2/src/9p/server.c` (the FS-bound .L server): every Txx parser bounds-checks `body_len` before consuming fields; every Rxx writer bounds-checks `resp_cap` before serialising; count fields in Tread/Twrite/Treaddir clamp against BOTH `msize - hdr - 4` AND `resp_cap - hdr - 4` so a small response buffer doesn't fail the request — it just emits less. h_read enforces `f->is_open` (R74-class lesson on .L server's stricter open-state contract vs. 9P2000 server's lax behavior); h_lopen refuses re-open of an already-open fid, refuses O_DIRECTORY on non-dir → ENOTDIR, refuses non-RDONLY on dir → EISDIR. First consumer is /ctl/ as of P9-CTL-2b (`v2/src/ctl/synfs.c`); slate daemon is the next planned consumer. Future consumers MUST inherit the same discipline when they add new ops: bounds-check body_len, clamp counts, validate fid state. R111 client-side caller-cap-bound lesson applies symmetrically here on the server side — `got > count` after vops->read returns is clamped before serialising. |
| 9P2000.L filesystem server (v2) | `v2/src/9p/server.c`, `v2/src/9p/wire.h`, `v2/include/stratum/9p.h` | (P9-9P-1; v2-only) Filesystem-bound 9P2000.L server that takes a live `stm_fs *` and dispatches every op to `stm_fs_*`. Composes against `v2/specs/fid.tla` — every Tread/Twrite/Tgetattr/Tsetattr/Tlock/... handler MUST verify the fid's cached_gen against the inode's current `si_gen` via `verify_fid_fresh` BEFORE forwarding to stm_fs (the IOReject gate from fid.tla); every Twalk / Tattach binding MUST snapshot current `si_gen` at bind time (the WalkBindsWithCurrentGen invariant). Five buggy configs enumerate the runtime-gate failure modes the reviewer must explicitly rule out — staleness gate skipped (confused-deputy attack), bind-time gen-snapshot wrong, Tclunk leaks fid, Tdetach leaks fids, IOReject fires falsely. Every fid that has held byte-range locks must call `stm_fs_release_lock_owner(owner_id)` at clunk + at Tdetach (composes against locks.tla::ReleaseOwner). |
| slate daemon (v2) | `v2/src/slate/slate.c`, `v2/include/stratum/slate.h`, `v2/src/cmd/stratum-slate/main.c`, `v2/specs/slate.tla` | (P9-SLATE-1 + P9-SLATE-2 + P9-SLATE-3a + P9-SLATE-3b; v2-only) Slate is the Plan-9-shaped TUI architected as a synthetic filesystem (docs/SLATE-DESIGN.md). The daemon owns all UI state; clients (terminal renderer, scripts, AI agents, future Halcyon panes) interact with it purely by reading/writing files in a 9P tree served over a Unix socket. Cumulative schema: SLATE-1 — /version, /status, /event, /redraw, /log/{tail,append}; SLATE-2 — /connection/{socket,connected,attach}, /panels/{left,right}/{path,entries}; SLATE-3a — /panels/{left,right}/{cursor,action} (cursor RW, action W); SLATE-3b — `key Enter` descend verb + walk refactor (panel_entries_render clones WORK→CWD before lopen so per-entry walks resolve from CWD instead of root, enabling nested-cwd correctness). Composes against `v2/specs/slate.tla` (VersionMonotonic + EventFIFO + ReadConsistent + DispatchProgress + ConnectionAtomic + PanelPathConsistent; 4 buggy configs trip the right invariants). **Per user decision 2026-05-07: future Slate chunks (SLATE-3+) skip formal-model verification — spec-first applies only to FS invariant-bearing components.** Concurrency model is fundamentally different from /ctl/'s SERIAL-per-server: slate **MUST** accept connections concurrently (one JOINABLE pthread per accepted connection per R114 P2-1; worker pool drained at shutdown) because /redraw is a blocking-read primitive — a renderer holding /redraw must NOT block another connection's /event write. Trust boundaries: (1) wire framing — same 4-byte LE size header + body discipline as stratumd's serve_ctl_client; per-connection bounded by msize. (2) Per-connection peer credentials — SO_PEERCRED on Linux / getpeereid() on macOS/BSD; refuse-by-default on resolution failure (R95 P2-2 carry); opt-in `--allow-unauth` for testing. (3) Mutex-cv discipline (load-bearing concurrency) — every /version mutation goes through `s->mu`; /redraw blocking-read does cond_wait on `s->cv`; every state-mutating action (dispatch_event, set_status, log/append write, attach, disconnect) MUST `pthread_cond_broadcast(&s->cv)` so blocked readers wake. Without the broadcast, /redraw would never wake → renderers stuck. (4) Stop discipline — `stm_slate_stop` sets a stop flag and broadcasts cv so blocked /redraw readers return immediately; daemon shutdown calls this BEFORE worker join (otherwise blocked readers hold their pthreads alive past process exit-attempt). (5) /event input bounds — STM_SLATE_EVENT_LINE_MAX (256 bytes) + zero-byte refusal (R101 P2-2 carry; same posture as /ctl/'s writable kinds). (6) Per-fid materialized body bounded at STM_SLATE_BODY_MAX (4 KiB); snprintf-then-check pattern. (7) Log ring buffer — STM_SLATE_LOG_LINES (100) most recent lines, STM_SLATE_LOG_LINE_MAX (320 bytes) per line; oldest dropped on overflow (no growth past cap). (8) Signal-mask discipline (R113 P1-1 carry from stratumd) — every per-connection worker pthread is spawned with SIGINT/SIGTERM/SIGHUP/SIGQUIT BLOCKED so signals route to the accept thread; broken posture = signal-routing deadlock at shutdown. (9) FIFO event dispatch — events processed in arrival order (slate.tla::EventFIFO); future writable kinds (panels/X/action, dialogs/.../result, editor/action — SLATE-3..5) MUST go through the same mu-guarded dispatch path so version bumps stay atomic and FIFO holds across all mutation surfaces. (10) **SLATE-2 backend lifecycle**: `stm_slate` holds an optional `stm_9p_client *backend` for the attached stratumd. ConnectionAtomic invariant: `connected == (socket_len > 0)`; PanelPathConsistent: connected ⇒ panel paths = "/", disconnected ⇒ panel paths = "". A SECOND mutex `backend_mu` serialises every libstratum-9p call against the held client (the lib is one-op-at-a-time per connection — see include/stratum/9p_client.h §Concurrency); without it, two slate connections both reading /panels/X/entries would corrupt the wire framing. backend_mu also pins the backend pointer for the duration of an op so Disconnect can't tear it down concurrently. **Lock ordering is backend_mu → mu**, never the reverse — Attach takes backend_mu, then briefly s->mu to swap state, then closes the prior client outside s->mu but UNDER backend_mu. Future writable kinds that interact with the backend (e.g., SLATE-3's panels/X/action descend-dir) MUST adopt the same lock-ordering discipline; never call into stm_9p_* without holding backend_mu. (11) **SLATE-2 attach trust posture**: /connection/attach accepts ARBITRARY socket paths at v2.0 — writing arbitrary paths doesn't exfiltrate data but a malicious local writer COULD redirect the panel to a different dataset. v1.1 may add per-uid ACL on attach (forward-noted; SLATE-DESIGN §7.1). Path bounded at STM_SLATE_SOCKET_MAX (1023 bytes); embedded NUL refused. Empty body = disconnect verb (NOT zero-byte refusal — clause 5's R101 P2-2 carry exempts /connection/attach because zero-len IS the documented disconnect verb). (12) **SLATE-2 panel-entries materialisation**: /panels/X/entries does its backend op (Twalk + Tlopen + Treaddir + per-entry Twalk + Tgetattr + Tclunk) at vops_lopen, OUTSIDE s->mu but UNDER backend_mu; result is cached in per-fid bulk_buf and served on subsequent reads — matches /log/tail's R114 P1-1 bulk_buf pattern. STM_SLATE_ENTRIES_MAX (200) caps body at ~64 KiB. **R115 P1-1 sanitization (load-bearing trust boundary)**: every entry name is sanitized at the line-render step — bytes < 0x20 OR == 0x7F replaced with '?' before snprintf into the line-oriented body. The ORIGINAL name is still used for the per-entry Twalk so the real backend file is resolved; only the rendered display gets sanitized. UTF-8 multi-byte sequences (≥ 0x80) pass through unchanged. This is the R99 P2-1 doctrine class extended to the entries-line surface — `fs_validate_dirent_name` only rejects '/' + '\\0' (POSIX permits anything else), so the safety boundary lives at the slate display layer. Future writable kinds that emit user-controlled strings into line-oriented bodies (e.g., SLATE-4 dialogs/.../body, SLATE-5 editor/active) MUST adopt the same posture. SLATE-3 will introduce `/panels/X/cursor` + `/panels/X/action` (writable); the action handler MUST inherit (a) backend_mu discipline for any descend-dir op, (b) version bump under s->mu, (c) cursor revalidation against the panel's current entries snapshot. (13) **Forward-note: deeper-than-MAX_WALK paths**: SLATE-2's `walk_to_cwd` parses cwd into a single Twalk with up to STM_9P_MAX_WALK (16) components; deeper paths return STM_EINVAL. SLATE-2's actual cwd is always "/" so this short-circuits to n_names=0; SLATE-3 (multi-component cwd via `goto`) MUST refactor this to iterative re-walk for deeper paths. Per-entry walk in panel_entries_render currently uses single-component-from-root, which is correct ONLY for cwd="/" — SLATE-3 must also refactor this to walk-from-cwd-then-name. (14) **SLATE-3a panel cursor + action**: /panels/X/cursor is the FIRST RW kind on slate (mode 0644 — readable AND writable on the same fid). vops_lopen now uses an accmode-vs-mode-bits gate (instead of a per-kind writable flag) so RDONLY/WRONLY/RDWR opens are validated against KIND_META[k].mode (0444 ⇒ readable, 0222 ⇒ writable). Read-capable opens materialise a body at lopen; write-only opens skip materialisation. Cursor write parses decimal — every byte must be a digit, leading-`-` refused, uint32_t overflow refused, empty-body refused, oversized refused. Cursor reset to 0 on attach + disconnect (alongside path reset). /panels/X/action is the FIRST writable verb-dispatch surface on slate; v1 supports `key Up` / `key Down` (cursor adjustment with clamp-at-0 for Up + clamp-at-UINT32_MAX for Down — though Down clamping is academic at v1 since the renderer enforces real bounds). Unknown verbs return STM_ENOTSUPPORTED so renderers know the action wasn't dispatched. SLATE-3b+ will add `key Enter` (descend into highlighted dir — composes with the deeper-cwd refactor in clause 13), `key F3` (view as file), and mouse verbs. Version bumps on cursor write AND on action verb that actually moved cursor; no-move actions (key Up at cursor=0) do NOT bump version (matches "version changes only via real state mutation" doctrine). (15) Forward-note: SLATE-3a does NOT yet implement /panels/X/selection (SLATE-3b), /dialogs/.../* (SLATE-4), /editor/* (SLATE-5), or /admin views (SLATE-7 gated on /ctl/-on-stratumd). The synfs schema must stay backward-compatible as those land — clients that hard-code v1.0 / v1.1 / v1.2 paths must continue working. |
| stratumd Unix-socket transport (v2) | `v2/src/cmd/stratumd/serve.c`, `v2/src/cmd/stratumd/main.c`, `v2/include/stratum/stratumd.h` | (P9-9P-4; v2-only; P9-CTL-2c added second-listener for /ctl/) Unix-socket daemon transport for the 9P server. Up to TWO listen sockets: FS (mandatory) + /ctl/ (opt-in via `ctl_socket_path`). Each accepted connection on FS gets a fresh `stm_9p_server` — one fid namespace per connection per CLAUDE.md / 9p.h doctrine; each accepted connection on /ctl/ gets a fresh `stm_lp9_server` — one fid namespace per connection per lp9.h doctrine. Each socket has its own SERIAL accept loop running on a dedicated pthread (FS on the calling thread of run(); /ctl/ on a worker thread spawned by run()). v2.0 is one-connection-at-a-time PER socket; concurrent multi-connection (epoll / pthread-per-connection) is forward-noted. Trust boundaries the reviewer must check: (1) wire framing — read 4-byte LE size header, reject sizes outside [STM_9P_HDR_SIZE, msize_max] (FS) / [STM_LP9_HDR_SIZE, msize_max] (CTL), read body, dispatch; out-of-range size disconnects; (2) peer credentials — SO_PEERCRED on Linux, getpeereid() on macOS/BSD, refuse-by-default on resolution failure (R95 P2-2; opt-in fallback via `allow_unauthenticated_peer`); (3) fs lifecycle — mount/unmount happens in `stm_stratumd_run`, NOT per-connection; concurrent serving against one `stm_fs *` is the regime under which R94 P2-1 (h_reflink stat-after-mutation) and the same-shape patterns at h_lcreate / h_mkdir / h_renameat become observable. The /ctl/ thread also reads stm_fs (via stm_ctl's getattr_at + materializers) concurrently with the FS thread; this is safe under the public stm_fs API's thread-safety contract but extends the R94 P2-1 race surface to /ctl/'s read paths. Reviewer of any concurrent-accept upgrade MUST address that whole stat-after-mutation class. (4) /ctl/ lifecycle ordering (P9-CTL-2c): stm_ctl is created AFTER fs mount and AFTER /ctl/ socket bind succeeds, but BEFORE the worker thread starts; tear-down is reverse — main thread shutdown(2) the /ctl/ listen fd to unblock accept(), pthread_join the worker, THEN stm_ctl_destroy, THEN unmount fs. Joining BEFORE stm_ctl_destroy is non-negotiable per R96 P2-1 (vops dispatch reads c->ctx; tearing ctx before join is use-after-free). Signal-mask discipline (R113 P1-1): the /ctl/ worker thread MUST be spawned with SIGINT/SIGTERM/SIGHUP/SIGQUIT BLOCKED so that process-directed signals are routed to the calling thread (which runs the FS accept loop and observes stop_flag → drives shutdown). Without this, POSIX delivers the signal to ANY unmasked thread; if it lands on the worker, the worker exits but the FS thread blocks forever in accept() → daemon hangs. Pattern: pthread_sigmask(SIG_BLOCK, &block_set) on the calling thread BEFORE pthread_create, pthread_sigmask(SIG_SETMASK, &prev) AFTER, so the worker inherits the blocked mask and the calling thread restores its own. Future multi-thread extensions (concurrent-accept upgrade, slate daemon, pluggable auth backends) MUST establish the same convention: main thread receives signals; worker threads have signals masked. (5) per-connection caller stamping (P9-CTL-2c): `stm_ctl_set_caller(ctl, peer_uid, peer_gid)` is called BEFORE `stm_lp9_server_create` on each /ctl/ accept; reset to (uid_t)-1 / (gid_t)-1 (the unset sentinel — fail-closed for admin checks) AFTER serve returns so a stale caller can't leak between connections. `stm_ctl_set_admin_uid(ctl, geteuid())` is called once at startup. (6) /ctl/ session drain (R101 P1-1 from CLAUDE.md /ctl/ trigger row clause 7): `stm_ctl_drop_all_sessions(c)` MUST fire between every pair of /ctl/ connections; serve_ctl_client calls it before returning. Without this, leaked sessions from connection N pre-empt connection N+1's fid allocations. (7) /ctl/ scope deferral: stratumd at v2.0 attaches stm_fs to stm_ctl but does NOT attach stm_pool / stm_scrub. Public getters `stm_fs_pool(fs)` / `stm_fs_scrub(fs)` are forward-noted to a future chunk; until then, /ctl/ over stratumd surfaces /version, /state, /datasets/, /admin/, /events, /debug/ but the /pools/(uuid) subtree readdir is empty. |
| FUSE callbacks | `src/cmd/fuse.c` | Same concerns as 9P: every callback is a trust boundary from kernel VFS. Single-threaded loop assumed — multi-threading would need locking. |
| Btree node cache | `src/btree/cache.c`, `stm_btree_read_node`, `stm_btree_write_node` | Cache correctness depends on invalidate-on-write + COW-fresh-paddr invariant. Any future path that writes a node without going through `stm_btree_write_node` breaks the contract silently. |
| Extent write ordering | `src/fs/fs.c::stm_fs_write`, `extent_write_data`, `extent_read_data` | Insert-before-free, ebuf tail-zero, partial-extent old-lookup are all audit-derived |
| Public API contracts | `include/stratum/fs.h`, `stm_fs_open_ro` vs `stm_fs_open` | RO vs RW contract depends on runtime guards; new accessors can create bypass surfaces |
| Inode allocator | `v2/src/inode/inode.c`, `v2/include/stratum/inode.h` | (P8-POSIX-1; v2-only — no v1 analog) The (ino, si_gen) tuple-uniqueness-across-time invariant from `v2/specs/inode.tla` underpins stale-fid detection in 9P (ARCH §11.3.2), per-file derived keys (§7.3.3), and NFS file handles. A buggy reuse path that doesn't bump gen exposes confused-deputy attacks. The two `inode_*_buggy.cfg` configs enumerate the failure modes the reviewer must rule out. [R69 P3-9: row uses explicit v2/ prefix since this surface only exists in v2.] |
| Dirent layer | `v2/src/dirent/dirent.c`, `v2/include/stratum/dirent.h` | (P8-POSIX-2; v2-only) Open-addressing chain integrity from `v2/specs/dirent.tla`. Three buggy configs (`dirent_unlink_uses_empty_buggy.cfg`, `dirent_create_overwrites_no_probe_buggy.cfg`, `dirent_lookup_stops_on_tombstone_buggy.cfg`) enumerate the canonical chain-integrity failure modes — silent loss of colliding-name reachability via missing-tombstone, silent overwrite of colliding occupant via no-probe-walk, and read-side analog of unlink-uses-empty. Writer-side guards must mirror decoder-side guards (R71 P1-1 lesson — same posture as the inode allocator). |
| Xattr layer | `v2/src/xattr/xattr.c`, `v2/include/stratum/xattr.h` | (P8-POSIX-6; v2-only) Open-addressing chain integrity from `v2/specs/xattr.tla` — structurally isomorphic to dirent.tla's write-side, keyed at `ino` instead of `dir_ino`. Three buggy configs (`xattr_unlink_uses_empty_buggy.cfg`, `xattr_create_overwrites_no_probe_buggy.cfg`, `xattr_lookup_stops_on_tombstone_buggy.cfg`) enumerate the canonical failure modes. Writer-side guards must mirror decoder-side guards on BOTH `name_len` AND `value_len` (R71 P1-1 + R77 P1-1 lesson — the OOB-read shape from inline data extends to xattr value records, so symmetric bounds must hold at both ≤ STM_XATTR_NAME_MAX (255) and ≤ STM_XATTR_VALUE_MAX (64 KiB) at every trust boundary). Tree-root introductions bind into Merkle chain in lockstep — `xattr_csum` is the 10th input to `compute_merkle_root`. |
| 9P client library (v2) | `v2/src/9p_client/9p_client.c`, `v2/include/stratum/9p_client.h` | (P9-LIB-1; v2-only) Synchronous 9P2000.L client per ARCH §10.2 ("libstratum-9p is the stable public ABI; all language bindings wrap it"). Public API at v2.0 (cumulative through P9-LIB-1d): read-side — dial+Tversion+Tattach handshake, stm_9p_walk, stm_9p_lopen, stm_9p_read, stm_9p_clunk, stm_9p_getattr, stm_9p_readdir; write-side — stm_9p_write (1b), stm_9p_lcreate / stm_9p_mkdir / stm_9p_unlinkat (1c), stm_9p_setattr / stm_9p_renameat / stm_9p_symlink / stm_9p_readlink / stm_9p_fsync (1d); stm_9p_link (1d-link). Tag auto-allocation (monotonic counter, STM_EOVERFLOW on NOTAG hit). Linux-ecode → stm_status mapping closed under err_map. Trust boundaries: (1) wire framing — every Rxx reply size is bound-checked against negotiated msize before parse, out-of-range disconnects with STM_EBACKEND; mirrors stratumd's `read_msg` / `write_msg` exactly. (2) Rlerror parsing — Linux ecode field mapped to stm_status via err_map(); unknown ecodes collapse to STM_EBACKEND so callers see a closed status set. (3) tag allocation — STM_EOVERFLOW once next_tag hits STM_9P_NOTAG; sync client can't disambiguate wrap-collisions cheaply, so the policy is "reconnect or fail". (4) tag-mismatch on reply — STM_EBACKEND + connection-poisoned posture: c->poisoned set by check_reply, every public op short-circuits at op_entry_check (R111 P3 F-11). (5) per-Rxx body-length validation — every parser checks body_len with strict equality (R111 P3 F-10): truncated replies + extra trailing bytes both surface as STM_EBACKEND. (6) caller-cap bound on every server-supplied count used as a write target (R111 P0 F-1 lesson): nwqid in Rwalk capped at caller's n_names, written in Rwrite capped at caller's count, tlen in Rreadlink capped at caller's buf_cap. The doctrine is doctrine carry: every NEW op MUST run op_entry_check at entry, validate names/targets at the lib boundary (saves a round-trip + stable status), use strict body-length equality on the reply, and bound any server-supplied count against the caller's cap before writing. (7) buffer pre-allocation — c->buf sized to msize at dial time; per-op malloc/free is avoided. Concurrency: NOT thread-safe across concurrent calls on the same client (one-op-at-a-time sync model); callers wanting concurrency open multiple clients. Hard-link surface complete: Tlink server handler + stm_9p_link client primitive shipped at P9-LIB-1d-link (alongside the new public API stm_fs_link_by_ino — POSIX link(2) by source inode, mirroring stm_fs_link's R74 P3-2 STM_EPERM-on-dir + R81 P3-1 ctime-stamp-after-success + R74 P3-3 rollback infallibility). Cross-dataset link refused server-side with STM_EXDEV. P9-LIB-2 async API extends with pipelined Txx + reply matching by tag; the bound-check + ecode-map discipline must carry forward. 9P2000-dialect support (for /ctl/ over the wire once /ctl/-on-stratumd lands) is forward-noted: the dial path must accept the version string as caller-configurable. |
| /ctl/ synthetic FS (v2) | `v2/src/ctl/synfs.c`, `v2/include/stratum/ctl.h` | (P9-CTL-1; P9-CTL-2b migrated codec from `stm_p9_server` (9P2000) to `stm_lp9_server` (.L) — KIND_META[] + qid encoding + materialize functions + admin gate + session lifecycle + audit-log doctrine ALL preserved verbatim; only the wire codec + vops adapter shape changed; v2-only) Operator-state synthetic filesystem (ARCH §14.3) served as a vops backend for the generic `stm_lp9_server`. Read paths report subsystem state; write paths trigger admin actions. Trust boundaries: (1) every read path must NOT leak secret bytes into the response (no DEKs, no on-disk structure that could enable offline attack); /ctl/state surfaces only stm_fs_stats counters which are non-secret. (2) every write path must validate caller uid against an operator policy — at v2.0 there are NO write paths (everything ENOACCES); future sub-chunks (e.g. `/pools/<name>/scrub`) MUST gate on uid before triggering the action. (3) per-fid materialized body buffer is bounded at STM_CTL_BODY_MAX (1 KiB); snprintf-then-check pattern guards against future field additions silently overflowing. (4) qid_path encoding reserves 56 bits — sub-chunks MUST keep the kind:8 high-byte invariant so a single qid can route across kind branches. (5) admin-gate trust boundary (P9-CTL-1d-uid): `caller_uid` + `admin_uid` are stamped pre-handle by stratumd via `stm_ctl_set_caller` / `stm_ctl_set_admin_uid`; `ctl_caller_is_admin(c)` predicates ANY admin-required kind. The default unset-caller AND default unset-admin_uid both fail-closed (uid 0 only is admin). Future sub-chunks adding admin-required kinds MUST set the `admin_required` bit in KIND_META[] AND the gate fires at vops_lopen (Tlopen) AND at vops_walk through admin-required dirs (R100 P2-1: walk-through must return ENOENT for non-admin so Tgetattr-after-walk cannot leak the file's mode bits — same posture as POSIX path-traversal through a mode-0500 dir). Forward-note: stratumd integration to call `stm_ctl_set_caller` with the SO_PEERCRED uid is not yet wired; until that lands, the admin gate fails-closed for all production deployments because the default caller is the unset sentinel. (6) writable-kind discipline (P9-CTL-1d-events): `KIND_ADMIN_CLEAR_EVENTS` is the FIRST writable kind — admin-gated, write-only mode 0200, vops_write specializes on it. Future writable kinds MUST inherit (a) admin gate at vops_lopen, (b) defense-in-depth admin re-check at vops_write, (c) zero-byte Twrite refusal (R101 P2-2 — `len == 0` returns STM_EINVAL since zero-byte writes are too easy to trigger accidentally), (d) consistent KIND_META[] mode-bit + vops_write/lopen dispatch. (7) sequential-server session-drain (P9-CTL-1d-events R101 P1-1): stratumd's serial-accept loop integrating /ctl/ MUST call `stm_ctl_drop_all_sessions(c)` between connections (after stm_lp9_server_destroy, before next accept iteration). Without this, leaked sessions from connection N pre-empt connection N+1's fid allocations and cause stale-snapshot reads + permanent slot depletion. The doctrine "safe across SEQUENTIAL stm_lp9_server" presumes the drain. (8) reader-vs-clear snapshot invalidation (R101 P2-1): /admin/clear-events MUST walk active sessions and reset uses_event_buf+snapshot_len for any /events fid, otherwise post-clear reads serve zero-padded frankenstein bytes. The same pattern applies to ANY future writable kind that mutates a buffer also surfaced by a snapshot-at-Tlopen reader. (9) admin-only diagnostic dir trees (P9-CTL-1d-debug-alloc): /debug/ is the second admin-only dir subtree after /admin/. Every NEW admin-only dir tree MUST (a) flag `admin_required=true` in KIND_META[] for the dir kind itself AND for every leaf child kind, (b) add an explicit `case KIND_X_DIR:` to vops_walk with `if (!ctl_caller_is_admin(c)) return STM_ENOENT;` at the head — clause 5's R100 P2-1 walk-through gate scales to N trees. The /debug/ surface composes against fs+sync (not pool) — `stm_fs_alloc_stats_get` is the trust-boundary wrapper that resolves stm_sync_alloc + calls stm_alloc_stats_get under fs->lock with stm_fs_stats_get's wedged-OK posture (diagnostics MUST work when fs is wedged). Future /debug/ sub-chunks (tree-walk, extent-map, integrity-verify) introduce their own thin stm_fs read-side wrappers following the same posture. (10) bulk-format kinds + label injection (P9-CTL-1e): /pools/<uuid>/metrics/prometheus is the FIRST kind whose worst-case body exceeds STM_CTL_BODY_MAX (1 KiB). The per-fid `bulk_buf` field in `ctl_session` is heap-allocated at vops_lopen's materializer call, sized via realloc-doubling growth bounded by STM_CTL_METRICS_MAX (64 KiB), and freed at vops_clunk → session_free_locked. Future bulk-format kinds (e.g., debug tree-walk dumps, extent-map dumps) MUST (a) set s->bulk_buf + s->bulk_len in their materializer; (b) NOT touch s->buf for these kinds (the bulk_buf branch in vops_read pre-empts the bounded-body branch); (c) preserve the free + memset discipline in session_free_locked (free runs BEFORE the memset that clears the slot pointer — the comment is load-bearing; reverse the order and the alloc leaks). The Prometheus exposition format is line-oriented ASCII; label values bounded to a fixed allowlist (UUIDs are 36-char hex; enum-name strings filtered through device_*_name / scrub_state_name with the R98 P3-4 trailing-"unknown" tamper-resilience). NO user-supplied strings flow into Prometheus labels at v2.0 (no dataset names, no snapshot names) — defers the Prometheus label-escape rule. R99 P2-1 line-injection class extends to label-value-injection in the exposition format: when per-dataset metrics are added, the dataset name MUST be sanitized OR labels keyed by dataset_id only. /metrics/ is world-readable mode 0444 (no admin gate — exposition is public per ARCH §14.8.1); body content adapts to fs/scrub attachment but is always renderable when c->pool is attached. The materializer takes each subsystem's lock independently (no cross-subsystem lock held concurrently); pool roster snapshot under stm_pool_lock_shared, fs gauges via stm_fs_stats_get + stm_fs_dataset_count, scrub status via stm_scrub_status_get. Forward-note: OTLP exposition (binary protobuf) is deferred to a sidecar translator; per-dataset op counters + latency histograms deferred until instrumentation hot-path work. |

**How to run an audit round:**

1. Spawn an Opus 4.7 soundness-prosecutor agent (general-purpose subagent, `run_in_background: true`).
2. In the prompt, include `memory/audit_r15_closed_list.md` contents as the "already fixed — do not re-report" preamble.
3. Scope the prompt to the surface you changed — e.g. "re-audit the changes in `src/snap/snap.c` commits X..Y for nonce-uniqueness / torn-write / wedge-state regressions."
4. Wait for the completion notification. Fix any P0/P1/P2 findings before merge.

## Invariants that must hold

See `docs/STRATUM.md` §22 "Soundness invariants" for the full list. The load-bearing ones:

- **Nonce uniqueness**: `disk ss_gen > fs->gen` at all times post-mount. Every write encrypts under `(paddr, write_gen=fs->gen)` and those pairs must be globally unique across the volume's lifetime.
- **Sync is three-phase**: reservation (G+1, pre-flush root) → flush at gen G → final (G+2, post-flush root). `fs->gen` advances by 1 per sync. Any refactor must preserve the invariant across every torn-write-or-crash boundary.
- **Rollback-bump before allocator swap**: if the bump write fails, `old_alloc` must still hold live refcounts for pre-rollback paddrs so they don't get reused at the same gen.
- **Wedged / read-only containment**: `fs->wedged` and `fs->read_only` are runtime-enforced at every public API entry. New public APIs must call `STM_FS_GUARD_READ` / `STM_FS_GUARD_WRITE`.
- **Counter clamps at mount**: `ss_gen`, `ss_next_ino`, `ss_next_snap_id` all rejected if near UINT64_MAX; walk-derived raises from on-disk tree contents.

## Technical reference upkeep (v2 only)

The v2 tree carries a comprehensive as-built reference at `v2/docs/REFERENCE.md` (top-level index + maintenance policy) and `v2/docs/reference/00..11.md` (per-subsystem deep dives). Every chunk that lands in v2/ MUST update the relevant reference section in the **same PR / commit** that lands the code.  Concretely:

- **New module / new public API** → add or extend the corresponding `reference/NN-*.md` section.
- **Change to a documented invariant or contract** → update the section, and (per the spec-first policy above) update the spec FIRST.
- **New TLA+ spec or buggy config** → update `reference/10-specs.md`'s catalog.
- **New term / acronym / state name** → add to `reference/11-glossary.md`.
- **`STM_UB_VERSION` bump or feature-flag change** → update `reference/00-overview.md`'s UB-versioning table.
- **Snapshot drift** (test count, spec count, tip hash in `REFERENCE.md`) — refresh in the close commit.

The reference is intentionally distinct from `docs/ARCHITECTURE.md` (design intent, including unimplemented work) and `docs/ROADMAP-V2.md` (phased plan): it documents what EXISTS in the tree right now, with `file:line` citations.  When the three disagree, the spec wins, the reference is corrected, and ARCHITECTURE.md gets an issue noted for cross-check.

If a chunk's diff doesn't include a reference-doc update, the reviewer should ask whether it was intentional (e.g., pure bug fix that doesn't change documented surface) or an oversight.

**Phase 9 deferment** (R96 P3-8): the per-subsystem `reference/NN-*.md` catalog last had entries for the post-P7 (Phase 7) modules. Phase 8 (POSIX surface — `inode/`, `dirent/`, `xattr/`, `locks/`) and Phase 9 (9P-server stack — `9p/`, `cmd/stratumd/`, `ctl/`) chunks have shipped without per-subsystem reference docs; the per-chunk detail lives in `phase8-status.md` / `phase9-status.md` instead. The REFERENCE.md Snapshot section is updated per-chunk with a tip line summary. Backfilling the per-subsystem catalog is a chunk-of-its-own scheduled before Phase 9 close. Until that chunk lands, Phase 8/9 chunks update phase status docs + Snapshot tip; new per-subsystem `reference/NN-*.md` files are NOT required as a precondition for chunk merge.

## Regression testing

- Every audit finding that can be made to fail without the fix should land a regression test. See `tests/test_fs.c::test_fs_sync_two_phase_gen_invariant`, `test_fs_mount_bump_encrypted`, `test_fs_write_gap_is_zeroed*`, `tests/test_snap.c::test_snap_rollback_bumps_gen_encrypted`, `tests/test_p9.c::test_p9_wire_validation`, `test_p9_fid_cache_and_dup` for the pattern.
- Before committing, run all 11 C suites + 10 Rust integration tests: `cmake --build build && for t in types key block node btree fs compress crypto snap p9 alloc; do build/tests/test_$t; done && (cd tui && cargo test --test integration --release)`.

## Clangd false positives

The CMake build is the source of truth. Clangd emits many "file not found" / "undeclared identifier" diagnostics in `src/` and `include/` because it doesn't see the cmake-generated compile commands for this tree. Ignore those and trust `cmake --build build`.

## Session-state files

- Built binaries deploy to `/Users/northkillpd/projects/dist/{stratum,stratum-tui}` and must be re-signed after deploy: `codesign --force --sign - <path>`.
- Tasks tracked via the in-conversation task list; one task per audit finding, per-round re-audit task as a checkpoint.
