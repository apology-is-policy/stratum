\* Fixed-impl model check: RequireRedundancyCheck=TRUE.
\* Expect: all invariants hold.

CONSTANTS
    AllDevices              = {1, 2, 3, 4}
    MirrorN                 = 2
    MaxGen                  = 4
    RequireRedundancyCheck  = TRUE

INIT Init
NEXT Next

INVARIANTS
    TypeOK
    RedundancyPreservedOnRemove
    AddDeviceIdempotent
    ReconcileRestoresState
    NoOrphanOnRemove
