\* Healthy fixed config — the canonical green run.
\*
\* Two connections, three paths, four sources (one being the default).
\* Bind cap = 2 keeps the per-connection state bounded; the model
\* checker explores every interleaving of attach/bind/unbind/lookup
\* across both connections without any buggy variants enabled.
\*
\* Expected verdict: TLC explores ~all interleavings up to symmetry and
\* reports zero invariant violations. Both LookupReflectsOwnBindings and
\* DetachClears hold by construction of the healthy actions.
SPECIFICATION Spec

CONSTANTS
    Connections             = {c1, c2}
    Paths                   = {p_root, p_home}
    Sources                 = {s_default, s_tank, s_archive}
    DefaultSource           = s_default
    NONE                    = NONE
    MaxBindsPerConn         = 2
    BuggyGlobalBindings     = FALSE
    BuggyDetachLeaks        = FALSE
    BuggyUnbindCrosstalk    = FALSE
    BuggyLookupCrosstalk    = FALSE

INVARIANTS
    Invariants
