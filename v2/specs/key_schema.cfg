\* R10 P2-3: the P4-4a MVP populates only dataset 0. The spec's
\* Init initializes every dataset in the Datasets set — so we scope
\* Datasets to {0} for the MVP config. P4-4c (multi-dataset +
\* rotation) widens this back to {0, 1, 2, ...} once the
\* dataset-create path atomically inserts a CURRENT key.
CONSTANTS
    Datasets  = {0}
    MaxKeyId  = 3
    MaxRefs   = 2

SPECIFICATION Spec

INVARIANT Invariants
