\* dcache_provisional — BUGGY config: the pre-RC-6 code (the insert
\* births VISIBLE; the recheck+self-remove runs after). TLC must find
\* the RC-3 audit-F1 three-party trace as a NoVisiblePostEvict
\* violation: Resolve -> EvictRemoveSlot -> EvictDrain -> Insert
\* (visible, landing after the drain) — servable after evict returned,
\* until the populator's own publish removes it.
SPECIFICATION Spec
CONSTANTS
    Populators = {p1, p2}
    BuggyVisibleBirth = TRUE
    BuggySplitPublish = FALSE
INVARIANTS
    TypeOK
    NoVisiblePostEvict
