\* Fixed config — clone lifecycle with 3 dataset slots + 2 snap slots.
\* Exercises Create snap → Create clone → Delete clone → Promote +
\* refused-Delete-when-clones.
SPECIFICATION Spec

CONSTANTS
    MaxDatasets = 3
    MaxSnaps = 2
    BuggyDeleteSnapWithClones = FALSE

INVARIANTS
    TypeOK
    RootInvariant
    CloneOriginPresent
    SnapWithClonesUndeletable
    PromoteIsTerminalForOrigin
    NoOriginMeansNotClone
    DatasetIdMonotonic
    SnapIdMonotonic
