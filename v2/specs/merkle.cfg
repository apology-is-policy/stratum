SPECIFICATION Spec

\* Small enough to finish in seconds; wide enough to exercise
\* write/commit/tamper interleavings over multi-leaf + internal cases.
CONSTANTS
    Paddrs = {p0, p1, p2, p3}
    Values = {a, b}
    Salt   = "salt"

INVARIANTS
    Invariants
