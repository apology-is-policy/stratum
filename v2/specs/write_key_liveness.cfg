\* write_key_liveness — clean config (the RC-3 fix): the Phase-3
\* re-validation holds NoDeadKeyIndexed and the retry terminates.
SPECIFICATION Spec
CONSTANTS
    Writers = {w1, w2}
    MaxKeys = 4
    BuggyNoRevalidate = FALSE
INVARIANTS
    TypeOK
    NoDeadKeyIndexed
PROPERTIES
    EventuallyAllDone
