SPECIFICATION Spec

\* Small instance that still exercises the race: 2 readers, chain up to 2
\* deltas, 3 total prepends, 3 epochs. Finishes in a few seconds on one
\* core.
CONSTANTS
    ReaderThreads  = {r0, r1}
    MaxChainLen    = 2
    MaxDeltas      = 3
    MaxGlobalEpoch = 3

INVARIANTS
    Invariants
