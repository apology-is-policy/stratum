SPECIFICATION Spec
INVARIANT Invariants
CONSTANTS
    \* Buggy variant: Flush / DirectWrite picks an already-in-use paddr.
    \* PaddrUniquenessAcrossCommitted fires when two committed extents
    \* share a paddr — the (paddr, gen) AEAD nonce uniqueness invariant
    \* downstream relies on PaddrFreshness composed with the allocator's
    \* NoReuseInSameGen; the writeback layer must not introduce reuse.
    MaxInos          = 2
    MaxOff           = 3
    MaxPaddrs        = 4
    MaxTxg           = 2
    MaxSeq           = 4
    InodeCapBlocks   = 2
    GlobalCapBlocks  = 3
    BuggyReadSkipsBuffer  = FALSE
    BuggyFlushReusesPaddr = TRUE
