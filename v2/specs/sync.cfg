SPECIFICATION Spec

\* Small state space — demonstrates the protocol, finishes in seconds.
\* Phase-9 exhaustive checks will widen bounds.
CONSTANTS
    Paddrs  = {p0, p1, p2}
    MaxTxg  = 4
    UBSlots = 2

INVARIANTS
    Invariants
