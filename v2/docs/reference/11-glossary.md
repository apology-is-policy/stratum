# 11 — Glossary

Terms, acronyms, and invariant names used across the codebase and
this reference. Cross-referenced to the reference chapter that
documents them in depth.

## Primitives

| Term | Meaning | See |
|---|---|---|
| **AEAD** | Authenticated Encryption with Associated Data. Symmetric cipher + MAC bundle — encrypt binds a ciphertext to a key, nonce, and associated data such that tamper is detectable. | [01-crypto](01-crypto.md) |
| **AEGIS-256** | Hardware-accelerated AEAD used by default on CPUs with AES-NI or ARMv8 crypto extensions. 32-byte key, 32-byte nonce, 32-byte tag. | [01-crypto](01-crypto.md) |
| **AD** | Associated Data — unencrypted bytes bound to a ciphertext via the AEAD MAC. Stratum uses it to tie metadata-node ciphertexts to `(pool_uuid, device_uuid)` so cross-device replay fails. | [01-crypto](01-crypto.md) |
| **ASan** | AddressSanitizer. Compile-time instrumentation catching heap / stack / use-after-free bugs. One of three CI matrix configurations. | [00-overview](00-overview.md) |
| **Argon2id** | Memory-hard passphrase-to-key function. Used to derive the wrap-key-derivation seed at pool create / rekey. | [01-crypto](01-crypto.md) |
| **BLAKE3** | Cryptographic hash (32 B output, tree-friendly, ~2–6 GB/s/core). Stratum's Merkle chain, bp_csum values, and truncated node csums are BLAKE3. | [01-crypto](01-crypto.md) |
| **bdev** | `stm_bdev` — the block device abstraction. One per physical device / loopback file. | [02-block](02-block.md) |
| **Bε-tree** | Bounded-epsilon tree. B+tree variant with message buffers at internal nodes, amortizing writes across levels. Stratum's metadata data structure. | [03-btree](03-btree.md) |
| **bp_csum** | BLAKE3-256 over a block pointer's target. Stored inside `stm_bptr`, forms the Merkle chain. For AEAD-encrypted targets, the AEAD tag acts as bp_csum. | [07-sb-sync](07-sb-sync.md) |
| **bptr** | `stm_bptr`, 64-byte block pointer: paddr + kind + flags + csum. | [07-sb-sync](07-sb-sync.md) |
| **Bw-tree** | Lock-free Bε-tree variant with CAS-built delta chains. Readers walk chains pinned by EBR; writers CAS-prepend. | [03-btree](03-btree.md) |
| **CAS** (atomic) | Compare-and-swap atomic primitive. Used ubiquitously in the Bw-tree. | [03-btree](03-btree.md) |
| **CAS** (storage) | Content-addressable storage — future dedup tier. Not confused with the above from context. | ARCH §7.16 |
| **CSPRNG** | Cryptographically-secure pseudo-random number generator. `stm_random_bytes` wraps libsodium's `randombytes_buf`. | [01-crypto](01-crypto.md) |
| **DEK** | Data Encryption Key. Per-dataset symmetric key. Wrapped blob on disk, unwrapped in RAM when a dataset is open. | [06-keyschema](06-keyschema.md) |
| **EBR** | Epoch-Based Reclamation. Lock-free memory-reclamation scheme enabling `btree_lf` to retire old chains safely. | [04-ebr](04-ebr.md) |
| **HKDF-SHA256** | RFC 5869 key derivation function. Used to derive the PQ-hybrid wrap key from the X25519 + ML-KEM-768 shared secrets. | [01-crypto](01-crypto.md) |
| **HMAC-SIV** | Stratum's XChaCha20-SIV construction — HMAC-SHA256 acts as the SIV tag generator over the full nonce, AD, and plaintext. | [01-crypto](01-crypto.md) |
| **HPKE** | Hybrid Public-Key Encryption (RFC 9180). Stratum's hybrid wrap is HPKE-inspired but not spec-compliant. | [01-crypto](01-crypto.md) |
| **io_uring** | Linux async I/O submission ring. One of the `stm_bdev` backends. | [02-block](02-block.md) |
| **jaanus** | Remote key-wrap daemon. Mutually exclusive with the in-process keyfile path for any key op. | [07-sb-sync](07-sb-sync.md) |
| **Merkle chain** | Hash of hashes: each metadata node's bp_csum covers its children, ultimately chaining to `ub_merkle_root`. Tamper of any covered node changes the root. | [01-crypto](01-crypto.md), [07-sb-sync](07-sb-sync.md) |
| **MLKEM-768** | ML-KEM-768 (NIST FIPS 203). Post-quantum key-encapsulation mechanism used in the hybrid wrap. Optional — graceful fallback to zero-padded slots if liboqs absent. | [01-crypto](01-crypto.md) |
| **Nonce** | In AEAD context, the 32-byte input that must be unique per `(key, op)` pair. Stratum's metadata-nonce layout: `paddr || gen || pool_uuid`. | [01-crypto](01-crypto.md) |
| **paddr** | Physical address — 64-bit value = `(16-bit device_id << 48) | 48-bit block_offset`. | [07-sb-sync](07-sb-sync.md) |
| **Poly1305** | 16-byte MAC used by XChaCha20-Poly1305. Used in `stm_hybrid_wrap`'s AEAD envelope. | [01-crypto](01-crypto.md) |
| **Roster** | The per-pool device list, up to 64 slots. Persisted in every uberblock's `ub_roster[2048]`. | [08-pool-redundancy](08-pool-redundancy.md) |
| **roster_hash** | le64 truncation of BLAKE3 over the 2048-byte roster. Witness for add/remove churn. | [08-pool-redundancy](08-pool-redundancy.md) |
| **TLA+** | Formal-specification language (Leslie Lamport). Stratum's 13 specs are TLA+. TLC model-checker verifies them at bounded scopes. | [10-specs](10-specs.md) |
| **TSan** | ThreadSanitizer. Compile-time instrumentation catching data races. CI matrix config. | [00-overview](00-overview.md) |
| **uberblock** | 4 KiB on-disk struct holding pool metadata roots + counters for one commit. 63 slots per label × 4 labels per device. | [07-sb-sync](07-sb-sync.md) |
| **wk** | `stm_hybrid_keys` — wrap keypair (PK + SK). 1216-byte PK, 2432-byte SK. In-process path (as opposed to janus). | [07-sb-sync](07-sb-sync.md) |
| **X25519** | Classical elliptic-curve DH. Classical half of the PQ-hybrid wrap. | [01-crypto](01-crypto.md) |
| **XChaCha20-SIV** | Stratum's custom nonce-misuse-resistant AEAD. 64-byte key (K_MAC ‖ K_ENC), 32-byte nonce, 16-byte tag. | [01-crypto](01-crypto.md) |
| **xxHash3** | Fast non-cryptographic hash. Used for `se_xxh` on unencrypted extents and in-memory structural hashing. | [01-crypto](01-crypto.md) |

## Protocol terms

| Term | Meaning | See |
|---|---|---|
| **auth_gen** | Most-recent committed final gen with quorum. 0 on fresh pool. Commits advance by 2; mount-claim by 1. | [07-sb-sync](07-sb-sync.md) |
| **commit ring** | The circular 63-slot-per-label × 4-label UB rotation. `label = gen % 4; slot = gen % 63`. | [07-sb-sync](07-sb-sync.md) |
| **content-quorum** | Byte-level agreement of shared UB fields across ≥quorum devices at `auth_gen`. Violating pools are rejected at mount (R14). | [07-sb-sync](07-sb-sync.md), [10-specs](10-specs.md) |
| **EvacuationAtomic** | Spec invariant (evac.tla): every block has ≥`MirrorN` replicas on non-REMOVED devices at every reachable state. | [10-specs](10-specs.md) |
| **gen** | Generation counter — monotonic per commit. 1 for fresh pool's first commit; `auth + 2` for subsequent commits. | [07-sb-sync](07-sb-sync.md) |
| **MountGenBump** | Spec invariant (sync.tla, quorum.tla): every mount must advance the durable gen past any in-flight gen from a previous process. Load-bearing for nonce uniqueness across crash recovery. | [10-specs](10-specs.md) |
| **orphan gen** | A gen at which fewer than quorum devices hold a valid UB; never authoritative; overwritten by the next commit. | [07-sb-sync](07-sb-sync.md) |
| **PENDING** | Alloc-tree entry state: `refcount == 0`, awaiting `sync_commit` sweep. Bitmap bit stays set. | [05-bootstrap-alloc](05-bootstrap-alloc.md) |
| **Quorum** | `⌊N/2⌋ + 1` of pool devices. Required for Phase 1 reservation + Phase 3 final + mount-claim. | [07-sb-sync](07-sb-sync.md) |
| **reservation UB** | Phase 1 UB at `gen = auth+1` carrying the PREVIOUS authoritative UB's roots. Rollback target if Phase 3 fails. | [07-sb-sync](07-sb-sync.md) |
| **safe removal** | Sync-layer wrappers that probe alloc drain before pool state transition. `stm_sync_remove_device`, `stm_sync_finish_evacuation`. | [08-pool-redundancy](08-pool-redundancy.md) |
| **verify-callback** | Caller-supplied per-block classification function (P5-5-β). Returns OK / REPAIRED / UNREPAIRABLE; encapsulates the bptr-aware redundancy iteration. Future P6 extent manager plugs the production cb. | [09-scrub](09-scrub.md) |
| **CallbackSetExclusivity** | Spec invariant (scrub.tla): the four scrub counters split cleanly by mode. α (no cb) keeps `repaired`/`unrepairable` at 0; β (cb installed) keeps `failed` at 0. | [10-specs](10-specs.md) |
| **wedge** | Runtime state where sync refuses all mutations because an in-flight rollback itself failed. `STM_EWEDGED`. | [07-sb-sync](07-sb-sync.md) |

## Device states

| State | Meaning |
|---|---|
| `UNSET` | Slot header before `add_device`; not a live state. |
| `ONLINE` | Normal operating state; participates in commit + mirror. |
| `OFFLINE` | Reserved; no current API emits. |
| `DEGRADED` | Reserved; no current API emits. |
| `FAULTED` | Admin- or fault-triggered. Excluded from `mirror_read`; bdev retained for rejoin. |
| `REMOVED` | Device detached from pool; bdev = NULL; UUID preserved for burned-UUID tracking. |
| `EVACUATING` | Draining its replicas onto survivors; excluded from new reserves. Transitions to REMOVED on `finish_evacuation`. |

## Status codes (selective)

| Code | Meaning |
|---|---|
| `STM_OK` | Success. |
| `STM_EINVAL` | Shape / arg validation. |
| `STM_ENOENT` | Not found (e.g. tree entry, UB slot). |
| `STM_EEXIST` | Duplicate (e.g. UUID collision in roster). |
| `STM_EBUSY` | Resource in wrong state to accept op (e.g. tree not drained for safe-remove). |
| `STM_ENOMEM` | Allocation failure. |
| `STM_EROFS` | Read-only pool refusing mutation. |
| `STM_EQUORUM` | Multi-device commit failed to reach quorum. |
| `STM_ECORRUPT` | On-disk corruption detected (csum / Merkle / format). |
| `STM_EBADTAG` | AEAD tag verification failed. |
| `STM_EBADVERSION` | On-disk format version unsupported. |
| `STM_ERANGE` | Caller buffer too small / value out of range. |
| `STM_ENOTSUPPORTED` | Feature reserved but not yet implemented (e.g. RS / LRC, dev 0 removal). |
| `STM_EWEDGED` | Sync handle wedged (post-rollback-of-rollback failure). |
| `STM_EIO` | Underlying block device I/O error. |

## Phase numbering

| Phase | Scope |
|---|---|
| 0 | Design docs (VISION / COMPARISON / NOVEL / ARCHITECTURE / ROADMAP-V2). |
| 1 | Crypto + hash + block + CMake + TLA+ infrastructure. |
| 2 | Bε-tree + Bw-tree + EBR + R0-R5 audits. |
| 3 | Single-device sync + uberblock + allocator + R6-R12 audits. |
| 4 | AEAD-AD + per-extent integrity + keyschema + PQ-hybrid wrap + janus + R13-R14b audits. |
| 5 | Multi-device pool + quorum commit + mirror(n) + device lifecycle + scrub (α + β) + R15-R23+ audits. |
| 6 | Namespace (datasets / snapshots / clones / dead-list). |
| 7 | Extent layer + cold-tier (CAS) + send/recv + reflinks. |
| 8 | 9P / FUSE surface. |
| 9+ | Post-MVP (RS/LRC, tiering, send/recv, encryption-at-rest-with-rotation sweep). |

## Audit round names

Each round audits a specific change surface. Closed-list preamble
files (in user memory) track which P0/P1/P2/P3 findings each round
resolved — auditors consult these so they don't re-report already-
closed items.

| Round | Scope | Tip at close |
|---|---|---|
| R0-R14b | v1 tree audits + R14b's P5-2 fix review. | (v1-era; see memory/audit_r15_closed_list.md + audit_v2_r0_closed_list.md) |
| R15 | P5-3 scope (mirror redundancy). | `dd8a275` |
| R16 | P5-4a scope (add_device + metadata_nonce.tla). | `a93ce5d` |
| R17 | P5-4b scope (remove + evacuation). | `350e144` |
| R18 | P5-4b-ii-β scope (per-pool rwlock). | `3c370c5` |
| R19 | P5-4c-α scope (replace_device_online). | `9207ae7` |
| R20 | P5-5-α scope (scrub verify-only). | `25d7c4a` |
| R21 | P5-6 full-phase audit. | `3edeb69` |
| R22 | P5-7 scope (replace resume from ADDED-ONLINE). | `e5fe085` |
| R23 | P5-8 scope (replace-in-flight claim + atomic resume). | `5468dac` |
| R24 | P5-5-β scope (scrub repair via cb). | `52503fe` |
| R25 | Backlog hygiene (R23 P3-2 + R20 P3-2/P3-3). | `cd6cb04` |
| R26 | P5-durable-cursors scope (γ scrub + STM_UB_VERSION 7→8). | `a6249eb` |
| R27 | P7-prework FastCDC content-defined chunking. | `a2ffd38` |
| R28 | P6-2 dataset-module C impl scope. | `bdb888b` |
| R29 | P6-3 snapshot-module C impl scope. | `000d394` |
| R30 | P6-4 property API on dataset module. | `8be3628` |
| R31 | P6-persist (dataset + snapshot persistent storage + UB v8→v9). | `bffee62` |
| R32 | P6-clone (clone C impl + UB v9→v10). | `4503405` |
| R33 | P6-deadlist C impl (snapshot dead-list + UB v10→v11). | `d4efeeb` |
| R34 | P7-2 extent index C impl (in-RAM MVP per extent.tla). | `433d2dd` |
| R35 | P7-3 extent persistence (UB v11→v12 + sync wire-in). | `b223975` |
| R36 | P7-4 fs.c/sync.c COW path integration (extent → dead-list / alloc-free routing). | `64a6278` |
| R37 | P7-5 production scrub β verify-callback (paddr→bptr resolver via extent walk). | `fc5f619` |
| R38 | P7-6 replica-list extension on extent records (UB v13 + bptr.tla full replica-walk in scrub cb). | `8d0c172` |
| R39 | P7-7 send/recv MVP (full-send byte-stream protocol, recv re-encrypts under target pool's key). | `73e9f20` |
| R40 | P7-8 snap-gen alignment (extent_txg field on snapshot record + UB v13→v14; closes incremental-send filter gap from P7-7). | `c9c29ee` |
| R41 | P7-9 truncate partial-extent split (`stm_sync_truncate` shrinks the crossing extent via read+decrypt+re-encrypt under fresh paddrs; extent.tla::Truncate refined with branch-(b)). | `5530a0e` |
| R42 | P7-10 per-dataset DEKs in fs_write/_read (extent `key_id` field + UB v14→v15 + sweep refcount enforcement). | `394150a` |
| R43 | P7-11 truncate `_locked` atomicity refactor (write_extent / read_extent split into public-wrapper + `_locked`; truncate spans sync->lock across all 3 phases; closes R41 P3-1 case (a) + R41 P3-2; case (b) Phase 3 ENOMEM partial-state remains as a POSIX-atomicity gap, no invariant violation). | `9af916e` |
| R44 | P7-12 truncate fault-free Phase 3 — closes R41 P3-1 case (b) via pre-allocation (new extent APIs `stm_extent_truncate_peek` + `_into`; sync_truncate peek-counts before Phase 2 so any ENOMEM lands index-unchanged). | `bb5e088` |
| R45 | P7-13 `stm_fs_create_dataset` (one-step bundle of `stm_dataset_create_child` + `stm_sync_add_dataset_key` under `fs->lock`; rolls back create on add_key failure; wrap-key source bound to mount-time `keyfile_path` / `janus_socket` per ARCH §7.7's pool-wide framing — closes the wrap-key mismatch footgun by construction). | `f30db5e` |
| R46 | P7-14 snap chain-ordering regression test (closes R40 P3-3; new gated test-only `stm_snapshot_create_for_test` produces chain-inverted shapes for the on-disk validator; new `STRATUM_BUILD_TESTING_HOOKS` CMake option strips the bypass symbol from production builds). | `485f0ef` |
| R47 | P7-15 repair-log persistence (closes R38 P3-1; new `src/repair_log/` module + UB v15→v16 with `ub_repair_log_root` / `ub_repair_log_root_gen` / `ub_repair_log_next_seq`; scrub β cb emits per-rewrite audit-trail entries; Merkle root chains `repair_log_csum` so tamper fires at mount; emit-time cap with `STM_REPAIR_LOG_MAX_ENTRIES = 2048`). | `82fe30e` |
| R48 | P7-16 reflinks (ARCH §11.12 / §8.6.3; new `stm_fs_reflink` POSIX-shape FICLONE; UB v16→v17 with origin-stamping in extent records; `LiveReplicasDisjoint` relaxed to `SharedReplicasAreCohabit`; R48 P0-1 fold added `link_gen` field at offset 88 so incremental send catches reflinked records in (S_from, S_to]; R48 P1-1 fold added refcount-aware `sync_drop_paddr_locked` so reflink + snap + dual-overwrite no longer hits R33 P2; new STM_EXDEV error code; cross-dataset reflinks deferred per same-key requirement). | `1951f65` |
| R49 | P7-CAS cold-tier index foundation (ARCH §6.9 / §7.6.3 / §12.10, NOVEL #3, cas.tla); UB v17→v18 adds `ub_cas_index_root_gen` carved from `ub_reserved` (the tree-root field `ub_cas_index_root` at offset 288 was carved at v3 but unused until now); new `STM_BPTR_KIND_CAS = 6` (also carved at v3) finally used; new `src/cas/cas_index.c` module + `stm_cas_index` API; extent record value layout adds 1-byte `kind` discriminator at byte 0 (HOT vs COLD); new AEAD AD struct `stm_ad_cas` (56 bytes, ARCH §7.6.3); new cas.tla spec (6 actions, 9 invariants, 6 buggy demos); migration / rehydration paths deferred to P7-CAS-2. | `61205c7` |
| R50 | P7-CAS-2 migration / rehydrate / auto-GC data plane: new `stm_fs_migrate_to_cold` + `stm_sync_migrate_to_cold` driving per-extent read+decrypt → BLAKE3-256 hash → CAS lookup-or-insert → atomic hot→cold swap via new `stm_extent_migrate_to_cold` (preserves NoOverlapWithinIno) → drop-route HOT replicas through refcount-aware `sync_drop_paddr_locked`; rehydrate-on-write integrated into `stm_sync_write_extent_locked` + `stm_sync_truncate`'s past-extent drop loop via cold-overlap pre-scan + post-deref bookend; new COLD-aware read branch in `stm_sync_read_extent_locked` decrypts CAS chunks under stm_ad_cas + pool metadata_key per ARCH §7.6.3; auto-GC sweep at `stm_sync_commit` walks refcount=0 entries via `cas_auto_gc_sweep_locked` + routes paddrs through `stm_alloc_free` (closes R49 P2-2); allocator-fresh paddr property closes R49 P2-1 (HotColdReplicasDisjoint enforced at migration-layer caller via `stm_alloc_reserve` rather than cross-index runtime check). R50 P2-2 STM_EBUSY skip + P3-2 pt_out check + P3-6 comment fixed inline; R50 P2-1 + P2-3 forward-noted as P7-CAS-3 deferrals (paddr-leak window at crash boundary; partial-sweep transactionality gap; both same class as R7d-P0-2). Cold-extent reflink-source + cold-crossing truncate refused with STM_ENOTSUPPORTED (P7-CAS-3 deferred). 13 new test_fs integration tests including the P2-2 race regression; cas.tla unchanged. | `6839cf0` |
| R51 | P7-CAS-3 closes R50 P2-1 + R50 P2-3 + adds cold-extent reflink. (1) Two-part R50 P2-1 closure: (1a) `cas_auto_gc_sweep_locked` moved to BEFORE `stm_alloc_commit` per-device loop in `stm_sync_commit` so Phase 2's `stm_alloc_free` calls produce PENDING(free_gen=target_gen) entries that alloc_commit PERSISTS. (1b) **R51 P1-1 inline fix**: `stm_alloc_load_tree_at` post-deserialize walks the loaded tree for refcount=0 entries and rebuilds `pending_head` (one `pending_entry` per refcount=0 with `free_gen=root_gen`). Without (1b) the unmount loses pending_head and the next mount's alloc_commit sweep walks an empty list — tree-resident PENDING entries leak across mount boundaries. With (1a)+(1b) the next sync_commit at target_gen+2 sweeps PENDING with free_gen<target_gen+2 → reclaims. Crash recovery is now clean. (2) Refactored `cas_auto_gc_sweep_locked` to a 3-phase transactional shape — Phase 1 captures `(hash, paddrs[N], n_paddrs)` tuples into new `cas_sweep_tuple` struct; Phase 2 alloc_frees per paddr with `stm_alloc_lookup` pre-check (refcount=0 → already PENDING from prior partial sweep retry → skip; refcount>1 → STM_ECORRUPT; refcount=1 → free); Phase 3 cas_gcs every captured hash with STM_EBUSY (P2-2 case) + STM_ENOENT skip semantics. Phase 2 failure aborts WITHOUT cas_gc → cas_idx state unchanged → retry safe. (3) `reflink_collect_cb` now accepts COLD extents; `stm_sync_reflink`'s Phase 2 branches on `e->kind` (HOT: `stm_alloc_ref` per replica + `hot_bumped` track; COLD: `stm_cas_ref` per record + `cold_bumped` track); Phase 3 inserts via `stm_extent_reflink` (HOT) or `stm_extent_write_cold` (COLD; inheriting gen / key_id / origin from src so AEAD AD reconstructs identically across siblings); rollback path symmetrically undoes both via alloc_free / cas_deref capped at hot_bumped / cold_bumped. R51 inline fixes: P1-1 (pending_head rebuild on load) + P3-1 (n_paddrs clamp at sweep-tuple capture) + P3-3 (wedge on unknown extent kind in reflink Phase 2). R51 P3-2 + P3-4 forward-noted (scrub-driven CAS GC vs STM_EBUSY skip; FAULTED/REMOVED-device sweep skip). test_fs grows 67 → 70 (replaced `fs_reflink_refuses_cold_source` with `fs_reflink_cold_extent_basic_share` + `_overwrite_diverges` + `_dst_must_be_empty`; added `fs_migrate_to_cold_pending_reclaim_across_mount` for R51 P1-1 regression). cas.tla unchanged (cold-reflink composes via existing `BumpRef` shape). No format break. | `ee25ff6` |
| R52 | P7-CAS-4a crossing-cold truncate. Lifts STM_ENOTSUPPORTED on truncate-across-cold (rec.kind == COLD AND rec.off < new_size < rec.off + rec.len). Pure compositional — no new code path: cold-aware `stm_sync_read_extent_locked` (P7-CAS-2) decrypts the chunk under stm_ad_cas; kept prefix slices into `stm_sync_write_extent_locked` which encrypts under fresh HOT AEAD nonce; the `cold_overlap_cb` pre-scan + post-`stm_cas_deref` bookend (P7-CAS-2) captures the dropped cold extent's content_hash and derefs it after `stm_extent_overwrite` drops the cold record. Net effect: cold crossing extent is replaced by a HOT prefix; CAS refcount drops by one; auto-GC reclaims at next commit if it hits zero. test_fs grows 70 → 72 (replaced `fs_truncate_refuses_cold_crossing` with `fs_truncate_crossing_cold_extent_basic` + `_persists_across_mount` + `_dedup_partial_release`). cas.tla unchanged (composes via RehydrateOnWrite + extent.tla::Write). No format break. | `<TBD>` |

## Phase 6 / clone terms

| Term | Meaning |
|---|---|
| **dataset** | A node in the pool's namespace forest. Each has a unique id, parent_id, name, and metadata (created_txg, flags, next_ino, properties). Root is id=1. ARCH §8.3. |
| **clone** | A dataset with a non-zero `origin_snap_id` referencing a snapshot. Structurally a regular dataset; the back-reference enables `clone.tla::SnapWithClonesUndeletable`. ARCH §8.6. |
| **promote** | Operation that clears a clone's `origin_snap_id` to `STM_DATASET_NO_ORIGIN`. After promote, the dataset is no longer a clone; the previously-referenced snap can be deleted if no other clones reference it. ARCH §8.6.2. |
| **CloneOriginPresent** | clone.tla invariant: every present clone's `origin_snap_id` references a present snapshot. Operationally enforced via the snap-delete cb. |
| **SnapWithClonesUndeletable** | clone.tla invariant: a snapshot with one or more present clones cannot be deleted. Enforced through-stack via `stm_snapshot_index_set_clone_check_cb` registered by sync to query `stm_dataset_clones_count_for_snap`. |
| **NO_ORIGIN / NO_PARENT / NO_PREV** | Sentinel values (all `((uint64_t)0)`) for absent references in dataset / snapshot fields. Disambiguate by FIELD they occupy, not by value. |
| **`ub_main_root` / `ub_snap_root`** | Uberblock bptrs for the dataset / snapshot index trees. Populated post-P6-persist (`348d165`); content kinds `STM_BPTR_KIND_DATASET` (=9) and `STM_BPTR_KIND_SNAP` (=5). |
| **`ub_main_root_gen` / `ub_snap_root_gen`** | AEAD gen trackers (le64) for the corresponding trees, symmetric to `ub_alloc_root_gen`. Required for mount-claim UBs that advance `ub_gen` past orphan writes without rewriting the trees. |
| **dead-list** | Per-snapshot list of blocks that have been COW'd-away from the live dataset since the snapshot was created. On snapshot delete, blocks UNIQUE to the snap (not in successor's dead-list) are freed; SURVIVING blocks (in both S's and successor's dead-list) migrate to predecessor's dead-list. Algorithm is O(blocks COW'd during S's lifetime), not O(tree). ARCH §8.5.5; spec `dead_list.tla`. |
| **next_dead** | Synonym for the per-snapshot dead-list (ZFS terminology). The "next" reflects "next snapshot to be deleted will free or migrate these blocks". |
| **most_recent_snap** | The newest PRESENT snapshot. New COW'd blocks are added to its dead-list. After SnapDelete of the most-recent, recomputed by walking PRESENT snaps. |

## Phase 7 / extent terms

| Term | Meaning |
|---|---|
| **extent record** | The 32-byte per-extent metadata `(paddr, write_gen, dlen, clen+comp, xxh)` that maps a logical file byte range to a physical paddr. Lives in a per-(dataset, ino) Bε-tree keyed by file offset. ARCH §11.6.1; spec `extent.tla`. |
| **extent tree** | The per-inode Bε-tree of `(file_offset → extent_record)` mappings. One per inode per dataset. Keyed by `(ino, type=DATA, offset)`. ARCH §11.6.2. |
| **NoOverlapWithinIno** | extent.tla load-bearing invariant: no two extents in the same `(ds, ino)` cover overlapping byte ranges. A read at any offset resolves to exactly one extent or a hole. |
| **PaddrFreshness** | extent.tla invariant: every extent's paddr is in `used_paddrs`, which grows monotonically in the spec. The C impl (P7-2) narrows this to the LIVE-paddr interpretation: no two CURRENT extents share a paddr; once dropped via Overwrite / Truncate / DeleteFile, a paddr returns to the allocator's reuse pool. Both shapes pin the safety property and compose with allocator.tla::NoReuseInSameGen for end-to-end (paddr, gen)-pair nonce uniqueness. |
| **OverwriteBlock cb (extent → snapshot)** | When extent.tla::Overwrite drops an extent, the C impl calls `stm_snapshot_index_overwrite_block(paddr)` to either route the paddr into the most-recent snap's dead-list or signal the caller to free directly. The composition between extent.tla and dead_list.tla is realized at the C-impl boundary; the production wiring lands with P7-4 (sync.c COW path integration). |
| **stm_extent_index** | The C-side handle for the extent index (P7-2). API in `include/stratum/extent.h`; impl in `src/extent/extent_index.c`. ERRORCHECK mutex with must_lock contract; in-RAM linear-array store. Persistent storage is the P7-3 follow-on. |
| **dropped paddrs** | Output of `stm_extent_overwrite` / `_truncate` / `_delete_file` — a malloc'd array of paddrs whose extents were just removed. Caller owns the array (`free()`) and MUST route each paddr through `stm_snapshot_index_overwrite_block` to compose with dead_list.tla. P7-4's `sync_drop_paddr_locked` is the production routing helper. |
| **sync_drop_paddr_locked** | sync.c helper (P7-4) realizing the C-impl boundary between extent.tla::Overwrite, dead_list.tla::OverwriteBlock, and allocator.tla::Free. For each dropped paddr: route via `stm_snapshot_index_overwrite_block` → if `should_free=true`, call `stm_alloc_free` on the device's allocator (selected via `stm_paddr_device`); else, the paddr stays in the most-recent snapshot's dead-list until that snapshot is deleted. |
| **stm_fs_write / stm_fs_read** | POSIX-shape extent I/O API (P7-4). Thin fs.c wrappers over `stm_sync_write_extent` / `stm_sync_read_extent`. MVP constraints: 4-KiB-aligned + len ≤ 128 KiB + single-extent per call. Encryption uses pool-wide `metadata_key`. |
| **stm_extent_lookup_by_paddr** | Read-path helper (P7-5) — exact-paddr lookup over the live extent records. O(n) linear scan; first match wins by live-paddr `PaddrFreshness`. Returns `STM_ENOENT` for non-base paddrs (mid-extent, metadata, bootstrap, scrub durable region). The production scrub cb is the primary consumer. |
| **production scrub cb** | The bptr-aware β verify-callback shipped in P7-5 via `stm_sync_scrub_install_production_cb`. Resolves each scanned paddr via `stm_extent_lookup_by_paddr`, AEAD-decrypts the matched extent's ciphertext+tag, and returns `STM_SCRUB_VERIFY_OK` on tag-pass / `STM_SCRUB_VERIFY_UNREPAIRABLE` on tag-fail. Mid-extent paddrs + non-extent allocs return OK trivially. **P7-6 promoted the cb from `bptr.tla`'s `NReplicas=1` corner to the full replica-walk + rewrite matrix** (per-replica csum-gate, pick first OK source, rewrite non-OK replicas, verify-back each rewrite, classify OK / REPAIRED / UNREPAIRABLE). |
| **extent replica set** | P7-6 extension to `stm_extent_record`: an extent now carries an array of up to `STM_EXTENT_MAX_REPLICAS=4` paddrs in `paddrs[0..n_replicas)`. The same plaintext is encrypted ONCE under canonical nonce `(paddrs[0], gen)` and the bytewise-identical ciphertext+tag is replicated to every paddr's bdev. `LiveReplicasDisjoint` (extent.tla) requires no paddr appear in two live extents' replica sets; the C impl validates within-set distinctness too. |
| **STM_EXTENT_MAX_REPLICAS** | C constant (=4) capping the on-disk replica-slot count. Mirror-N pools with N ≤ 4 are supported natively; larger N would need a record-format extension (post-v13). The on-disk record reserves all 4 slots regardless of `n_replicas`; trailing slots are sentinel-zero. |
| **STM_RED_MIRROR / mirror_n** | Pool redundancy profile bits (sync.h's `stm_redundancy_profile`). `kind=STM_RED_MIRROR, mirror_n=N` directs the extent write path to allocate N replicas across N distinct devices. P7-6 wires this into `stm_sync_write_extent`'s replica fan-out. RAID-Z (`STM_RED_RS`) and LRC (`STM_RED_LRC`) remain unsupported MVP. |
| **send/recv** | Phase 7 §10.3 dataset replication protocol (P7-7 MVP). `stm_send_init/_next/_close` produces a wire-format byte stream for a source dataset's snapshot range; `stm_recv_init/_apply/_finish/_close` consumes records on the target pool. Wire = HEADER (52B body — magic STMS, version, pool_uuid, dataset_id, snap_ids) + EXTENT* (32B meta + plaintext) + END (32B BLAKE3 csum). Send decrypts source's extents under source pool's `metadata_key`; recv re-encrypts under target's via `stm_sync_write_extent` — fresh paddrs + fresh AEAD key + fresh nonces, no cross-pool nonce reuse hazard. |
| **send wire format magic** | `STM_SEND_MAGIC = 0x534D5453` ("STMS" little-endian) at the start of every HEADER body. Receiver refuses any HEADER record whose first 4 bytes don't match. |
| **send incremental gap (closed in P7-8)** | The P7-7 MVP's snap-bounded gen filter was best-effort because `snapshot.created_txg` and `sync.current_gen` lived in independent counter spaces. P7-8 added an `extent_txg` field on the snapshot record (capturing `sync.current_gen` at SnapshotCreate; format break v13 → v14) and rewrote the send filter to use it. The filter is now authoritative when callers follow the documented operational pattern `commit → snap_create → commit` (analogous to ZFS's TXG bracketing). |
| **extent_txg** | P7-8 field on `stm_snapshot_entry`. Captured from `sync.current_gen` at `stm_snapshot_create` time (under `idx->lock`; R40 P2-1 chain-ordering check rejects stale captures with STM_EINVAL). The send filter's bound is `from.extent_txg < extent.gen <= to.extent_txg` — same counter space as `extent.gen`, so the filter is mathematically authoritative. Distinct from `created_txg` (snap-index counter). |
| **snap-gen alignment** | The P7-8 chunk's headline. Refers to aligning the snap-bounded send filter's gen-counter with `extent.gen` by capturing `sync.current_gen` at SnapshotCreate. The bracketed pattern `commit → snap_create → commit` establishes a strict gen boundary so writes after a snap have gen strictly greater than the snap's `extent_txg`. |
| **ExtentTxgBoundedBySync** | snapshot.tla invariant (P7-8): every snap's captured `snap_extent_txg ≤ sync_gen`. Captured monotonically; sync_gen never decreases. Refutes a buggy impl that stamps a future-dated extent_txg. Buggy demo `snapshot_extent_txg_unbounded_buggy.cfg`. |
| **ChainExtentTxgOrdered** | snapshot.tla invariant (P7-8): along the `snap_prev` chain (filtering ABSENT links), `snap_extent_txg` is non-decreasing — older has ≤ newer. The `≤` (not `<`) tolerates the equality case of two consecutive `SnapshotCreate`s with no intervening `Write`. Refutes a buggy impl that captures stale gen values (e.g., concurrent-create race, R40 P2-1). |
| **truncate partial-extent split (P7-9)** | The production semantic for `stm_sync_truncate`: when `new_size` lands inside an extent (`off < new_size < off+len`), the extent is REPLACED by a shrunk version at `[off, new_size)` re-encrypted under fresh `(paddr_0, current_gen)` AEAD nonce. The original's replicas flow through dead-list / free per the COW path. Pre-P7-9 the extent was left at its original full length (MVP simplification), making bytes past `new_size` readable until a future overwrite. Spec refinement in `extent.tla::Truncate` adds the branch under fresh `replicas \cap used_paddrs = {}`. |
| **per-dataset DEKs (P7-10)** | Every extent now carries a `key_id` field naming which DEK in the dataset's keyschema decrypts it. `stm_sync_write_extent` resolves the dataset's CURRENT `(key_id, DEK)` via `keyschema_lookup_current` + `sync_dek_find`, encrypts under the DEK, and stamps `key_id` on the extent record. `stm_sync_read_extent` resolves DEK by the stamped `key_id` (NOT the dataset's CURRENT) so RETIRED keys still decrypt their original extents. The DEK is NOT in the AEAD nonce or AD; the nonce stays canonical `(paddrs[0], gen, pool_uuid)` so AEAD-tag remains the integrity gate. Pre-P7-10 every extent encrypted under the per-pool `metadata_key` (per-dataset key isolation was a deferred capability of the keyschema layer P4-4c). Format break STM_UB_VERSION 14 → 15. |
| **extent.key_id** | P7-10 field on `stm_extent_record` (in-RAM) and on the on-disk extent value at offset 56 (le64) — repurposes the always-zero `xxh` slot from v13/v14. Stamped by `stm_extent_write` / `_overwrite` / `stm_sync_truncate`. Spec: `extent.tla::ExtentRec.key_id ∈ KeyIds`. |
| **STM_SYNC_ROOT_DATASET_ID** | C constant (=1) for the root dataset's id. `stm_sync_create` auto-installs the root DEK as keyschema entry `(1, 0, CURRENT)` alongside the pool metadata key `(0, 0)` so `stm_sync_write_extent` on `(ds=1, ...)` works out of the box. Non-root datasets need explicit `stm_sync_add_dataset_key` provisioning. |
| **sync_resolve_current_dek_locked (P7-10)** | sync.c private helper that, under sync's lock, looks up a dataset's CURRENT key_id via `stm_keyschema_lookup_current` and copies its DEK from `s->deks[]` into a stack-local 32-byte buffer. Returns STM_ENOENT if the dataset has no CURRENT entry (caller forgot to provision); STM_ECORRUPT if CURRENT exists but the DEK map slot is missing (mount unwrap failure). |
| **sweep refcount enforcement (P7-10)** | `stm_sync_keyschema_sweep` walks the extent index and refuses to prune any RETIRED key with live extent references. Maps to `key_schema.tla::PruneSafety`. Closes the long-standing P4-4c TODO ("Phase 6's extent manager will own the refcount check before invoking this"). The walk runs under sync's lock so the count is consistent with the prune step. |
| **extent_keyids.cfg (P7-10)** | TLC config exercising `MaxKeyIds=2` at the pre-P7-9 bound (MaxFileBlocks=2, MaxPaddrs=4) so two extents under distinct key_ids — modeling pre/post-rotation — coexist. 67304 distinct states; 5s wall. Complements `extent.cfg` which holds `MaxKeyIds=1` to preserve the P7-9 838164-state partial-shrink coverage. |
| **truncate single-lock-span (P7-11)** | `stm_sync_truncate` post-P7-11 holds `sync->lock` across all three phases (lookup → read+re-encrypt → past-extent drop + drop-route) under one acquisition. Closes **R41 P3-1 case (a)** (concurrent `stm_sync_commit` between Phase 2 and Phase 3 splits the on-disk view) and **R41 P3-2** (same gap, scrub-flavored). The case-(b) Phase 3 ENOMEM gap was closed separately in P7-12 via the `stm_extent_truncate_peek` + `_into` pre-allocation pattern. Trade-off is lock-hold duration: decrypt + encrypt + bdev I/O for the crossing extent's prefix all happen under `sync->lock`; cascade-extends scrub-step latency by the verify cb's brief s->lock takes. POSIX truncate-vs-write atomicity at the FS-layer file-handle level remains the caller's responsibility. |
| **truncate fault-free Phase 3 (P7-12)** | `stm_extent_truncate_peek` is a pure-read API that counts past-extents + their total replica paddrs without mutating the index. `stm_extent_truncate_into` truncates using caller-provided pre-allocated buffers and never allocates internally — returns STM_ERANGE atomically (no index mutation) if either buffer cap is insufficient. `stm_sync_truncate` peek-counts past-extents under sync.lock, mallocs both buffers BEFORE Phase 2's overwrite, then runs Phase 3 via `_into` (fault-free for the cap match). Closes **R41 P3-1 case (b)** (Phase 3 ENOMEM after Phase 2 succeeded → partial in-RAM state committable). Composed with P7-11's single-lock-span, the whole truncate is atomic w.r.t. concurrent commit/write AND ENOMEM-safe. |
| **`_locked` variants (P7-11)** | Internal helpers `stm_sync_write_extent_locked` / `stm_sync_read_extent_locked` containing the original public-API bodies minus the lock acquire/release and minus the args+wedged/RO checks. Caller MUST hold `sync->lock` AND have already passed write-side guards. Used by `stm_sync_truncate` to compose under one lock-hold. The public `stm_sync_write_extent` / `_read_extent` are now thin wrappers that validate, lock, call `_locked`, unlock. |
| **stm_fs_create_dataset (P7-13)** | One-step FS-level API that bundles `stm_dataset_create_child` + `stm_sync_add_dataset_key` under `fs->lock`. Removes the long-standing test_fs restriction "only ds=1 (root) writes work without explicit DEK install"; the freshly-created id is immediately usable for `stm_fs_write` / `stm_fs_read`. Atomicity: on add_key failure the freshly-created leaf is rolled back via `stm_dataset_destroy` (infallible-by-spec for a non-root no-children leaf under fs->lock). Wrap-key source is bound to mount-time `keyfile_path` / `janus_socket` retained on the fs handle (R45 P2-1) — ARCH §7.7 frames wrap keys as pool-wide so per-call overrides have no documented use case, and a mismatched per-call source would silently persist an unwrappable CURRENT entry that R42 P1-1's hard-fail policy turns into a permanent mount refusal. The fs-handle binding closes that footgun by construction. |
| **fs handle wrap-key retention (P7-13 / R45 P2-1)** | Post-P7-13, `stm_fs` retains `keyfile_path` / `janus_socket` strdup'd from `stm_fs_mount_opts` (mutually exclusive; freed at unmount). `stm_fs_create_dataset` reuses this source per-call (load → use → wipe / connect → use → disconnect) so wrap-key material is not retained as plaintext in fs RAM but the SOURCE identity is — guarantees subsequent dataset creates use the same wrap pair as every existing keyschema entry. |
| **stm_snapshot_create_for_test (P7-14)** | Test-only API in `<stratum/snapshot_testing.h>` that bypasses ONLY the R40 P2-1 in-process chain-ordering check. All other arg validation runs (NULL args, ds=0, name length, name collision, EOVERFLOW saturation guard). Used to construct the chain-inverted on-disk shape that exercises `sp_validate_shadow`'s `ChainExtentTxgOrdered` rejection at mount-load. Compile-gated behind `STRATUM_BUILD_TESTING_HOOKS`; production builds with the option OFF have neither the prototype nor the symbol in `libstm_snapshot.a`. |
| **STRATUM_BUILD_TESTING_HOOKS (R46 P2-1)** | CMake option (default ON) that gates compile-time inclusion of test-only API hooks that intentionally bypass production-path correctness gates. Distinct from existing test-only seams in `<stratum/fs_testing.h>` and `<stratum/block_inject.h>` which remain ungated per established precedent — those seams expose READ pointers; the hooks gated by this option WRITE corruption-class shapes (currently `stm_snapshot_create_for_test`'s chain-inverted snapshots), so the stricter compile-time gate prevents the bypass symbols from leaking into production binaries even via mistaken include or hand-rolled extern. Set PUBLIC on `stm_snapshot` so the macro propagates from library to test consumers. |
| **on-disk chain-inversion validator (P7-8 / P7-14)** | `sp_validate_shadow` rejects every loaded snapshot chain (per dataset, along the `prev_snap_id` link) where `extent_txg` is not non-decreasing. Producer-side check at `stm_snapshot_create` (R40 P2-1) is the first line of defense; this on-disk check (P7-8) catches buggy producers / tampered disk. P7-14 closes R40 P3-3 by adding the regression test that drives the bad shape onto disk via the gated `_for_test` seam and confirms the next mount returns STM_ECORRUPT. |
| **repair-log entry (P7-15)** | Per-pool audit-trail record emitted by the production scrub β cb on every Phase-3 replica rewrite. 32-byte fixed value layout: `timestamp_ns` (le64, CLOCK_REALTIME) + `target_paddr` (le64, the rewritten replica) + `source_paddr` (le64, the OK replica we sourced from) + `target_replica_idx` (u8) + `source_replica_idx` (u8) + `type` (u8: CSUM_FAIL or IO_ERR — bptr.tla `read_outcome` tag) + `result` (u8: OK_VERIFIED or FAIL — verify-back outcome) + 4 bytes reserved. Keyed by 8-byte big-endian monotonic `seq_id`. Persisted in a single-leaf btnode-encoded plaintext + Merkle-covered tree rooted at `ub_repair_log_root`; append-only (audit-trail semantic). ARCH §7.15.4. |
| **STM_BPTR_KIND_REPAIR_LOG** | Bptr kind = 11 (P7-15). Tags the repair-log tree's root in `ub_repair_log_root`. Distinct from `STM_BPTR_KIND_KEYSCHEMA = 7` despite both being plaintext + Merkle-covered single-leaf btnodes — the kind tag enforces structural type-checking at mount. |
| **STM_REPAIR_LOG_MAX_ENTRIES (R47 P3-1)** | Constant = 2048. Single-leaf MVP cap enforced at `stm_repair_log_index_emit` time: the in-RAM list refuses new emits with STM_ERANGE once at the cap, rather than letting `_commit` fail at sync-flush time and wedging every subsequent sync_commit (data writes, dataset/snapshot mutations, scrub progress all stall). The scrub β cb's emit absorbs STM_ERANGE silently — the rewrite still lands, only the audit-trail entry is lost. Multi-leaf graduation deferred. |
| **LogIntegrity (bptr.tla)** | Spec invariant: every emitted log entry corresponds to a rewrite that actually landed and carries the picked source's index. Pinned by `bptr.tla::LogIntegrity`. C-side coverage: `stm_repair_log_index_emit` validates `target_replica_idx ≠ source_replica_idx` (else STM_EINVAL) at the API boundary; the scrub cb emits exactly one entry per Phase-3 rewrite (success and failure paths). |
| **repair_log Merkle inclusion (R47 P2-1)** | `compute_merkle_root` chains `repair_log_csum` as its 7th input (after `extent_csum`, before `salt`). Closes the asymmetry where keyschema (also plaintext + Merkle-covered) was Merkle-bound but repair_log was not — an offline tamper of the audit trail now requires recomputing the Merkle root, which fires at the mount-time recompute. v16 is unreleased outside this branch, so the Merkle change folds into v16 rather than bumping to v17. |
| **reflink (P7-16)** | POSIX-shape `ioctl(FD, FICLONE, ...)` semantic: O(extent count) copy that shares underlying ciphertext until either side is COW'd. ARCH §11.12. Implemented via the new `stm_fs_reflink(fs, src_ds, src_ino, dst_ds, dst_ino)` which, under fs->lock, delegates to `stm_sync_reflink` — collects src extents via iter, bumps `stm_alloc_ref` on each replica paddr (refcount → N+1), inserts reflinked records via `stm_extent_reflink` with origin INHERITED from src. dst_ino MUST be empty at v1 MVP; cross-dataset deferred per ARCH §11.12.3 (refused with STM_EXDEV). |
| **origin (extent record field, P7-16)** | The (origin_dataset_id, origin_ino, origin_off) triple stamped on every extent record. Names the (ds, ino, off) at which the AEAD ciphertext was first encrypted. AD reconstruction at read / scrub / send sources from origin (rather than live ds/ino/off) so reflink-siblings sharing the same paddrs reconstruct the SAME AEAD AD and AEGIS-256 verify succeeds across the share. For freshly-written extents origin equals the live (ds, ino, off); for reflinked extents origin is inherited from src — invariant via extent.tla::SharedReplicasAreCohabit. Tampering origin requires defeating the metadata-tree (btnode) AEAD; field is no weaker than the live identity was for non-reflinked extents. |
| **SharedReplicasAreCohabit** | extent.tla invariant (P7-16; replaces the prior `LiveReplicasDisjoint`). Two distinct extent records sharing ANY replica paddr MUST share the WHOLE replica set AND the same gen, key_id, AND origin_* fields — i.e., be legitimate reflink-siblings. Partial overlap or whole-share-with-mismatched-tuple is forbidden. Enforced at write time by `stm_extent_reflink`'s `cohabit_check_locked`, and at mount-load time by `ex_validate_shadow`. Buggy demo `reflink_rotates_origin_buggy.cfg` flips the cfg constant `BuggyReflinkRotatesOrigin = TRUE` (Reflink mutates origin to dst's live identity instead of inheriting from src) and fires at depth 4 / 595 states. |
| **OriginConsistentInBounds** | extent.tla invariant (P7-16): every extent's `origin_off + len ≤ MaxFileBlocks`. Pins on-disk origin field's typing — origin must address a byte range that fits in the origin file. Closed by every action since origin is either current (Write/Overwrite/Truncate) or inherited from a live src extent (Reflink). |
| **stm_extent_reflink (P7-16)** | Extent-layer record-insertion API for the reflink path. Refuses ds/ino/origin sentinels, len=0, invalid replica set, dst-overlap, BirthTxgBound violation, and SharedReplicasAreCohabit violation (partial overlap OR whole-share-with-mismatched-tuple). Caller MUST have already bumped allocator refcounts on each paddr. `cohabit_check_locked` does the cross-record validation under idx->lock. |
| **stm_sync_reflink (P7-16)** | Sync-layer composition for whole-file reflink. 3-phase under sync->lock: (1) collect src extents via `stm_extent_iter_ds` + `reflink_collect_cb`; (2) bump `stm_alloc_ref` on every replica paddr in every src extent (per-paddr device routing via `stm_paddr_device`); (3) insert reflinked records via `stm_extent_reflink` with origin = src.origin. On Phase 2 partial-fail rolls back the first `bumped` refs. On Phase 3 partial-fail uses `stm_extent_delete_file` to drop dst_ino's records + `stm_alloc_free` each + walks the collect snapshot from the failing index forward to undo refs for inserts that never landed. Refuses STM_EWEDGED / STM_EROFS / STM_EXDEV (cross-dataset) / STM_EEXIST (dst_ino non-empty) / STM_EINVAL (self-reflink). |
| **stm_fs_reflink (P7-16)** | Public POSIX-shape FICLONE API at the FS layer. Holds fs->lock + delegates to `stm_sync_reflink`. Errors propagate from sync layer; FS_GUARD_WRITE (RO/wedged refused). |
| **STM_EXDEV (P7-16)** | New error code (-18, matches POSIX EXDEV). Returned by `stm_sync_reflink` / `stm_fs_reflink` when src and dst datasets differ — cross-dataset reflinks deferred to a future chunk per ARCH §11.12.3 (requires same-encryption-key check between the two datasets). |
| **DisableReflink (cfg constant, P7-16)** | Boolean cfg toggle on `extent.tla` Next: when TRUE, the Reflink action is excluded so the spec exercises only Write/Overwrite/Truncate/DeleteFile/AdvanceTxg. Used in `extent.cfg` to keep the bumped bounds (MaxFileBlocks=3, MaxPaddrs=5; 838,164 states / ~1m wall) tractable for the P7-9 partial-shrink coverage; `extent_keyids.cfg` flips it to FALSE and exercises Reflink at smaller bounds (MaxDatasets=2 lets Reflink fire). All 5 prior buggy cfgs were also updated with `DisableReflink=TRUE` (their bug-firing depths are reachable without Reflink). |
| **link_gen (extent record field, R48 P0-1)** | The gen at which an extent record was inserted into the LIVE extent index. Distinct from the AEAD `gen` field (the gen at which the underlying ciphertext was first encrypted, inherited from src on Reflink). For Write / Overwrite / Truncate's shrunk crossing extent, `link_gen == write_gen` (no semantic shift from pre-R48). For Reflink, `link_gen` is freshly stamped at reflink-time (NOT inherited from src). On-disk: 8 bytes le64 at extent value offset 88..95 (uses the prior 8 reserved bytes carved in v17). Format-fold within v17, no STM_UB_VERSION bump. The send/recv pipeline's incremental gen filter uses `link_gen` (not `gen`) so reflinked records appearing in window (S_from, S_to] are emitted even when their AEAD `gen` predates `S_from.extent_txg`. Closes silent-data-loss on backup-via-send-recv across reflink-touched datasets. |
| **LinkGenBoundedByTxg** | extent.tla invariant (R48 P0-1): every extent's `link_gen ≤ current_txg`. Bounded by the same monotonic txg as `gen`. Closed by every record-inserting action — Write / Overwrite / Truncate / Reflink all stamp `link_gen = current_txg`. |
| **refcount-aware sync_drop_paddr_locked (R48 P1-1)** | The C-impl's `sync_drop_paddr_locked` consults the alloc refcount BEFORE consulting `snap_idx`. If refcount > 1 (the paddr is reflink-shared with another live extent), just `stm_alloc_free` (DecRef) without dead-list capture — the sibling keeps the paddr alive at the allocator level. If refcount == 1 (last live ref), fall through to the pre-P7-16 routing (snap-with-most-recent → dead_list captures, no-snap → alloc_free directly). Without this, reflink + snap + dual-side-Overwrite hits dead_list.tla's R33 P2 single-ownership defense (which scans all PRESENT snaps' dead-lists for duplicates), refusing the second drop with STM_EINVAL while the first already added the shared paddr to S.dead_list — paddr leak. The C-side relaxation matches extent.tla::SharedReplicasAreCohabit; dead_list.tla single-ownership at the spec level remains as a future refinement. |
| **CAS tier (P7-CAS / NOVEL #3)** | Content-addressed cold-tier blob store. A Bε-tree keyed by `BLAKE3-256(content)` with per-entry `(replicas, refcount, length, gen)`. Multiple cold extents (across files, snapshots, datasets) can reference the same hash — that is the dedup property. Spec `cas.tla`. C impl `src/cas/cas_index.c`. Format break STM_UB_VERSION 17 → 18 (P7-CAS): the tree root lives at `ub_cas_index_root` (offset 288, carved at v3 but unused until now); the gen tracker `ub_cas_index_root_gen` (le64) carved from head of `ub_reserved` at offset 3280. |
| **extent_kind (P7-CAS, byte 0 of extent value)** | v18 1-byte discriminator at on-disk extent value offset 0. `0x01 = HOT` (paddr-addressed; v17 layout for bytes 1..95, with n_replicas shifted from byte 0 to byte 1). `0x02 = COLD` (hash-addressed; bytes 1..7 reserved zero, bytes 8..39 carry BLAKE3-256 chunk hash, bytes 40..95 same as HOT — gen, dlen, clen_and_comp, key_id, origin, link_gen). v17 had no kind byte; v18 mounts of v17 pools rely on the SB version check rejecting first. |
| **STM_EXTENT_KIND_HOT / STM_EXTENT_KIND_COLD** | C constants (=1, =2) for the v18 extent record's `kind` field. HOT extents carry `paddrs[0..n_replicas)` (replicas hold the AEAD ciphertext); COLD extents carry `content_hash[32]` naming a CAS index entry whose replicas hold the chunk's ciphertext under CAS AD. |
| **stm_cas_index (P7-CAS)** | C-side handle for the CAS-tier index. API in `include/stratum/cas.h`; impl in `src/cas/cas_index.c`. ERRORCHECK mutex with must_lock contract; in-RAM linear-array shadow + persistent btree_store-backed tree on device 0. Mutations: `stm_cas_insert` (CAS-miss branch of MigrateToCold), `stm_cas_ref` (dedup-hit refcount bump), `stm_cas_deref` (RehydrateOnWrite / DeleteFile per-hash decrement), `stm_cas_gc` (remove zero-refcount entry). Lookup + iter + count + persistence (set_storage / set_crypt_ctx / load_at / commit / get_root / get_gen / verify) mirror `stm_extent_index` shape. |
| **stm_cas_record** | In-RAM CAS entry shape: `content_hash[32]`, `n_replicas`, `paddrs[STM_CAS_MAX_REPLICAS=4]`, `refcount` (u32), `length` (u64 plaintext bytes), `gen` (le64 — AEAD nonce gen of the underlying chunk; PINNED across dedup-hit refcount bumps). |
| **stm_ad_cas (ARCH §7.6.3)** | 56-byte AEAD AD struct for CAS chunks: `magic` (=0x45534143 'CASE') + `version` (=1) + `pool_uuid[2]` + `content_hash[32]`. Binds ciphertext to its content hash — an attacker can't substitute one chunk for another without breaking AEAD. CAS chunks share storage across files/datasets/snapshots, so AD CANNOT include (ds, ino, off) — it must be a function of content alone. |
| **STM_BPTR_KIND_CAS** | Bptr kind = 6 (carved at v3; unused until P7-CAS / v18). Tags the CAS index tree's root in `ub_cas_index_root`. |
| **STM_CAS_HASH_LEN / STM_CAS_MAX_REPLICAS** | C constants (=32 = BLAKE3-256 byte length; =4 = matching `STM_EXTENT_MAX_REPLICAS` for mirror-N pools). |
| **CASIndexUnique** | cas.tla invariant: at most one CAS entry per hash. Pinned by `stm_cas_insert` refusing duplicate hash with STM_EEXIST (caller must use `stm_cas_ref` instead) and by every other action's update path preserving the at-most-one shape. |
| **RefcountConsistent** | cas.tla load-bearing invariant: for every live CAS entry, `refcount` == count of cold extents naming this hash. The CAS-tier dedup property's correctness axis. Enforced operationally by callers calling `stm_cas_ref` on every dedup-hit MigrateToCold and `stm_cas_deref` on every RehydrateOnWrite / DeleteFile. Buggy demos `cas_migrate_forgets_refbump_buggy.cfg`, `cas_rehydrate_no_deref_buggy.cfg`, `cas_delete_forgets_deref_buggy.cfg` each fire this. |
| **NoDanglingColdRef** | cas.tla invariant: every cold extent's hash names a live CAS entry. Buggy demo `cas_gc_race_buggy.cfg` (GC removes a still-referenced entry) fires this. |
| **HotColdReplicasDisjoint** | cas.tla invariant: a hot extent's replicas never collide with any CAS entry's replicas. AEAD ADs differ between hot (stm_ad_extent: ds + ino + off + gen + key_id) and cold (stm_ad_cas: pool + content_hash); a paddr storing both kinds of ciphertext is impossible. Buggy demo `cas_migrate_reuses_hot_paddr_buggy.cfg` (CAS-miss reuses hot paddrs as CAS replicas without re-encrypting on fresh paddrs) fires this. |
| **CASReplicasDisjoint** | cas.tla invariant: distinct CAS entries reference distinct paddrs. Each chunk is independently AEAD-encrypted under its own `(paddr, gen)` nonce. Operationally enforced by `stm_cas_insert`'s cross-set paddr scan. |
| **stm_extent_migrate_to_cold (P7-CAS-2)** | Atomic per-extent hot→cold swap at the extent_idx layer. Drops the existing live HOT extent at exactly (ds, ino, off, len) AND inserts a COLD extent at the same coordinates referencing `content_hash` — in one critical section so NoOverlapWithinIno is preserved across the transition. Returns the dropped HOT replica paddrs in a caller-owned cap-4 array for routing through `sync_drop_paddr_locked`. Refuses STM_ENOENT if no extent at (ds, ino, off); STM_EINVAL if the matching extent's len differs OR is COLD already (caller bug). The in-place swap pattern (overwrite the matching slot rather than two-step compact + append) sidesteps the gap where a concurrent inserter could land in the (ds, ino, [off, off+len)) range between drop and insert. |
| **stm_sync_migrate_to_cold (P7-CAS-2)** | sync-layer migration driver. Walks every HOT extent at (ds, ino) into a snapshot, then per-extent: read+decrypt → BLAKE3-256 hash → CAS lookup-or-insert → atomic hot→cold swap → drop-route. Holds s->lock across the whole pipeline; per-extent atomicity (already-migrated extents stay migrated on a partial-failure retry). Allocator-fresh paddrs in the CAS-miss branch close R49 P2-1 (HotColdReplicasDisjoint enforced at the migration-layer caller via `stm_alloc_reserve` rather than cross-index runtime check). MVP: extent-granularity hashing (one BLAKE3 per HOT extent → one CAS chunk); FastCDC sub-chunking is a P7-CAS-3+ refinement. |
| **stm_fs_migrate_to_cold (P7-CAS-2)** | Public FS-layer wrapper. Holds fs->lock + FS_GUARD_WRITE across the inner `stm_sync_migrate_to_cold`. POSIX-shape "make this file cold" intent. |
| **rehydrate-on-write (P7-CAS-2)** | Implemented via cold-overlap pre-scan + post-deref bookend in `stm_sync_write_extent_locked`. Before `stm_extent_overwrite` drops the overlap set, we capture each COLD extent's `content_hash` via `stm_extent_iter` + `cold_overlap_cb`. After overwrite, we `stm_cas_deref` each captured hash. Equivalent per-overlap-set bookend in `stm_sync_truncate`'s past-extent drop loop. Realizes cas.tla::RehydrateOnWrite (cold extent replaced with a fresh hot extent + per-hash refcount decrement). Crossing-COLD truncate refused with STM_ENOTSUPPORTED in MVP. |
| **COLD-aware read (P7-CAS-2)** | New branch in `stm_sync_read_extent_locked`: when `rec.kind == STM_EXTENT_KIND_COLD`, resolves rec.content_hash → CAS entry via `stm_cas_lookup`, AEAD-decrypts the chunk's ciphertext under stm_ad_cas + pool metadata_key, slices the requested `len` bytes into the caller's buffer. Each replica is tried in order; first AEAD-verifying replica wins. CAS chunks use the pool metadata_key (not a per-dataset DEK) per ARCH §7.6.3 so they're cross-dataset shareable. |
| **cas_auto_gc_sweep_locked (P7-CAS-2)** | sync_commit phase that walks the in-RAM CAS index for refcount=0 entries (via `stm_cas_iter` + `cas_capture_zero_cb`) and per-hash calls `stm_cas_gc` + routes returned paddrs through `stm_alloc_free`. Runs BEFORE `stm_cas_index_commit` so the persisted btree reflects post-GC state. Closes R49 P2-2 forward-note about orphan paddrs on unmount-after-deref (the cas_index commit already excluded refcount=0 entries from the on-disk btree; the auto-GC adds the missing piece — paddrs routed to allocator's free path). |
| **stm_ad_cas (ARCH §7.6.3)** | AEAD AD struct for CAS chunks (56 bytes packed: magic 'CASE' + version + pool_uuid[2] + content_hash[32]). Binds ciphertext to its content hash — an attacker cannot substitute a chunk for another without breaking AEAD. CAS AD deliberately omits dataset_id (chunks are cross-dataset shareable). Encrypted under pool metadata_key with nonce (paddrs[0], gen, pool_uuid). |

## Policy terms

| Term | Meaning |
|---|---|
| **audit-triggering change** | A change to one of the surfaces listed in CLAUDE.md requires spawning an adversarial soundness audit before merge. |
| **spec-first policy** | Any feature touching a load-bearing invariant models in TLA+ FIRST; code implements against the verified spec. |
| **closed list** | File-per-round in user memory tracking P0/P1/P2/P3 findings that were resolved. Auditors consult these to avoid re-reporting. |
| **idempotent commit** | A commit pattern where a clean-state commit returns the previous (paddr, csum) without writing new bytes. Preserves quorum's `ContentQuorumAtGen` across retries. |
| **burned UUID** | A device UUID that was once in the pool roster and is now REMOVED. Cannot be re-added — would collide with historical AEAD nonces written under that device's metadata_key. |
