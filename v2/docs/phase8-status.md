# Phase 8 — status and pickup guide

Authoritative pickup guide for Phase 8 (POSIX surface — inodes,
dirents, xattr, ACLs, statx, small-file inline, reflink wrapper,
the full set of POSIX file/dir operations).

**Phase 8 ENTERED 2026-04-30** at `bea7f82` after the user
surfaced that ARCHITECTURE §11 (POSIX surface) was a missing
phase under the prior 10-phase ROADMAP numbering. Phase 8 was
inserted; client interfaces shifted to Phase 9. Tag
`phase-7-complete` (at `1759caf`) still marks the Phase 7 → 8
boundary.

ROADMAP §11 lists the deliverables and §11.2 the exit criteria.
ARCHITECTURE §11 is the design document — the on-disk shape is
fully committed there (256-byte `stm_inode`, hash-indexed B-tree
dirents keyed by `(dir_ino, STM_KEY_DIRENT, fnv1a(name))`, tagged
data union extent / inline / symlink / device, `si_gen` for
ino-reuse detection, inline data ≤100 bytes for small files).
This phase implements §11.

## Why this phase exists

Phase 7 built the data plane (extent records keyed by `(ds, ino,
off, len)`, FastCDC, CAS tier, send/recv, reflinks) and the
namespace plane (datasets, snapshots, clones, dead-list). What's
missing is the **filesystem semantics layer** — the inode metadata
and directory entry storage that turns a (dataset, ino, byte-
offset) byte store into a POSIX-shape file/directory tree:

- `stm_fs_lookup(parent_ino, name) → child_ino` — name resolution
- `stm_fs_create_file` / `stm_fs_mkdir` / `stm_fs_unlink` /
  `stm_fs_rmdir` / `stm_fs_rename` — directory mutations
- `stm_fs_stat` / `stm_fs_chmod` / `stm_fs_chown` / `stm_fs_utimens`
  — metadata ops
- `stm_fs_readdir` — directory traversal
- `stm_fs_link` / `stm_fs_symlink` / `stm_fs_readlink` — links
- xattr + POSIX ACLs
- modern POSIX features (`O_TMPFILE`, `F_SEAL_*`, `statx`)

v1's `src/fs/fs.c` had this layer; v2 has a flat (ds, ino, off)
extent API. Phase 8's job is the v2 reimplementation per ARCH
§11 — extending the prior 88-byte v1 inode to the 256-byte v2
inode and porting the hash-indexed dirent shape.

Phase 9 (client interfaces) is gated on Phase 8 exit: 9P's Twalk
/ Tcreate / Treaddir / Topen / Tstat have nothing to forward to
without this layer.

## Sub-chunk plan

Each sub-chunk is its own substantive + R-close + hash-fixup
cycle (per CLAUDE.md three-commit pattern) where the audit-trigger
surface applies. New surfaces added to CLAUDE.md's trigger list
during this phase:

- `src/inode/` — inode tree write paths.
- `src/dirent/` — dirent btree mutations + hash-collision chain
  walks (single-byte invariant violations here are how
  ext4 + xfs have historically had silent-corruption regressions).
- `src/fs/fs.c` for any new public POSIX op API additions.

| Sub-chunk | Scope | TLA+ scaffold | Audit trigger |
|---|---|---|---|
| **P8-POSIX-1** *(spec scaffold + substantive landed; R69 audit running)* | `stm_inode` struct + in-memory allocator (alloc-fresh-only). New `include/stratum/inode.h` (256-byte struct stm_inode_value per ARCH §11.3, with v2 simplification: 16-byte (paddr, gen) extent-tree-root vs ARCH's 57-byte stm_bptr). New `src/inode/inode.c` (allocator with pthread_mutex_t-guarded records[] + per-dataset next_ino[]). 19 tests in test_inode.c green default + ASan + TSan. AllocReused (with gen bump per inode.tla) deferred to P8-POSIX-1b. | **`inode.tla` LANDED** — alloc / free / generation invariants; healthy 879025 distinct states / depth 25; two buggy variants (BuggyReuseNoGenBump, BuggyDoubleAllocate) fire as designed. | new surface (`src/inode/inode.c` + `include/stratum/inode.h` joined CLAUDE.md trigger list at substantive landing) |
| **P8-POSIX-1b** *(closed; R70 + R71 + R71b audits run + applied)* | Per-pool inode tree persistence backed by btree_store (mirrors `stm_extent_index`'s load_at + commit shape). Format break **STM_UB_VERSION 23 → 24** (new `ub_inode_root` + `ub_inode_root_gen` fields in per-device payload; `ub_reserved` 776 → 704). AllocReused path with gen bump per `inode.tla::AllocReused`; FREED state encoded inline via `STM_INO_FLAG_FREED` in `si_flags`. **R70 verdict: 1 P0 + 0 P1 + 2 P2 + 8 P3** — all addressed inline. **R70 P0-1: `inode_csum` folded into the Merkle chain** (closes the same shape as R47 P2-1's repair_log_csum binding — without it an offline-write attacker could swap the inode tree by recomputing `ub_csum` alone). **R71 verdict on the R70 close: 0 P0 + 1 P1 + 2 P2 + 10 P3** — all P0/P1/P2 applied inline. **R71 P1-1: `stm_inode_set` rejects nlink=0 on ALLOCATED records** (closes the silent-commit-then-wedge surface left by R70's asymmetric decoder-only FREED ⇔ nlink invariant — the writer-side guard now mirrors the decoder's check). R71 P2-1 (latch comment split) + P2-2 (get_root docstring drift) fixed. R71 P3-1 through P3-10 forward-noted (mostly regression-test coverage gaps for invariants pinned at audit time). **R71b verdict on the R69-close gap (`6ebf295` retroactive): 0 P0 + 0 P1 + 0 P2 + 2 P3** — clean. R71b P3-1 (cap-doubling guard tightened to bound the `* sizeof` multiplication in `get_or_create_dsstate` + `append_record`) + P3-2 (state-preservation-after-rejection assertion in `inode_set_refuses_unknown_data_kind`) applied inline. test_inode 32 → 36; test_sync 24 → 25. | composes over `inode.tla` (no spec extension required) | inode (existing surface) + sync.c (commit hookup) + uberblock layout (format break) + Merkle root chain (R70 P0-1) |
| **P8-POSIX-2** *(closed; R72 audit run + applied)* | Dirent layer. Per-pool hash-indexed B-tree per ARCH §11.4: key `(le64 dataset_id || le64 dir_ino || le64 hash_probe)` where `hash_probe = fnv1a64(name) + probe_offset`. Probe-chain (cap 64) for collisions per ARCH §11.4.2. Format break **STM_UB_VERSION 24 → 25** (new `ub_dirent_root` + `ub_dirent_root_gen` fields; `ub_reserved` 704 → 632; new `STM_BPTR_KIND_DIRENT_TREE = 13`). Merkle binding extended to 9th input (`dirent_csum`) in lockstep with the field introduction (R70 P0-1 lesson). Open-addressing primitives `stm_dirent_alloc` / `_lookup` / `_unlink` / `_count_for_dir` per dirent.tla; tombstones encoded inline via `STM_DIRENT_FLAG_TOMBSTONE`. Writer-side guards symmetric with decoder-side guards (R71 P1-1 lesson) — alloc rejects every shape the decoder rejects (zero ino / zero name_len / invalid child_type / oversized name). **R72 verdict: 0 P0 + 0 P1 + 1 P2 + 5 P3** — clean; disciplines from R70/R71/R71b transferred correctly. R72 P2-1 (sync-level integration coverage gap, analog of R70 P2-1 for inode) closed inline by `sync_dirent_persistence_roundtrip` test in test_sync.c covering set_storage / set_crypt_ctx / load_at order / bp_kind check / csum mirror / build_uberblock with the dirent triple / 9-input Merkle binding across both 1-phase fresh-pool commit + 2-phase across-mount commit, plus tombstone-slot-reuse post-load (R72 P3-2 folded in). R72 P3-1 (forced-FNV-collision regression test), P3-3 (chain-exhaustion STM_ENOSPC coverage), P3-4 (uint64-wrap cosmetic), P3-5 (byte-layout pin) forward-noted into P8-POSIX-2b's audit. 15 tests in test_dirent.c + sync_dirent_persistence_roundtrip in test_sync.c green default + ASan + TSan. Higher-level fs APIs (stm_fs_lookup / _create_file / _mkdir / _unlink / _rmdir) deferred to P8-POSIX-2b — they compose dirent + inode at the per-fs lock layer. | **`dirent.tla` LANDED** — open-addressing chain integrity (581 healthy states / depth 7); 3 buggy variants `BuggyUnlinkUsesEmpty` (82 states), `BuggyCreateOverwritesNoProbe` (14 states), `BuggyLookupStopsOnTombstone` (84 states) all fire as designed. **No spec extension required** for persistence (composes via `btree_store`). | new surface (`src/dirent/` + `include/stratum/dirent.h` joined CLAUDE.md trigger list at substantive landing) + sync.c (commit hookup + Merkle binding) + uberblock layout (format break v24 → v25) + Merkle root chain (9th csum) |
| **P8-POSIX-2b** *(closed; R73 audit run + applied)* | fs.c POSIX wrappers composing dirent + inode at the per-fs lock layer: `stm_fs_lookup` / `_create_file` / `_mkdir` / `_unlink` / `_rmdir`. Lock chain: `fs->lock → inode_idx mutex / dirent_idx mutex` (siblings, no cross-locking). Atomicity: create-paths roll back the freshly-allocated inode if dirent-link fails (no orphans on failure). MVP single-link semantics — unlink frees the inode unconditionally; hard links + nlink decrement defer to P8-POSIX-3 (`P8-POSIX-3-TODO:` markers added inline at the cascade-free site for grep-discoverability). POSIX name validation: 1..255 bytes, no NUL/'/'; "." and ".." reserved. New error codes `STM_ENOTDIR / STM_EISDIR / STM_ENOTEMPTY` (POSIX-aligned values). **R73 verdict: 0 P0 + 0 P1 + 1 P2 + 7 P3** — composition is sound. R73 P2-1 (rmdir leaks orphan tombstones at freed dir_ino) closed inline by new `stm_dirent_drop_for_dir` primitive called from rmdir before inode_free; P3-5 (rmdir-with-tombstones test) folded into the same close as `fs_p2b_r73_p2_1_rmdir_cleans_orphan_tombstones`; P3-6 (P8-POSIX-3-TODO comment) applied. R73 P3-1 (forced FNV-collision test for tombstone-walk), P3-2 (uint8_t > 255u dead-code), P3-3 + P3-4 (R72 forward-noted concerns still uncovered: forced collision + chain exhaustion at wrapper layer), P3-7 (per-API arg-validation matrix coverage gap) all forward-noted into P8-POSIX-3's audit close. test_dirent 15 → 17 (drop_for_dir tests); test_fs 170 → 171. | composes over `inode.tla` + `dirent.tla` (no spec extension required for MVP single-link semantics; nlink + hard-link atomicity invariants land at P8-POSIX-3) | composes 2 trigger surfaces: inode allocator + dirent layer + Public API contracts (5 new fs.h decls + 1 new dirent.h primitive) |
| **P8-POSIX-3** *(closed; R74 audit run + applied)* | Metadata ops + nlink + hard links. New fs APIs: `stm_fs_stat` (returns full 256-byte stm_inode_value), `stm_fs_chmod` (preserves S_IFMT bits; rejects type-mismatch), `stm_fs_chown` (UINT32_MAX = leave-unchanged, POSIX `chown(-1)` semantics), `stm_fs_utimens` (atime + mtime + caller-supplied ctime; nsec validated < 1e9), `stm_fs_link` (same-dataset hard link; signature precludes cross-dataset; hard-link-on-directory refused with STM_ENOTSUPPORTED per POSIX). New inode-layer APIs: `stm_inode_link` (nlink++) + `stm_inode_unlink` (nlink--; cascade-free atomically when nlink reaches 0 — models inode.tla::Unlink). The MVP single-link unconditional `stm_inode_free` from P8-POSIX-2b's unlink/rmdir wrappers has been replaced by `stm_inode_unlink`'s cascade-free path. **R74 verdict: 0 P0 + 0 P1 + 0 P2 + 4 P3** — clean. R74 P3-1 (cross-dataset STM_EXDEV unreachable in single-`dataset_id` signature) + R74 P3-2 (STM_EPERM ↛ STM_ENOTSUPPORTED docstring drift) closed inline via docstring tightening; R74 P3-3 (rollback failure swallowed in stm_fs_link's (void)-cast — defense-in-depth gap, theoretical only under fs->lock) annotated inline with future-mark-wedged note; R74 P3-4 (utimens 0/0 epoch sentinel collision — placeholder until P8-POSIX-7 statx clock integration) forward-noted. test_fs 171 → 178; test_inode 36 → 40. | **`inode.tla` EXTENDED** — adds `nlink` shadow var + Link/Unlink actions + `LinkedAllocatedHasPositiveNlink` + `FreedHasZeroNlink` invariants. New `BuggyUnlinkLeavesZeroNlink` config (fires LinkedAllocatedHasPositiveNlink). Healthy 9.87M states / 2.96M distinct (Inos=3, MaxGen=3, MaxNlink=2). | inode (allocator + new link/unlink) + fs (5 new public APIs) |
| **P8-POSIX-4** *(closed; R75 audit run + applied)* | `stm_fs_readdir` (with `STM_FS_READDIR_FLAG_NO_DOTS` flag for callers who manage `.`/`..` themselves) + low-level `stm_dirent_readdir`. Opaque `uint64_t` cursor combines synth phase (0=`.`, 1=`..`) with stored-dirent scan (≥2 in dirent-cursor space). Stored entries returned in `hash_probe`-ascending order; tombstones skipped (per `dirent.tla::ReaddirNoTombstoneEmitted`); cursor strictly advances past every emitted slot (per `dirent.tla::ReaddirNoDuplicateProbeInLog` + `ReaddirCursorMonotonicEmits`); cursor never re-emits the same probe within an iteration even under inter-call Create/Unlink. fs-layer wrapper validates `dir_ino` is `S_IFDIR` (else `STM_ENOTDIR`); rejects unknown flag bits (forward-compat). Synthesized `.` has `child_ino=dir_ino`, ".." has `child_ino=parent_ino` (caller-provided; for root dataset, pass `parent_ino=dir_ino` per POSIX root convention). `child_gen=0` on synth entries (synth never goes stale). MVP perf: `stm_dirent_readdir` does an O(N) scan + O(N log N) qsort per call (N = total records under (ds, dir_ino)); future top-K-heap optimization deferred until xfstests stresses 1M-entry dirs (ARCH §11.4.3 perf concern). **R75 verdict: 0 P0 + 0 P1 + 1 P2 + 4 P3** — YELLOW; all addressed inline. **R75 P2-1: cursor saturation produces infinite-emit loop on `hash_probe == UINT64_MAX`** — strict-less-than filter `r->hash_probe < UINT64_MAX` is false at probe=UINT64_MAX, so a record at that probe gets re-emitted forever after the cursor advance saturates. Adversary effort: ~2^58 FNV-1a pre-image per matching probe offset (feasible against shared-tenancy filesystems with hostile name selection). Fixed by short-circuit on `*cursor == UINT64_MAX` at entry to both `stm_dirent_readdir` and `stm_fs_readdir`; UINT64_MAX is now formally a "iteration done" sentinel. R75 P3-1 (out-param zero-init order drift from R57 P3-5 / R58 P3-1 norm) closed by moving `*out_returned = 0` BEFORE arg validation. R75 P3-2 (doc/impl mismatch on saturation) closed by tightening dirent.h + fs.h docs. R75 P3-3 (RO-mount positive test gap) closed by `fs_p4_readdir_r75_p3_3_ro_mount_succeeds`. R75 P3-4 (defense-in-depth name_len bound at fs.c → dirent.c trust boundary) closed by adding STM_ECORRUPT-on-oversize check in fs.c emit loop (R71 P1-1 lesson — symmetric guards across trust boundaries). No format break (no on-disk surface change). test_dirent 17 → 28 (9 base readdir tests + 2 R75 regressions: cursor-saturation-terminates + out-param-zero-init-on-einval). test_fs 178 → 189 (8 base fs-level + 3 R75 regressions: cursor-saturation-terminates + zero-init-on-einval + ro-mount-succeeds). | **`dirent.tla` EXTENDED** — adds `iter` (cursor) + `emit_log` (sequence) + `iter_active` (flag) state + 3 readdir actions (`ReaddirReset`, `ReaddirStep`, `ReaddirEnd`) + 4 readdir invariants (`ReaddirNoTombstoneEmitted`, `ReaddirNoDuplicateProbeInLog`, `ReaddirCursorMonotonicEmits`, `ReaddirCompleteAtEnd`). 3 new buggy cfgs: `BuggyReaddirIncludesTombstones` (547 states), `BuggyReaddirNoCursorAdvance` (548 states), `BuggyReaddirSkipsLiveRecord` (1113 states) all fire as designed. Stable-iteration model: Create/Unlink gated on `~iter_active[d]` (mutations only between iterations). Healthy 241,017 distinct states / depth 17 / 11s. | dirent (existing) + fs (existing) — both already on CLAUDE.md trigger list |
| **P8-POSIX-5** *(closed; R76 audit run + applied)* | Inline data optimization. fs.c::stm_fs_write/read now dispatch on `(inode exists, mode & S_IFMT == S_IFREG, si_data_kind)`: regular-file writes go through `fs_write_regular_locked` which keeps small files inline (≤ STM_INODE_INLINE_MAX = 100 B) in `si_inline_data` (no extent allocation), and triggers a one-way inline → extent transition when a write would grow the file past the inline cap. Transition path builds a single block-aligned combined buffer (existing inline prefix + user bytes overlaid + zero pad up to next 4 KiB boundary) and writes it via one `stm_sync_write_extent` call at offset 0 — handles sub-block user writes (e.g., off=50 / len=200 / total=250) by routing them through the combined buffer. EXTENT-mode subsequent writes pass through to `stm_sync_write_extent` and bump `si_size` if extending. The legacy direct-extent path is preserved for non-S_IFREG inodes (directories, etc.) and for (ds, ino) tuples that don't have an inode in the index — keeps the older P7-era tests working. New helpers `fs_write_regular_locked` + `fs_read_regular_locked` + `fs_inode_bump_size`. **R76 verdict: 0 P0 + 0 P1 + 1 P2 + 4 P3** — GREEN; all addressed inline. **R76 P2-1: fs_read EXTENT path didn't bound by `si_size`** — the combined-buffer transition's block-padding made the extent layer hold zero bytes past logical EOF; pre-fix readers would see those padding bytes as file content (POSIX-EOF divergence vs the INLINE path's clamp). Fixed by clamping `*out_read` to `min(*out_read, cur_size - off)` in the EXTENT branch + early-return-0 on `off >= cur_size`. R76 P3-1 + P3-2 (theoretical-only post-write inode_set partial-failure) annotated inline with the lock-posture infallibility argument (every STM_E* return from `stm_inode_set` presupposes a state mutation only the alloc/free path can perform; fs->lock holding excludes those paths). R76 P3-3 (out_read zero-init drift from R57 P3-5 / R58 P3-1 norm) closed by adding `if (out_read) *out_read = 0;` at fs_read entry. R76 P3-4 (test gap) closed by 3 new tests: `fs_p5_r76_p3_4_transition_oversize_returns_erange` (sparse off=8MB-50/len=200 → STM_ERANGE), `_transition_with_hole_at_end` (write at off>data_len in transition zero-fills the gap), `_transition_with_empty_inline` (data_len=0 → skip inline-prefix branch). test_fs 189 → 199 (7 P5 substantive + 3 R76 regressions). No format break — STM_UB_VERSION=25 preserved (no on-disk surface change; the existing `si_inline_data[100]` slot has been there since P8-POSIX-1b at v24). 37 ctest suites green. | **`inode.tla` EXTENDED** — adds `data_kind` (NONE/INLINE/EXTENT) + `data_len` (logical inline size) + `ever_extent` (BOOLEAN one-way latch) state vars, gated by `EnableInlineDataModel` flag so existing 9.87M-state run is unaffected. 5 new actions: `WriteInline` / `TransitionToExtent` / `WriteExtent` / `TruncateInline` / `TruncateExtent`. 3 new invariants: `DataKindMatchesState` / `InlineLenBounded` / `OneWayInlineToExtent`. 2 new buggy cfgs: `BuggyTruncateReinlines` (reverts to INLINE on truncate-to-small post-transition; fires `OneWayInlineToExtent`) + `BuggyInlineWriteSpills` (allows data_len > MaxInline without transitioning; fires `InlineLenBounded`). New healthy cfg `inode_inline.cfg`: 9 distinct states; existing 4 cfgs updated with FALSE flags for backward-compat. | inode (existing trigger surface) + fs (existing) — both already on CLAUDE.md trigger list |
| **P8-POSIX-6** *(closed; R80 audit running)* | xattr keyspace `(dataset_id, ino, hash_probe) → value` per ARCH §11.5 + xattr.tla. Per-pool xattr tree backed by btree_store (mirrors stm_dirent_index persistence shape). Format break **STM_UB_VERSION 25 → 26** (new `ub_xattr_root` (64-byte stm_bptr at offset 3432) + `ub_xattr_root_gen` (le64 at offset 3496); `ub_reserved` shrinks 632 → 560 bytes). New `STM_BPTR_KIND_XATTR_TREE = 14`. Merkle binding extended to 10th input (`xattr_csum`) in lockstep with the field introduction (R70 P0-1 / R72 lesson). Open-addressing primitives `stm_xattr_set` / `_get` / `_remove` / `_list` / `_drop_for_ino` per xattr.tla; tombstones encoded inline via `STM_XATTR_FLAG_TOMBSTONE`. Writer-side guards symmetric with decoder-side guards (R71 P1-1 + R77 P1-1 lesson — both name_len ≤ 255 AND value_len ≤ 64 KiB bounded at writer + decoder + 3 defense-in-depth reader sites — the OOB-read shape from inline data extends to xattr value records). New error code `STM_ENODATA = -61` (POSIX-aligned). New fs APIs: `stm_fs_setxattr` (with `STM_FS_XATTR_CREATE` / `STM_FS_XATTR_REPLACE` flags), `stm_fs_getxattr` (POSIX-shape `*out_size` reports full length regardless of buf cap), `stm_fs_listxattr` (POSIX-shape NUL-separated names), `stm_fs_removexattr`. POSIX namespace gating at fs layer: name must start with `user.` / `system.` / `security.` / `trusted.` per ARCH §11.5.1. Inode-existence precondition at every fs xattr API entry (returns STM_ENOENT for unallocated inos, not silent record-leak). 22 tests in test_xattr.c green default + ASan + TSan; 11 fs-level tests in test_fs.c (setxattr/getxattr/listxattr/removexattr × namespace validation × CREATE/REPLACE flags × persistence × RO mount × oversize × arg validation matrix); 1 sync-level integration test in test_sync.c covering set_storage / set_crypt_ctx / load_at order / bp_kind check / csum mirror / build_uberblock with the xattr triple / 10-input Merkle binding across both 1-phase fresh-pool commit + 2-phase across-mount commit. test_pool.c UB version assertion bumped 25 → 26. test_fs grew 227 → 238; test_sync 26 → 27; new test_xattr 22. POSIX ACLs (`system.posix_acl_access` + `system.posix_acl_default`) compose through this layer with no additional spec or impl work — the spec is namespace-agnostic and the fs API accepts the `system.` prefix. listxattr cursor stability deferred to a future P8-POSIX-6b chunk (analog of P8-POSIX-4's readdir extension to dirent.tla; would extend xattr.tla with a similar `iter`/`emit_log`/`iter_active` extension). | **`xattr.tla` LANDED** at spec scaffold — 133 distinct states / 543 generated; 3 buggy variants (`xattr_unlink_uses_empty_buggy.cfg`, `xattr_create_overwrites_no_probe_buggy.cfg`, `xattr_lookup_stops_on_tombstone_buggy.cfg`) all fire as designed. **No further spec extension required for substantive** — `xattr.tla` covers the open-addressing state machine; persistence composes via `btree_store` (same pattern as dirent's). | new surface (`src/xattr/` + `include/stratum/xattr.h` joined CLAUDE.md trigger list at substantive landing) + sync.c (commit hookup + Merkle binding 10th input + lifecycle + accessor `stm_sync_xattr_index`) + uberblock layout (format break v25 → v26) + Merkle root chain (10th csum) |
| **P8-POSIX-7** | Full modern POSIX surface (no hedged subset; per ROADMAP §11.1's commitment to complete coverage). May subdivide into 7a/7b/... at impl time: **(7a)** `O_TMPFILE` + every `F_SEAL_*` flag (`SEAL`, `SHRINK`, `GROW`, `WRITE`, `FUTURE_WRITE`) + `statx(2)` shape with btime + nanosecond timestamps. **(7b)** Every `FALLOC_FL_*` flag (`KEEP_SIZE`, `PUNCH_HOLE`, `COLLAPSE_RANGE`, `ZERO_RANGE`, `INSERT_RANGE`, `UNSHARE_RANGE`) — extent-tree manipulation primitives for COLLAPSE/INSERT land alongside; UNSHARE composes with the CAS rehydrate path. **(7c)** `name_to_handle_at(2)` + `open_by_handle_at(2)` over the `(dataset_id, ino, si_gen)` tuple; stale-handle detection via the `inode.tla`-pinned `si_gen` uniqueness invariant. **(7d)** Advisory locking (`flock(2)` + `fcntl(2)` `F_SETLK`/`F_GETLK`/`F_OFD_SETLK`); per-inode lock table with deadlock detection. **(7e)** `posix_fadvise(2)` pass-through to `STM_PROP_TIERING` advisory layer. | composes over inode.tla; `(7d)` introduces a `locks.tla` extension for the per-inode lock table's deadlock-freedom invariant. | inode + dirent + sync |
| **P8-POSIX-8** *(closed; R77 audit run + applied)* | Symlinks (inline target only — ≤ STM_INODE_INLINE_MAX = 100 B). New public APIs: `stm_fs_symlink(fs, ds, parent, name, target, target_len, uid, gid, *out_ino)` creates a symlink inode with `mode = S_IFLNK \| 0777` (POSIX convention; symlink permission bits unused), stamps `si_data_kind = STM_DATA_SYMLINK` + `si_data_len = target_len` + `si_data.symlink_target` bytes, links in parent dirent with `STM_DT_LNK`. `stm_fs_readlink(fs, ds, ino, target_buf, target_max, *out_len)` validates the inode is `S_IFLNK` (else STM_EINVAL per POSIX `readlink(2)`), copies up to `target_max` bytes, returns full target length via `*out_len` so callers can re-call with a larger buffer if truncated. New error code `STM_ENAMETOOLONG = -36` (POSIX-aligned) for target > 100 B refusal. Long-symlink (extent-backed) support deferred — would compose with P8-POSIX-5's inline → extent transition path; symlinks on Linux are typically < 60 bytes (full path + filename), so the inline-only MVP covers > 95% of real-world cases. **R77 verdict: 0 P0 + 1 P1 + 1 P2 + 2 P3** — YELLOW; all addressed inline. **R77 P1-1: stm_fs_readlink OOB stack read on corrupt si_data_len** — neither `in_decode_value` (decoder) nor `stm_inode_set` (writer) bounded `si_data_len ≤ STM_INODE_INLINE_MAX` for INLINE/SYMLINK kinds; a tampered on-disk record carrying `si_data_kind=STM_DATA_SYMLINK` + `si_data_len=200` would load cleanly and `stm_fs_readlink` would memcpy 200 bytes from `iv.si_data.symlink_target[100]` — leaking up to 156 bytes of stack to caller-controlled buffer. Same R71 P1-1 shape (writer-side / decoder-side guards must mirror reader assumptions). Fixed at the canonical sites: decoder-side guard in `in_decode_value` + writer-side guard in `stm_inode_set` (both reject `si_data_len > STM_INODE_INLINE_MAX` for INLINE/SYMLINK with STM_ECORRUPT/STM_EINVAL). Defense-in-depth caps added in `fs_write_regular_locked` + `fs_read_regular_locked` (INLINE) + `stm_fs_readlink` (SYMLINK) — STM_ECORRUPT if any future bypass leaks an oversize record past the boundary guards. R77 P2-1 (regression test) closed by `fs_p8_r77_p1_1_inode_set_rejects_oversize_si_data_len`: allocates valid SYMLINK + REG inodes, mutates iv.si_data_len to 200, attempts stm_inode_set → STM_EINVAL (writer-guard catches); confirms the cap (100) is accepted. R77 P3-1 (zero-init order drift) closed by moving `*out_child_ino = 0;` BEFORE arg validation in stm_fs_symlink. R77 P3-2 (ENAMETOOLONG vs ERANGE doc) closed by header docstring tightening. test_fs 199 → 207 (7 P5 substantive + 1 R77 regression). No format break — STM_UB_VERSION=25 preserved (existing `si_data.symlink_target[100]` slot has been there since P8-POSIX-1b at v24). 37 ctest suites green. test_fs ctest timeout bumped 60s → 180s in `tests/CMakeLists.txt` since the suite now runs ~40s wall under `-j2`. | composes over inode.tla — no spec extension (the symlink kind doesn't transition in MVP scope; no load-bearing invariants beyond what stm_inode_alloc + stm_inode_set already pin) | fs (existing) + inode (existing) — both already on CLAUDE.md trigger list |
| **P8-POSIX-9** *(MVP closed; R79 audit run + applied; RENAME_EXCHANGE + RENAME_WHITEOUT deferred)* | `stm_fs_rename(fs, ds, src_parent, src_name, dst_parent, dst_name, flags)` — atomic rename. Composes `dirent_alloc(dst → src_ino) + dirent_unlink(src) + (overwrite path) dirent_unlink(dst) + inode_unlink(dst-prior, &freed)` under fs->lock so the next sync_commit flushes them as a unit. MVP: basic rename + `STM_FS_RENAME_NOREPLACE` flag. Refusals: name validation, ds/parent_ino == 0, unknown flag bits, parent not S_IFDIR (STM_ENOTDIR), src missing (STM_ENOENT), dst exists with NOREPLACE (STM_EEXIST), kind mismatch (file→dir → STM_EISDIR; dir→file → STM_ENOTDIR), dir→non-empty-dir (STM_ENOTEMPTY), wedged/RO (STM_EWEDGED/STM_EROFS). Same-path no-op returns STM_OK. Best-effort rollback on alloc / unlink failure with stm_fs_mark_wedged on rollback failure. **R79 verdict: 0 P0 + 0 P1 + 1 P2 + 2 P3** — GREEN; all addressed inline. R79 P2-1 (hardlink-rename test gap) closed by `fs_p9_r79_p2_1_rename_hardlink_same_inode_consistent` — pins that `rename(a, b)` when `a` and `b` are hardlinks (nlink=2) to the same inode produces the POSIX-correct outcome (a gone, b preserved → ino, nlink=1). R79 P3-1 (defense-in-depth on rollback's freshly-installed-dst-unlink rv) closed by capturing the rv + feeding it into the wedge guard. R79 P3-2 (misleading comment about which slot dirent_alloc reuses) closed by tightening to clarify the chain probes deterministically by hash and either install slot is correct. test_fs 217 → 227 (9 P9 substantive + 1 R79 regression). No format break — STM_UB_VERSION=25 preserved. **RENAME_EXCHANGE + RENAME_WHITEOUT deferred** to follow-up sub-chunks (EXCHANGE needs a new dirent atomic-two-slot-swap primitive; WHITEOUT is rare). | composes dirent.tla — no spec extension for MVP (rename = atomic Unlink+Create composition; existing chain-integrity invariants cover. RENAME_EXCHANGE follow-up will require a new dirent.tla action `Swap(d, n1, n2)` with corresponding `SwapAtomicity` invariant) | fs (existing) — already on CLAUDE.md trigger list |
| **P8-POSIX-10** *(truncate-half closed; R78 audit run + applied; reflink-wrapper deferred)* | `stm_fs_truncate(fs, ds, ino, new_size)` — POSIX-shape truncate(2) wrapper composing the inode kind dispatch (P8-POSIX-5) with `stm_sync_truncate` (P7-9). Six paths: INLINE-shrink (just update si_data_len + si_size); INLINE-grow within inline cap (zero-fill the gap, stays inline); INLINE-grow past cap (transition via combined-buffer write — same shape as fs_write_regular_locked's transition path); EXTENT-shrink (delegate to sync_truncate, requires 4 KiB-aligned new_size, stays EXTENT per ARCH §11.3.3 / inode.tla::OneWayInlineToExtent); EXTENT-grow (just update si_size, sparse extent semantics zero-fill the [old_size, new_size) range on read); no-op (new_size == cur_size). Refusals: directory → STM_EISDIR (POSIX); non-S_IFREG → STM_EINVAL; sub-block EXTENT-mode truncate → STM_EINVAL; oversize transition → STM_ERANGE. R77 P1-1 defense-in-depth: rejects corrupt si_data_len > STM_INODE_INLINE_MAX before any memcpy. **R78 verdict: 0 P0 + 0 P1 + 0 P2 + 3 P3** — GREEN; P3-1 (lock-posture infallibility annotations on the 3 stm_inode_set call-sites) closed inline; P3-2 (truncate-then-write composition coverage gap) closed by 2 new regression tests (`fs_p10_r78_p3_2_truncate_then_write_within_inline` + `fs_p10_r78_p3_2_truncate_extent_then_write_inside_no_size_regress` — the latter pins that overwriting an EXTENT file's earlier blocks via fs_write doesn't regress si_size when prior truncate-grow extended it); P3-3 (no-op path skips ctime/mtime stamp) forward-noted as a P8-wide gap to land at the eventual statx clock integration in P8-POSIX-7. test_fs 207 → 217 (8 P10 substantive + 2 R78 regressions). No format break — STM_UB_VERSION=25 preserved. **`copy_file_range` reflink wrapper deferred** — `stm_fs_reflink` (P7-16) already provides POSIX FICLONE shape at the fs.c layer; the P8-POSIX-10 row's "reflink wrapper" intent was inode-level validation + better integration with the inode-aware fs_write/fs_read paths, which can land as a follow-up sub-chunk once xfstests reveals the gaps. | composes inode.tla — no spec extension (the truncate state-machine is `data_kind` transitions already pinned by inode.tla's P8-POSIX-5 invariants — `OneWayInlineToExtent`, `InlineLenBounded`, `DataKindMatchesState`) | fs (existing) — already on CLAUDE.md trigger list |
| **P8-POSIX-11** | Tests + audit + xfstests integration prep. Property-based tests for the file/dir/xattr/ACL layer. Regression suite for ARCH §11 corner cases. | — | full P8 surface |

## Exit criteria (ROADMAP §11.2)

- [ ] All POSIX file/dir ops produce semantically correct results
      matching ext4/XFS/APFS for a representative test corpus.
- [ ] Hash-collision chain works correctly with ≥1k dirents per
      directory.
- [ ] Hard link nlink remains correct under concurrent create/unlink.
- [ ] Inline-to-extent transition recovers correctly across crash
      boundaries.
- [ ] `statx` returns nanosecond timestamps + btime.
- [ ] POSIX ACL roundtrip preserves grants/denies bit-exact.
- [ ] `inode.tla` + `dirent.tla` (+ likely `xattr.tla`) pin the
      load-bearing invariants under TLC.
- [ ] xfstests subset (file/dir/xattr/ACL portion) green on
      Stratum-backed mount once Phase 9 (FUSE) lands; in Phase 8
      the equivalent verification runs against the `stm_fs_*` API
      directly.

## Posture going in

Inheriting from Phase 7 exit (`1759caf` + P8-NS-1 spec at `bea7f82`):

| | |
|---|---|
| STM_UB_VERSION | 23 (last bumped at P7-CAS-16). Phase 8 will likely bump for the inode tree on-disk format addition (probably v23 → v24 at P8-POSIX-1). |
| STM_SEND_VERSION | 3 |
| Spec posture | 22 modules / 26 fixed cfgs / 38 buggy cfgs (after P8-NS-1's namespace.tla landed). Phase 8 adds `inode.tla`, `dirent.tla`, probably `xattr.tla` — projected 24-25 modules at Phase 8 exit. |
| ctest | 35 suites green default + ASan + TSan |
| test_fs | 159 |
| test_send_recv | 40 |
| Audits closed | R0 — R68 |

## Format break expectations

P8-POSIX-1 will add a NEW per-dataset on-disk surface (the inode
tree). This is a format break — STM_UB_VERSION 23 → 24. v23 pools
won't have an inode tree; mounting them on v24 would need either
a migration step or a refusal at mount. Probably refusal at
mount with STM_EBADVERSION (mirroring P7-CAS-16's pattern), since
v23 → v24 represents adding a load-bearing structural layer
rather than extending an existing record shape.

Format-break signoff required per CLAUDE.md before P8-POSIX-1
substantive lands.

## Operational notes

- Spec-first (CLAUDE.md): inode and dirent invariants are
  load-bearing. `inode.tla` lands BEFORE P8-POSIX-1 substantive,
  `dirent.tla` lands BEFORE P8-POSIX-2 substantive.
- Audit-per-change: `src/inode/` and `src/dirent/` join the
  CLAUDE.md trigger list as soon as the first chunk lands. The
  v1 `src/p9/p9.c` audit pattern (R6, R12 specifically) carries
  forward — silent dirent-tree corruption is the canonical
  failure mode here.
- Memory primer roll at every chunk close: MEMORY.md index +
  project_v2_active.md tip / lineage table + project_v2_next_session.md.
