# Phase 8 — status and pickup guide

Authoritative pickup guide for Phase 8 (POSIX surface — inodes,
dirents, xattr, ACLs, statx, small-file inline, reflink wrapper,
the full set of POSIX file/dir operations).

**Phase 8 ENTERED 2026-04-30** at `bea7f82` after the user
surfaced that ARCHITECTURE §11 (POSIX surface) was a missing
phase under the prior 10-phase ROADMAP numbering. Phase 8 was
inserted; client interfaces shifted to Phase 9. Tag
`phase-7-complete` (at `1759caf`) still marks the Phase 7 → 8
boundary.

ROADMAP §11 lists the deliverables and §11.2 the exit criteria.
ARCHITECTURE §11 is the design document — the on-disk shape is
fully committed there (256-byte `stm_inode`, hash-indexed B-tree
dirents keyed by `(dir_ino, STM_KEY_DIRENT, fnv1a(name))`, tagged
data union extent / inline / symlink / device, `si_gen` for
ino-reuse detection, inline data ≤100 bytes for small files).
This phase implements §11.

## Why this phase exists

Phase 7 built the data plane (extent records keyed by `(ds, ino,
off, len)`, FastCDC, CAS tier, send/recv, reflinks) and the
namespace plane (datasets, snapshots, clones, dead-list). What's
missing is the **filesystem semantics layer** — the inode metadata
and directory entry storage that turns a (dataset, ino, byte-
offset) byte store into a POSIX-shape file/directory tree:

- `stm_fs_lookup(parent_ino, name) → child_ino` — name resolution
- `stm_fs_create_file` / `stm_fs_mkdir` / `stm_fs_unlink` /
  `stm_fs_rmdir` / `stm_fs_rename` — directory mutations
- `stm_fs_stat` / `stm_fs_chmod` / `stm_fs_chown` / `stm_fs_utimens`
  — metadata ops
- `stm_fs_readdir` — directory traversal
- `stm_fs_link` / `stm_fs_symlink` / `stm_fs_readlink` — links
- xattr + POSIX ACLs
- modern POSIX features (`O_TMPFILE`, `F_SEAL_*`, `statx`)

v1's `src/fs/fs.c` had this layer; v2 has a flat (ds, ino, off)
extent API. Phase 8's job is the v2 reimplementation per ARCH
§11 — extending the prior 88-byte v1 inode to the 256-byte v2
inode and porting the hash-indexed dirent shape.

Phase 9 (client interfaces) is gated on Phase 8 exit: 9P's Twalk
/ Tcreate / Treaddir / Topen / Tstat have nothing to forward to
without this layer.

## Sub-chunk plan

Each sub-chunk is its own substantive + R-close + hash-fixup
cycle (per CLAUDE.md three-commit pattern) where the audit-trigger
surface applies. New surfaces added to CLAUDE.md's trigger list
during this phase:

- `src/inode/` — inode tree write paths.
- `src/dirent/` — dirent btree mutations + hash-collision chain
  walks (single-byte invariant violations here are how
  ext4 + xfs have historically had silent-corruption regressions).
- `src/fs/fs.c` for any new public POSIX op API additions.

| Sub-chunk | Scope | TLA+ scaffold | Audit trigger |
|---|---|---|---|
| **P8-POSIX-1** *(spec scaffold + substantive landed; R69 audit running)* | `stm_inode` struct + in-memory allocator (alloc-fresh-only). New `include/stratum/inode.h` (256-byte struct stm_inode_value per ARCH §11.3, with v2 simplification: 16-byte (paddr, gen) extent-tree-root vs ARCH's 57-byte stm_bptr). New `src/inode/inode.c` (allocator with pthread_mutex_t-guarded records[] + per-dataset next_ino[]). 19 tests in test_inode.c green default + ASan + TSan. AllocReused (with gen bump per inode.tla) deferred to P8-POSIX-1b. | **`inode.tla` LANDED** — alloc / free / generation invariants; healthy 879025 distinct states / depth 25; two buggy variants (BuggyReuseNoGenBump, BuggyDoubleAllocate) fire as designed. | new surface (`src/inode/inode.c` + `include/stratum/inode.h` joined CLAUDE.md trigger list at substantive landing) |
| **P8-POSIX-1b** *(closed; R70 + R71 + R71b audits run + applied)* | Per-pool inode tree persistence backed by btree_store (mirrors `stm_extent_index`'s load_at + commit shape). Format break **STM_UB_VERSION 23 → 24** (new `ub_inode_root` + `ub_inode_root_gen` fields in per-device payload; `ub_reserved` 776 → 704). AllocReused path with gen bump per `inode.tla::AllocReused`; FREED state encoded inline via `STM_INO_FLAG_FREED` in `si_flags`. **R70 verdict: 1 P0 + 0 P1 + 2 P2 + 8 P3** — all addressed inline. **R70 P0-1: `inode_csum` folded into the Merkle chain** (closes the same shape as R47 P2-1's repair_log_csum binding — without it an offline-write attacker could swap the inode tree by recomputing `ub_csum` alone). **R71 verdict on the R70 close: 0 P0 + 1 P1 + 2 P2 + 10 P3** — all P0/P1/P2 applied inline. **R71 P1-1: `stm_inode_set` rejects nlink=0 on ALLOCATED records** (closes the silent-commit-then-wedge surface left by R70's asymmetric decoder-only FREED ⇔ nlink invariant — the writer-side guard now mirrors the decoder's check). R71 P2-1 (latch comment split) + P2-2 (get_root docstring drift) fixed. R71 P3-1 through P3-10 forward-noted (mostly regression-test coverage gaps for invariants pinned at audit time). **R71b verdict on the R69-close gap (`6ebf295` retroactive): 0 P0 + 0 P1 + 0 P2 + 2 P3** — clean. R71b P3-1 (cap-doubling guard tightened to bound the `* sizeof` multiplication in `get_or_create_dsstate` + `append_record`) + P3-2 (state-preservation-after-rejection assertion in `inode_set_refuses_unknown_data_kind`) applied inline. test_inode 32 → 36; test_sync 24 → 25. | composes over `inode.tla` (no spec extension required) | inode (existing surface) + sync.c (commit hookup) + uberblock layout (format break) + Merkle root chain (R70 P0-1) |
| **P8-POSIX-2** *(closed; R72 audit run + applied)* | Dirent layer. Per-pool hash-indexed B-tree per ARCH §11.4: key `(le64 dataset_id || le64 dir_ino || le64 hash_probe)` where `hash_probe = fnv1a64(name) + probe_offset`. Probe-chain (cap 64) for collisions per ARCH §11.4.2. Format break **STM_UB_VERSION 24 → 25** (new `ub_dirent_root` + `ub_dirent_root_gen` fields; `ub_reserved` 704 → 632; new `STM_BPTR_KIND_DIRENT_TREE = 13`). Merkle binding extended to 9th input (`dirent_csum`) in lockstep with the field introduction (R70 P0-1 lesson). Open-addressing primitives `stm_dirent_alloc` / `_lookup` / `_unlink` / `_count_for_dir` per dirent.tla; tombstones encoded inline via `STM_DIRENT_FLAG_TOMBSTONE`. Writer-side guards symmetric with decoder-side guards (R71 P1-1 lesson) — alloc rejects every shape the decoder rejects (zero ino / zero name_len / invalid child_type / oversized name). **R72 verdict: 0 P0 + 0 P1 + 1 P2 + 5 P3** — clean; disciplines from R70/R71/R71b transferred correctly. R72 P2-1 (sync-level integration coverage gap, analog of R70 P2-1 for inode) closed inline by `sync_dirent_persistence_roundtrip` test in test_sync.c covering set_storage / set_crypt_ctx / load_at order / bp_kind check / csum mirror / build_uberblock with the dirent triple / 9-input Merkle binding across both 1-phase fresh-pool commit + 2-phase across-mount commit, plus tombstone-slot-reuse post-load (R72 P3-2 folded in). R72 P3-1 (forced-FNV-collision regression test), P3-3 (chain-exhaustion STM_ENOSPC coverage), P3-4 (uint64-wrap cosmetic), P3-5 (byte-layout pin) forward-noted into P8-POSIX-2b's audit. 15 tests in test_dirent.c + sync_dirent_persistence_roundtrip in test_sync.c green default + ASan + TSan. Higher-level fs APIs (stm_fs_lookup / _create_file / _mkdir / _unlink / _rmdir) deferred to P8-POSIX-2b — they compose dirent + inode at the per-fs lock layer. | **`dirent.tla` LANDED** — open-addressing chain integrity (581 healthy states / depth 7); 3 buggy variants `BuggyUnlinkUsesEmpty` (82 states), `BuggyCreateOverwritesNoProbe` (14 states), `BuggyLookupStopsOnTombstone` (84 states) all fire as designed. **No spec extension required** for persistence (composes via `btree_store`). | new surface (`src/dirent/` + `include/stratum/dirent.h` joined CLAUDE.md trigger list at substantive landing) + sync.c (commit hookup + Merkle binding) + uberblock layout (format break v24 → v25) + Merkle root chain (9th csum) |
| **P8-POSIX-2b** *(closed; R73 audit run + applied)* | fs.c POSIX wrappers composing dirent + inode at the per-fs lock layer: `stm_fs_lookup` / `_create_file` / `_mkdir` / `_unlink` / `_rmdir`. Lock chain: `fs->lock → inode_idx mutex / dirent_idx mutex` (siblings, no cross-locking). Atomicity: create-paths roll back the freshly-allocated inode if dirent-link fails (no orphans on failure). MVP single-link semantics — unlink frees the inode unconditionally; hard links + nlink decrement defer to P8-POSIX-3 (`P8-POSIX-3-TODO:` markers added inline at the cascade-free site for grep-discoverability). POSIX name validation: 1..255 bytes, no NUL/'/'; "." and ".." reserved. New error codes `STM_ENOTDIR / STM_EISDIR / STM_ENOTEMPTY` (POSIX-aligned values). **R73 verdict: 0 P0 + 0 P1 + 1 P2 + 7 P3** — composition is sound. R73 P2-1 (rmdir leaks orphan tombstones at freed dir_ino) closed inline by new `stm_dirent_drop_for_dir` primitive called from rmdir before inode_free; P3-5 (rmdir-with-tombstones test) folded into the same close as `fs_p2b_r73_p2_1_rmdir_cleans_orphan_tombstones`; P3-6 (P8-POSIX-3-TODO comment) applied. R73 P3-1 (forced FNV-collision test for tombstone-walk), P3-2 (uint8_t > 255u dead-code), P3-3 + P3-4 (R72 forward-noted concerns still uncovered: forced collision + chain exhaustion at wrapper layer), P3-7 (per-API arg-validation matrix coverage gap) all forward-noted into P8-POSIX-3's audit close. test_dirent 15 → 17 (drop_for_dir tests); test_fs 170 → 171. | composes over `inode.tla` + `dirent.tla` (no spec extension required for MVP single-link semantics; nlink + hard-link atomicity invariants land at P8-POSIX-3) | composes 2 trigger surfaces: inode allocator + dirent layer + Public API contracts (5 new fs.h decls + 1 new dirent.h primitive) |
| **P8-POSIX-3** *(closed; R74 audit run + applied)* | Metadata ops + nlink + hard links. New fs APIs: `stm_fs_stat` (returns full 256-byte stm_inode_value), `stm_fs_chmod` (preserves S_IFMT bits; rejects type-mismatch), `stm_fs_chown` (UINT32_MAX = leave-unchanged, POSIX `chown(-1)` semantics), `stm_fs_utimens` (atime + mtime + caller-supplied ctime; nsec validated < 1e9), `stm_fs_link` (same-dataset hard link; signature precludes cross-dataset; hard-link-on-directory refused with STM_ENOTSUPPORTED per POSIX). New inode-layer APIs: `stm_inode_link` (nlink++) + `stm_inode_unlink` (nlink--; cascade-free atomically when nlink reaches 0 — models inode.tla::Unlink). The MVP single-link unconditional `stm_inode_free` from P8-POSIX-2b's unlink/rmdir wrappers has been replaced by `stm_inode_unlink`'s cascade-free path. **R74 verdict: 0 P0 + 0 P1 + 0 P2 + 4 P3** — clean. R74 P3-1 (cross-dataset STM_EXDEV unreachable in single-`dataset_id` signature) + R74 P3-2 (STM_EPERM ↛ STM_ENOTSUPPORTED docstring drift) closed inline via docstring tightening; R74 P3-3 (rollback failure swallowed in stm_fs_link's (void)-cast — defense-in-depth gap, theoretical only under fs->lock) annotated inline with future-mark-wedged note; R74 P3-4 (utimens 0/0 epoch sentinel collision — placeholder until P8-POSIX-7 statx clock integration) forward-noted. test_fs 171 → 178; test_inode 36 → 40. | **`inode.tla` EXTENDED** — adds `nlink` shadow var + Link/Unlink actions + `LinkedAllocatedHasPositiveNlink` + `FreedHasZeroNlink` invariants. New `BuggyUnlinkLeavesZeroNlink` config (fires LinkedAllocatedHasPositiveNlink). Healthy 9.87M states / 2.96M distinct (Inos=3, MaxGen=3, MaxNlink=2). | inode (allocator + new link/unlink) + fs (5 new public APIs) |
| **P8-POSIX-4** *(closed; R75 audit run + applied)* | `stm_fs_readdir` (with `STM_FS_READDIR_FLAG_NO_DOTS` flag for callers who manage `.`/`..` themselves) + low-level `stm_dirent_readdir`. Opaque `uint64_t` cursor combines synth phase (0=`.`, 1=`..`) with stored-dirent scan (≥2 in dirent-cursor space). Stored entries returned in `hash_probe`-ascending order; tombstones skipped (per `dirent.tla::ReaddirNoTombstoneEmitted`); cursor strictly advances past every emitted slot (per `dirent.tla::ReaddirNoDuplicateProbeInLog` + `ReaddirCursorMonotonicEmits`); cursor never re-emits the same probe within an iteration even under inter-call Create/Unlink. fs-layer wrapper validates `dir_ino` is `S_IFDIR` (else `STM_ENOTDIR`); rejects unknown flag bits (forward-compat). Synthesized `.` has `child_ino=dir_ino`, ".." has `child_ino=parent_ino` (caller-provided; for root dataset, pass `parent_ino=dir_ino` per POSIX root convention). `child_gen=0` on synth entries (synth never goes stale). MVP perf: `stm_dirent_readdir` does an O(N) scan + O(N log N) qsort per call (N = total records under (ds, dir_ino)); future top-K-heap optimization deferred until xfstests stresses 1M-entry dirs (ARCH §11.4.3 perf concern). **R75 verdict: 0 P0 + 0 P1 + 1 P2 + 4 P3** — YELLOW; all addressed inline. **R75 P2-1: cursor saturation produces infinite-emit loop on `hash_probe == UINT64_MAX`** — strict-less-than filter `r->hash_probe < UINT64_MAX` is false at probe=UINT64_MAX, so a record at that probe gets re-emitted forever after the cursor advance saturates. Adversary effort: ~2^58 FNV-1a pre-image per matching probe offset (feasible against shared-tenancy filesystems with hostile name selection). Fixed by short-circuit on `*cursor == UINT64_MAX` at entry to both `stm_dirent_readdir` and `stm_fs_readdir`; UINT64_MAX is now formally a "iteration done" sentinel. R75 P3-1 (out-param zero-init order drift from R57 P3-5 / R58 P3-1 norm) closed by moving `*out_returned = 0` BEFORE arg validation. R75 P3-2 (doc/impl mismatch on saturation) closed by tightening dirent.h + fs.h docs. R75 P3-3 (RO-mount positive test gap) closed by `fs_p4_readdir_r75_p3_3_ro_mount_succeeds`. R75 P3-4 (defense-in-depth name_len bound at fs.c → dirent.c trust boundary) closed by adding STM_ECORRUPT-on-oversize check in fs.c emit loop (R71 P1-1 lesson — symmetric guards across trust boundaries). No format break (no on-disk surface change). test_dirent 17 → 28 (9 base readdir tests + 2 R75 regressions: cursor-saturation-terminates + out-param-zero-init-on-einval). test_fs 178 → 189 (8 base fs-level + 3 R75 regressions: cursor-saturation-terminates + zero-init-on-einval + ro-mount-succeeds). | **`dirent.tla` EXTENDED** — adds `iter` (cursor) + `emit_log` (sequence) + `iter_active` (flag) state + 3 readdir actions (`ReaddirReset`, `ReaddirStep`, `ReaddirEnd`) + 4 readdir invariants (`ReaddirNoTombstoneEmitted`, `ReaddirNoDuplicateProbeInLog`, `ReaddirCursorMonotonicEmits`, `ReaddirCompleteAtEnd`). 3 new buggy cfgs: `BuggyReaddirIncludesTombstones` (547 states), `BuggyReaddirNoCursorAdvance` (548 states), `BuggyReaddirSkipsLiveRecord` (1113 states) all fire as designed. Stable-iteration model: Create/Unlink gated on `~iter_active[d]` (mutations only between iterations). Healthy 241,017 distinct states / depth 17 / 11s. | dirent (existing) + fs (existing) — both already on CLAUDE.md trigger list |
| **P8-POSIX-5** *(closed; R76 audit run + applied)* | Inline data optimization. fs.c::stm_fs_write/read now dispatch on `(inode exists, mode & S_IFMT == S_IFREG, si_data_kind)`: regular-file writes go through `fs_write_regular_locked` which keeps small files inline (≤ STM_INODE_INLINE_MAX = 100 B) in `si_inline_data` (no extent allocation), and triggers a one-way inline → extent transition when a write would grow the file past the inline cap. Transition path builds a single block-aligned combined buffer (existing inline prefix + user bytes overlaid + zero pad up to next 4 KiB boundary) and writes it via one `stm_sync_write_extent` call at offset 0 — handles sub-block user writes (e.g., off=50 / len=200 / total=250) by routing them through the combined buffer. EXTENT-mode subsequent writes pass through to `stm_sync_write_extent` and bump `si_size` if extending. The legacy direct-extent path is preserved for non-S_IFREG inodes (directories, etc.) and for (ds, ino) tuples that don't have an inode in the index — keeps the older P7-era tests working. New helpers `fs_write_regular_locked` + `fs_read_regular_locked` + `fs_inode_bump_size`. **R76 verdict: 0 P0 + 0 P1 + 1 P2 + 4 P3** — GREEN; all addressed inline. **R76 P2-1: fs_read EXTENT path didn't bound by `si_size`** — the combined-buffer transition's block-padding made the extent layer hold zero bytes past logical EOF; pre-fix readers would see those padding bytes as file content (POSIX-EOF divergence vs the INLINE path's clamp). Fixed by clamping `*out_read` to `min(*out_read, cur_size - off)` in the EXTENT branch + early-return-0 on `off >= cur_size`. R76 P3-1 + P3-2 (theoretical-only post-write inode_set partial-failure) annotated inline with the lock-posture infallibility argument (every STM_E* return from `stm_inode_set` presupposes a state mutation only the alloc/free path can perform; fs->lock holding excludes those paths). R76 P3-3 (out_read zero-init drift from R57 P3-5 / R58 P3-1 norm) closed by adding `if (out_read) *out_read = 0;` at fs_read entry. R76 P3-4 (test gap) closed by 3 new tests: `fs_p5_r76_p3_4_transition_oversize_returns_erange` (sparse off=8MB-50/len=200 → STM_ERANGE), `_transition_with_hole_at_end` (write at off>data_len in transition zero-fills the gap), `_transition_with_empty_inline` (data_len=0 → skip inline-prefix branch). test_fs 189 → 199 (7 P5 substantive + 3 R76 regressions). No format break — STM_UB_VERSION=25 preserved (no on-disk surface change; the existing `si_inline_data[100]` slot has been there since P8-POSIX-1b at v24). 37 ctest suites green. | **`inode.tla` EXTENDED** — adds `data_kind` (NONE/INLINE/EXTENT) + `data_len` (logical inline size) + `ever_extent` (BOOLEAN one-way latch) state vars, gated by `EnableInlineDataModel` flag so existing 9.87M-state run is unaffected. 5 new actions: `WriteInline` / `TransitionToExtent` / `WriteExtent` / `TruncateInline` / `TruncateExtent`. 3 new invariants: `DataKindMatchesState` / `InlineLenBounded` / `OneWayInlineToExtent`. 2 new buggy cfgs: `BuggyTruncateReinlines` (reverts to INLINE on truncate-to-small post-transition; fires `OneWayInlineToExtent`) + `BuggyInlineWriteSpills` (allows data_len > MaxInline without transitioning; fires `InlineLenBounded`). New healthy cfg `inode_inline.cfg`: 9 distinct states; existing 4 cfgs updated with FALSE flags for backward-compat. | inode (existing trigger surface) + fs (existing) — both already on CLAUDE.md trigger list |
| **P8-POSIX-6** *(closed; R80 audit run + applied)* | xattr keyspace `(dataset_id, ino, hash_probe) → value` per ARCH §11.5 + xattr.tla. Per-pool xattr tree backed by btree_store (mirrors stm_dirent_index persistence shape). Format break **STM_UB_VERSION 25 → 26** (new `ub_xattr_root` (64-byte stm_bptr at offset 3432) + `ub_xattr_root_gen` (le64 at offset 3496); `ub_reserved` shrinks 632 → 560 bytes). New `STM_BPTR_KIND_XATTR_TREE = 14`. Merkle binding extended to 10th input (`xattr_csum`) in lockstep with the field introduction (R70 P0-1 / R72 lesson). Open-addressing primitives `stm_xattr_set` / `_get` / `_remove` / `_list` / `_drop_for_ino` per xattr.tla; tombstones encoded inline via `STM_XATTR_FLAG_TOMBSTONE`. Writer-side guards symmetric with decoder-side guards (R71 P1-1 + R77 P1-1 lesson — both name_len ≤ 255 AND value_len ≤ 64 KiB bounded at writer + decoder + 3 defense-in-depth reader sites — the OOB-read shape from inline data extends to xattr value records). New error code `STM_ENODATA = -61` (POSIX-aligned). New fs APIs: `stm_fs_setxattr` (with `STM_FS_XATTR_CREATE` / `STM_FS_XATTR_REPLACE` flags), `stm_fs_getxattr` (POSIX-shape `*out_size` reports full length regardless of buf cap), `stm_fs_listxattr` (POSIX-shape NUL-separated names), `stm_fs_removexattr`. POSIX namespace gating at fs layer: name must start with `user.` / `system.` / `security.` / `trusted.` per ARCH §11.5.1. Inode-existence precondition at every fs xattr API entry (returns STM_ENOENT for unallocated inos, not silent record-leak). **R80 verdict: 1 P0 + 0 P1 + 3 P2 + 7 P3** — RED, all addressed inline. **R80 P0-1: install_bytes OOM zombie wedges pool on next mount** — `stm_xattr_set`'s append_record path bumped n_records++ before install_bytes; on install_bytes STM_ENOMEM, the zero-keyed slot survived in records[] and got serialized at the next sync_commit. On reopen, xa_load_iter rejected ds==0 with STM_ECORRUPT — unmountable pool. Fixed by tracking target_freshly_appended + rolling back n_records-- on install_bytes failure; structural regression test `xattr_r80_p0_1_install_failure_no_zombie_record`. **R80 P2-1: setxattr accepts NUL bytes in names** (POSIX listxattr's NUL-separated output convention would split them into "ghost" entries — confusion vector). Fixed by NUL-byte rejection in `fs_xattr_name_in_posix_namespace` after prefix-match; regression `fs_p6_r80_p2_1_setxattr_refuses_nul_in_name`. **R80 P2-2: Merkle xattr_csum binding regression test missing** (a future change dropping xattr_csum from compute_merkle_root would not be caught by consistency tests alone). Fixed by `sync_xattr_root_csum_tamper_detected` — directly tampers ub_xattr_root.bp_csum + recomputes ub_csum to keep SB integrity intact, expects sync_open's Merkle recompute to reject with STM_ECORRUPT. **R80 P2-3: STM_ENOSPC chain-exhaustion path has no test** — forward-noted (real exhaustion test needs a STM_BUILD_TESTING_HOOKS-gated seam; deferred to P8-POSIX-11 alongside the malloc-fault-injection harness for P0-1). R80 P3-1 through P3-7 fixed inline (docstring tightening, dead-code annotation, defense-in-depth value_len caps, explicit zero-init contracts in xa_decode_value, encode value-NULL guard, format-break test coverage acknowledged). 23 tests in test_xattr.c green default + ASan + TSan; 12 fs-level tests in test_fs.c (setxattr/getxattr/listxattr/removexattr × namespace validation × CREATE/REPLACE flags × persistence × RO mount × oversize × arg validation matrix); 1 sync-level integration test in test_sync.c covering set_storage / set_crypt_ctx / load_at order / bp_kind check / csum mirror / build_uberblock with the xattr triple / 10-input Merkle binding across both 1-phase fresh-pool commit + 2-phase across-mount commit. test_pool.c UB version assertion bumped 25 → 26. test_fs grew 227 → 238; test_sync 26 → 27; new test_xattr 22. POSIX ACLs (`system.posix_acl_access` + `system.posix_acl_default`) compose through this layer with no additional spec or impl work — the spec is namespace-agnostic and the fs API accepts the `system.` prefix. listxattr cursor stability deferred to a future P8-POSIX-6b chunk (analog of P8-POSIX-4's readdir extension to dirent.tla; would extend xattr.tla with a similar `iter`/`emit_log`/`iter_active` extension). | **`xattr.tla` LANDED** at spec scaffold — 133 distinct states / 543 generated; 3 buggy variants (`xattr_unlink_uses_empty_buggy.cfg`, `xattr_create_overwrites_no_probe_buggy.cfg`, `xattr_lookup_stops_on_tombstone_buggy.cfg`) all fire as designed. **No further spec extension required for substantive** — `xattr.tla` covers the open-addressing state machine; persistence composes via `btree_store` (same pattern as dirent's). | new surface (`src/xattr/` + `include/stratum/xattr.h` joined CLAUDE.md trigger list at substantive landing) + sync.c (commit hookup + Merkle binding 10th input + lifecycle + accessor `stm_sync_xattr_index`) + uberblock layout (format break v25 → v26) + Merkle root chain (10th csum) |
| **P8-POSIX-7** | Full modern POSIX surface (no hedged subset; per ROADMAP §11.1's commitment to complete coverage). Subdivides into 7a/7b/... at impl time: **(7a)** `O_TMPFILE` + every `F_SEAL_*` flag (`SEAL`, `SHRINK`, `GROW`, `WRITE`, `FUTURE_WRITE`) + `statx(2)` shape with btime + nanosecond timestamps. **(7b)** Every `FALLOC_FL_*` flag (`KEEP_SIZE`, `PUNCH_HOLE`, `COLLAPSE_RANGE`, `ZERO_RANGE`, `INSERT_RANGE`, `UNSHARE_RANGE`) — extent-tree manipulation primitives for COLLAPSE/INSERT land alongside; UNSHARE composes with the CAS rehydrate path. **(7c)** `name_to_handle_at(2)` + `open_by_handle_at(2)` over the `(dataset_id, ino, si_gen)` tuple; stale-handle detection via the `inode.tla`-pinned `si_gen` uniqueness invariant. **(7d)** Advisory locking (`flock(2)` + `fcntl(2)` `F_SETLK`/`F_GETLK`/`F_OFD_SETLK`); per-inode lock table with deadlock detection. **(7e)** `posix_fadvise(2)` pass-through to `STM_PROP_TIERING` advisory layer. | composes over inode.tla; `(7d)` introduces a `locks.tla` extension for the per-inode lock table's deadlock-freedom invariant. | inode + dirent + sync |
| **P8-POSIX-7a-statx** *(closed; R81 audit run + applied)* | Clock source + ctime/mtime/btime stamping discipline. Closes the P8-wide R78 P3-3 forward-noted gap: pre-7a, every inode came out of `stm_inode_alloc` with all timestamps zeroed; post-7a, every metadata-changing op auto-stamps ctime to "now" via `clock_gettime(CLOCK_REALTIME)` + every content-changing op auto-stamps mtime + ctime. btime is stamped exactly once at inode creation (POSIX `creation time`; immutable thereafter). The `stm_inode_value` already had ns timestamps + btime slots since P8-POSIX-1b — no format break. New `fs_clock_now_le` helper + `fs_stamp_create_times` / `fs_stamp_mtime_ctime_now` / `fs_stamp_ctime_now` static helpers in fs.c. Stamping wired into: `stm_fs_create_file` / `_mkdir` / `_symlink` (create stamp); `stm_fs_write` (mtime+ctime, all 3 paths INLINE / transition / EXTENT incl. no-grow); `stm_fs_truncate` (mtime+ctime, all 3 paths); `stm_fs_chmod` / `_chown` (ctime; chown only when uid/gid changed); `stm_fs_setxattr` / `_removexattr` (ctime); `stm_fs_link` (ctime on linked inode, post-dirent-alloc-success per R81 P3-1); `stm_fs_unlink` / `_rmdir` (ctime on surviving inode if not cascade-freed); `stm_fs_rename` (ctime on moved inode); `stm_fs_utimens` (always auto-stamps ctime; caller-supplied ctime args ignored per POSIX utimensat). atime-on-read deliberately NOT stamped (Linux noatime default; future per-dataset opt-in possible). `stm_fs_statx` not added as a separate API — `stm_fs_stat` already returns the full 256-byte stm_inode_value with all statx-equivalent fields (statx(2) exit criterion satisfied). **R81 verdict: 0 P0 + 0 P1 + 0 P2 + 8 P3** — GREEN; all addressed inline. **R81 P3-1**: link's ctime stamp moved to AFTER dirent_alloc success (failed link no longer spuriously bumps ctime — POSIX/Linux alignment). **R81 P3-3**: zero-length write short-circuit (no spurious mtime bump on `write(fd, buf, 0)`). **R81 P3-6**: utimens drops ctime_nsec validation since arg is now ignored. **R81 P3-8**: stm_fs_reflink dst-inode stamping forward-note added (deferred to P8-POSIX-10 reflink-wrapper sub-chunk per POSIX copy_file_range(2)). R81 P3-2 (setxattr best-effort stamp) acknowledged theoretical-only. R81 P3-4 (persistence-roundtrip), P3-5 (no-op truncate), P3-7 (test perf) regression tests added. 15 new tests in test_fs.c covering each stamping rule + btime immutability across composite ops + R81 regressions (failed-link / no-op-truncate / zero-len-write / persistence-roundtrip). test_fs 239 → 254. No format break (UB v26 preserved). | composes over inode.tla — no spec extension. | fs (existing) — already on CLAUDE.md trigger list |
| **P8-POSIX-7a-anon** *(closed; R85 audit run + applied)* | Linux O_TMPFILE — anonymous (orphan) inode lifecycle. Three new public APIs: `stm_fs_create_anon` (alloc orphan: nlink=0 + STM_INO_FLAG_ORPHAN set), `stm_fs_linkat_anon` (materialize: install dirent + flip nlink 0→1 + clear ORPHAN flag, atomically under fs->lock), `stm_fs_unlink_anon` (explicit free of orphan via stm_inode_free). New inode-layer APIs: `stm_inode_alloc_anon` + `stm_inode_materialize`. New flag bit `STM_INO_FLAG_ORPHAN = 0x40000000u` (bit 30; sits adjacent to FREED at bit 31 — both internal allocator-state markers). **Spec extension**: `inode.tla` extended with `EnableOrphanModel` flag + `ever_linked` shadow var + 3 new actions (`AllocAnon` / `Materialize` / `FreeAnon`) + reformulated `LinkedAllocatedHasPositiveNlink` (now ALLOCATED + ever_linked → nlink ≥ 1) + new `OrphanHasZeroNlink` invariant (ALLOCATED + ~ever_linked → nlink = 0). Init sets ever_linked = TRUE for every ino so pre-7a-anon configs (EnableOrphanModel=FALSE) keep their pre-existing semantics. New buggy variant `BuggyAllocAnonClaimsLinked` (sets ever_linked=TRUE while leaving nlink=0 — fires LinkedAllocatedHasPositiveNlink). New cfgs: `inode_orphan.cfg` (healthy) + `inode_alloc_anon_claims_linked_buggy.cfg`. Composes over inode.tla; no new module. Decoder + writer-side guard updates (R71 P1-1 / R77 P1-1 lesson — symmetric guards): `in_decode_value` exempts ORPHAN-flagged ALLOCATED records from the FREED⇔nlink≥1 check; `stm_inode_set` exempts orphans from the nlink≥1 writer-guard + enforces the dual ORPHAN⇒nlink=0; `stm_inode_set` ALSO refuses any ORPHAN-bit toggle (orphan-state transitions go through `_alloc_anon` / `_materialize`, not Set). `stm_inode_link` + `stm_inode_unlink` REJECT orphan inputs (ORPHAN flag set → STM_EINVAL) — caller must use materialize / unlink_anon. **No on-disk format break** — the ORPHAN bit lives inside the existing si_flags le32 field. STM_UB_VERSION=26 preserved. 9 new tests in test_inode.c covering alloc-anon-state / materialize / non-orphan refusal / link/unlink refuse orphan / set rejects orphan-bit toggle / set rejects ORPHAN+nlink>0 / alloc-anon-after-free bumps gen / arg validation. 9 new tests in test_fs.c covering create-anon-returns-orphan / linkat-materializes / unlink-anon-frees / linkat refuses non-orphan / linkat refuses EEXIST / persist-across-remount / create-then-write-then-link / arg validation / wedged-refused. test_fs 303 → 313 (substantive 9 + R85 P0-1 regression 1); test_inode 41 → 50. 38 ctest suites green default + ASan. **R85 verdict: 1 P0 + 0 P1 + 1 P2 + 4 P3 — RED, all addressed inline.** **R85 P0-1 (CRITICAL)**: `stm_inode_free` set FREED but DIDN'T clear `STM_INO_FLAG_ORPHAN` — the on-disk record carried FREED+ORPHAN, the load_at decoder rejected it as structurally inconsistent (ORPHAN ⇒ ALLOCATED dual). The headline O_TMPFILE workflow `create_anon → unlink_anon → unmount → remount` returned STM_ECORRUPT — a silent wedge across mount cycles. Fixed by clearing ORPHAN alongside setting FREED in `stm_inode_free`. Regression test `fs_p7a_anon_r85_p0_1_unlink_anon_persists_across_remount` exercises the full workflow + asserts AllocReused works post-remount. **R85 P3-1**: same fix applied to `stm_inode_unlink`'s cascade-free branch (defense-in-depth — today unreachable since `stm_inode_unlink` refuses orphan inputs at entry, but the symmetric clear is the right hygiene). R85 P2-1 (Linux O_TMPFILE divergences: pool-wide vs dir-bound, no O_EXCL, no umount-time reaper) acknowledged in commit; reaper forward-noted to P11. R85 P3-2 (create-stamp `(void)` swallow), P3-3 (spec doesn't model the on-disk witness for `ever_linked`), P3-4 (spec doc consolidation) acknowledged. | **`inode.tla` EXTENDED** — adds `ever_linked` + 3 actions + reformulated `LinkedAllocatedHasPositiveNlink` + new `OrphanHasZeroNlink`. `inode_orphan.cfg` + 1 buggy cfg. Pre-existing 5 cfgs amended with `EnableOrphanModel=FALSE` + `BuggyAllocAnonClaimsLinked=FALSE`. | inode (existing) + fs (existing) — both already on CLAUDE.md trigger list. |
| **P8-POSIX-7c** *(closed; R83 audit run + applied)* | File handles (Linux `name_to_handle_at(2)` + `open_by_handle_at(2)`). **48-byte** opaque `stm_fs_file_handle` struct (post-R83 P2-1: pool_uuid binding added; pre-R83 was 32 bytes): 4-byte magic ('STMH' = 0x484D5453) + 4-byte version (2; bumped from 1 at R83 close) + 16-byte pool_uuid[2] + 8-byte dataset_id + 8-byte ino + 8-byte si_gen. 2 new public APIs: `stm_fs_name_to_handle(fs, ds, parent_ino, name, name_len, *out_handle)` resolves a (parent, name) pair to a serializable handle by reading the child's CURRENT si_gen from the inode index (defense-in-depth — the dirent's child_gen IS provably equal but a redundant lookup catches future invariant violations). `stm_fs_open_by_handle(fs, *handle, *out_ino)` validates magic + version + pool_uuid + (ds, ino, gen) tuple. **R83 P2-1 fix**: handle binds to fs's `pool_uuid` so cross-pool handles refuse with STM_ESTALE (pre-fix, two pools' independent ino allocators could each produce identical (ds=1, ino=N, gen=0) triples — a handle from pool A presented to pool B would silently open a different file). **R83 P2-2 fix**: new `STM_ESTALE = -116` (POSIX-aligned, matches Linux `open_by_handle_at(2)`'s ESTALE) distinguishes "was here, gone now" (pool mismatch / FREED inode / gen mismatch) from STM_ENOENT ("never existed in this pool"). The split lets POSIX-aware callers (NFS, FUSE) route cache-invalidate-and-retry separately from give-up. **R83 P3-2 fix**: magic mismatch returns STM_EINVAL (was STM_EBADTAG; "aead tag mismatch" string didn't fit the structural-validation semantic). **R83 P3-3 fix**: docstring states explicitly that handles are NOT cryptographically authenticated (parallels POSIX `name_to_handle_at` — handles are tuple-encoded identifiers, not signed tokens; authorization is via fs access, not handle contents). **R83 P3-5 fix**: dropped redundant le64 store/load round-trip on si_gen capture (direct assignment is bit-equivalent + saves a byte-swap pair on big-endian builds). **R83 P3-6 fix**: defense-in-depth comment added on the redundant inode lookup explaining why we re-read si_gen rather than trust the dirent's child_gen snapshot (defense-in-depth against future invariant drift, not correctness-load-bearing today). **R83 P3-1 fix**: docstring commits to fixed-size + version-bump approach (no caller-provided buffer/length form). 14 tests in test_fs.c (9 substantive at de3a6b3 + 5 R83 regressions: cross-pool isolation via pool_uuid mismatch / never-existed-vs-stale split / directory handle / symlink handle / gen-monotonicity across 32 cycles / out_ino zero on STM_EBADVERSION). **R83 verdict: 0 P0 + 0 P1 + 2 P2 + 6 P3** — YELLOW; all addressed inline. test_fs 276 → 291 (15 new total, including 1 from cross-pool isolation that exercises 2 separate fs handles). **No format break** — handles are out-of-band wire shapes only, no on-disk surface change. STM_UB_VERSION = 26 preserved. 38 ctest suites green default + ASan. | composes over inode.tla's `TupleUniqueAllTime` — no spec extension. | fs (existing) — already on CLAUDE.md trigger list. |
| **P8-POSIX-7a-seals** *(closed; R82 audit run + applied)* | File seals (Linux memfd_create `F_SEAL_*` surface; POSIX shape via `fcntl(F_ADD_SEALS, F_GET_SEALS)`). 5 new flag bits in `si_flags` (`STM_INO_FLAG_SEAL_SEAL` / `_SHRINK` / `_GROW` / `_WRITE` / `_FUTURE_WRITE` at bits 8..12; reserved bits 3..7 left for future ext-style flags); 2 new public APIs (`stm_fs_add_seals` for sticky additive set + `stm_fs_get_seals` for the inode's current mask); enforcement at the 2 size/content-mutation seams (`fs_write_regular_locked` rejects per `SEAL_WRITE`/`SEAL_FUTURE_WRITE`/`SEAL_GROW`-vs-end_off; `stm_fs_truncate` rejects per `SEAL_WRITE`/`SEAL_FUTURE_WRITE`/`SEAL_GROW` truncate-up / `SEAL_SHRINK` truncate-down — same-size no-op truncate always allowed since it short-circuits before the seal check). **R82 P0-1 fix:** the seal-mask check extends to `stm_fs_reflink` — pre-fix, reflink installed extents on a SEAL_WRITE'd dst inode without checking the seal mask, silently defeating the seal (Linux's kernel reflink path checks `IS_IMMUTABLE` + `F_SEAL_WRITE` and refuses with EPERM; v2 had a parallel gap). Post-fix, reflink looks up dst's `iv` under fs->lock + refuses with `STM_EPERM` if SEAL_WRITE / SEAL_FUTURE_WRITE is set; refuses with STM_EPERM if SEAL_GROW is set AND src has more bytes than dst (would extend dst's si_size). 3 regression tests cover the SEAL_WRITE-blocks-reflink / SEAL_GROW-blocks-reflink / SEAL_SHRINK-allows-reflink branches. New error code `STM_EPERM = -1` (POSIX-aligned for sealed-file rejection — distinct from `STM_EACCES` mode-permission denial and `STM_EROFS` mount-level read-only so 9P/FUSE bindings can route the seal branch separately). Sealing is sticky additive — once `STM_FS_SEAL_SEAL` is set, every subsequent `add_seals` call refuses with `STM_EPERM` (matching Linux behavior on `F_ADD_SEALS` against a SEAL-sealed inode). Idempotent re-add of an already-set bit is `STM_OK` no-op (no ctime bump, no inode_set call); `seals == 0` is also a legal trivial no-op. Successful effective change stamps ctime (matches Linux `kernel/fs/inode.c::file_seals_change`'s `inode_inc_iversion + ctime stamp`). `stm_fs_get_seals` allowed on RO mounts (FS_GUARD_READ); `stm_fs_add_seals` refuses on RO with `STM_EROFS`, on wedged with `STM_EWEDGED`. Uniform out-param contract: `*out_seals` zero-initialized BEFORE arg validation (R57 P3-5 / R58 P3-1 norm). FUTURE_WRITE in MVP behaves identically to WRITE for non-mmap write paths (no mmap surface yet); the bit is published so future mmap support can distinguish "block new writers" from "block all writes" per Linux fcntl(2) semantics. **R82 verdict: 1 P0 + 0 P1 + 3 P2 + 4 P3** — RED, all addressed inline. **R82 P0-1**: `stm_fs_reflink` seal enforcement (above). **R82 P2-1**: `stm_fs_link` returns `STM_EPERM` for hard-link-on-directory (was `STM_ENOTSUPPORTED` pre-R82 because STM_EPERM didn't exist; now closed). **R82 P2-2**: `stm_inode_set` rejects writes that clear seal bits (writer-side guard mirrors decoder-side guards per R71 P1-1 / R77 P1-1 lesson — defense-in-depth against future or test-only paths that build an iv from scratch with cleared seal bits). New regression test `inode_r82_p2_2_set_rejects_clearing_seal_bits` in test_inode.c. **R82 P2-3**: zero-init order in `stm_fs_get_seals` confirmed correct — non-finding. **R82 P3-1** stale comment in `fs_write_regular_locked` rewritten (the seal check is AFTER the zero-length short-circuit, not before; the docstring inversion was left over from an earlier draft). **R82 P3-2**: `si_flags` bit ranges documented in inode.h (bits 0..2 ext flags, 3..7 reserve, 8..12 seals, 13..30 reserved-zero, 31 FREED). **R82 P3-3**: `_Static_assert` drift guards added between `STM_FS_SEAL_*` (fs.h) and `STM_INO_FLAG_SEAL_*` (inode.h) — the two parallel define sets must stay numerically identical or the seal-mask check would let through bits the enforcement seams wouldn't recognize. **R82 P3-4** closed by 3 reflink-seal regression tests above. 22 tests in test_fs.c (19 substantive + 3 R82 P0-1 regressions); 1 new test in test_inode.c (R82 P2-2 regression). test_fs 254 → 276; test_inode 40 → 41. **No format break** — UB v26 preserved (the 5 seal bits live in the existing `si_flags` field which has been there since P8-POSIX-1b at v24). 38 ctest suites green default + ASan. | composes over inode.tla — no spec extension (seals are flag-bit additions to the data state machine; the alloc/free/link/unlink machine is unchanged). The write/truncate refusal is enforced at the fs-layer wrapper which sits above the inode-tree state machine. | fs (existing) + inode (existing) — both already on CLAUDE.md trigger list |
| **P8-POSIX-8** *(closed; R77 audit run + applied)* | Symlinks (inline target only — ≤ STM_INODE_INLINE_MAX = 100 B). New public APIs: `stm_fs_symlink(fs, ds, parent, name, target, target_len, uid, gid, *out_ino)` creates a symlink inode with `mode = S_IFLNK \| 0777` (POSIX convention; symlink permission bits unused), stamps `si_data_kind = STM_DATA_SYMLINK` + `si_data_len = target_len` + `si_data.symlink_target` bytes, links in parent dirent with `STM_DT_LNK`. `stm_fs_readlink(fs, ds, ino, target_buf, target_max, *out_len)` validates the inode is `S_IFLNK` (else STM_EINVAL per POSIX `readlink(2)`), copies up to `target_max` bytes, returns full target length via `*out_len` so callers can re-call with a larger buffer if truncated. New error code `STM_ENAMETOOLONG = -36` (POSIX-aligned) for target > 100 B refusal. Long-symlink (extent-backed) support deferred — would compose with P8-POSIX-5's inline → extent transition path; symlinks on Linux are typically < 60 bytes (full path + filename), so the inline-only MVP covers > 95% of real-world cases. **R77 verdict: 0 P0 + 1 P1 + 1 P2 + 2 P3** — YELLOW; all addressed inline. **R77 P1-1: stm_fs_readlink OOB stack read on corrupt si_data_len** — neither `in_decode_value` (decoder) nor `stm_inode_set` (writer) bounded `si_data_len ≤ STM_INODE_INLINE_MAX` for INLINE/SYMLINK kinds; a tampered on-disk record carrying `si_data_kind=STM_DATA_SYMLINK` + `si_data_len=200` would load cleanly and `stm_fs_readlink` would memcpy 200 bytes from `iv.si_data.symlink_target[100]` — leaking up to 156 bytes of stack to caller-controlled buffer. Same R71 P1-1 shape (writer-side / decoder-side guards must mirror reader assumptions). Fixed at the canonical sites: decoder-side guard in `in_decode_value` + writer-side guard in `stm_inode_set` (both reject `si_data_len > STM_INODE_INLINE_MAX` for INLINE/SYMLINK with STM_ECORRUPT/STM_EINVAL). Defense-in-depth caps added in `fs_write_regular_locked` + `fs_read_regular_locked` (INLINE) + `stm_fs_readlink` (SYMLINK) — STM_ECORRUPT if any future bypass leaks an oversize record past the boundary guards. R77 P2-1 (regression test) closed by `fs_p8_r77_p1_1_inode_set_rejects_oversize_si_data_len`: allocates valid SYMLINK + REG inodes, mutates iv.si_data_len to 200, attempts stm_inode_set → STM_EINVAL (writer-guard catches); confirms the cap (100) is accepted. R77 P3-1 (zero-init order drift) closed by moving `*out_child_ino = 0;` BEFORE arg validation in stm_fs_symlink. R77 P3-2 (ENAMETOOLONG vs ERANGE doc) closed by header docstring tightening. test_fs 199 → 207 (7 P5 substantive + 1 R77 regression). No format break — STM_UB_VERSION=25 preserved (existing `si_data.symlink_target[100]` slot has been there since P8-POSIX-1b at v24). 37 ctest suites green. test_fs ctest timeout bumped 60s → 180s in `tests/CMakeLists.txt` since the suite now runs ~40s wall under `-j2`. | composes over inode.tla — no spec extension (the symlink kind doesn't transition in MVP scope; no load-bearing invariants beyond what stm_inode_alloc + stm_inode_set already pin) | fs (existing) + inode (existing) — both already on CLAUDE.md trigger list |
| **P8-POSIX-7b** *(closed; R89 audit pending)* | Linux fallocate(2) — every FALLOC_FL_* flag. New public API `stm_fs_fallocate(fs, ds, ino, off, len, flags)`. Constants match Linux `<linux/falloc.h>` verbatim (KEEP_SIZE=0x01 / PUNCH_HOLE=0x02 / COLLAPSE_RANGE=0x08 / ZERO_RANGE=0x10 / INSERT_RANGE=0x20 / UNSHARE_RANGE=0x40; 0x04 reserved-zero per Linux's defunct NO_HIDE_STALE). **Spec extension**: extent.tla extended with 3 new actions — `PunchHole(ds, ino, off, len)` (drop fully-contained extents in range; precondition: no crossing-boundary extents); `CollapseRange(ds, ino, off, len)` (shift extents at off' >= off+len down by len; precondition: range empty + no crossing); `InsertRange(ds, ino, off, len)` (shift extents at off' >= off up by len; precondition: no crossing). Composes with existing `NoOverlapWithinIno` + `LengthPositive` + `PaddrFreshness` invariants — no new invariants needed. UNSHARE_RANGE composes over `cas.tla::RehydrateOnWrite` (no extent.tla extension); ZERO_RANGE composes over `Overwrite` (no extension); KEEP_SIZE is metadata-only (no extension). **Two new extent-tree primitives**: `stm_extent_punch_range` (drop fully-contained extents in [off, off+len); refuses STM_ENOTSUPPORTED on crossing extent — partial-extent punch not in MVP); `stm_extent_shift_range_keys(idx, ds, ino, cutoff, signed delta)` (in-place key shift for COLLAPSE/INSERT — works on records[] without rebuilding the btree; persistence happens at next sync_commit). **Flag-combination matrix** (Linux-aligned): PUNCH_HOLE requires KEEP_SIZE; COLLAPSE / INSERT cannot combine with any other flag; UNSHARE / ZERO_RANGE may combine with KEEP_SIZE only; at most one of {PUNCH/COLLAPSE/ZERO/INSERT/UNSHARE} per call. **Block-alignment**: every range op requires 4 KiB-aligned off + len (stratum doesn't split extents at byte boundaries; sub-block ops refuse with STM_EINVAL). **Seal enforcement**: SEAL_WRITE / SEAL_FUTURE_WRITE block any fallocate; SEAL_GROW blocks default-preallocate / INSERT / ZERO-without-KEEP_SIZE; SEAL_SHRINK blocks COLLAPSE. **MVP scope cuts**: PUNCH_HOLE refuses STM_ENOTSUPPORTED on partial-extent overlap (full block-aligned only); COLLAPSE refuses STM_ENOTSUPPORTED if range non-empty (caller must punch first; the combined op is per Linux behavior 2-step at the user level too); UNSHARE_RANGE composes via `stm_fs_promote_to_hot` which promotes EVERY COLD extent at the ino (broader than POSIX requires for partial range — correct + conservative). **No format break** — UB v26 preserved. 13 new tests in test_fs_phase8.c (default-preallocate / KEEP_SIZE-alone / PUNCH_HOLE-drops / PUNCH-requires-KEEP_SIZE-EINVAL / COLLAPSE-shifts / INSERT-shifts / ZERO-drops / UNSHARE-promotes / COLLAPSE-with-other-flags-EINVAL / block-alignment / arg validation / wedged-refused / SEAL_WRITE-blocks). test_fs_phase8 191 → 204. 39 ctest suites green default + ASan in isolation. R89 audit pending. | **`extent.tla` EXTENDED** — 3 new actions (PunchHole / CollapseRange / InsertRange). Composes existing invariants. | extent (existing) + fs (existing) — both already on CLAUDE.md trigger list. |
| **P8-POSIX-9b WHITEOUT** *(closed; R88 audit run + applied)* | renameat2 RENAME_WHITEOUT — atomic rename PLUS leave a whiteout marker at the SOURCE name (overlayfs convention; Linux DT_WHT). New `STM_FS_RENAME_WHITEOUT = 0x04u` flag (mutually exclusive with EXCHANGE; combinable with NOREPLACE per Linux). New flag bit `STM_DIRENT_FLAG_WHITEOUT = 0x02u` distinct from TOMBSTONE. New `STM_DT_WHITEOUT = 14u` (matches Linux DT_WHT). New dirent-layer primitive `stm_dirent_whiteout(idx, ds, dir, name, name_len)` converts a live record to a whiteout slot in-place: name PRESERVED + child_ino=0 + child_type=STM_DT_WHITEOUT + flags=WHITEOUT. **Spec extension**: dirent.tla extended with `EnableWhiteoutModel` flag + new `Whiteout(d, name)` action + `WhiteoutRec(n)` slot kind + `IsWhiteout(s)` predicate + `WhiteoutSlotShape` invariant + `ReaddirEmitsWhiteoutsAtEnd` invariant. LookupWalk + FirstInstallSlot + ReaddirStep updated. New cfgs: `dirent_whiteout.cfg` (healthy), `dirent_lookup_returns_record_on_whiteout_buggy.cfg` (fires LookupAgreesWithLinks), `dirent_whiteout_drops_name_buggy.cfg` (TLC-GREEN-by-design — the spec doesn't pin readdir-emission at the abstract slot level; runtime-tested via C impl tests). Pre-existing 7 dirent cfgs amended with EnableWhiteoutModel=FALSE + 2 new buggy=FALSE. Whiteout runtime semantics: lookup of a whiteout name → STM_ENOENT; Create on a SAME-NAME whiteout overwrites the slot with a live record (overlayfs "promote NAME from lower to upper layer"); Create on a DIFFERENT-name whiteout PRESERVES the marker (R88 P2-1 fix — chain integrity); Unlink of a whiteout-name → STM_ENOENT; count_for_dir excludes whiteouts; readdir EMITS whiteouts. On-disk: 4 slot kinds (live, tombstone, whiteout, EMPTY); decoder rejects malformed combinations (TOMBSTONE+WHITEOUT mutually exclusive; live records refuse type=STM_DT_WHITEOUT). fs.c rename WHITEOUT branch: replaces `stm_dirent_unlink(src)` with `stm_dirent_whiteout(src)` post-`stm_dirent_alloc(dst, src_ino)`; ctime stamped on moved inode. **R88 verdict: 0 P0 + 0 P1 + 1 P2 + 6 P3 — GREEN, all addressed inline.** **R88 P2-1 (CRITICAL spec+impl bug)**: `stm_dirent_alloc`'s chain walk treated EVERY whiteout (regardless of name) as install candidate; under hash collision (or via the test seam) a different-named alloc would silently overwrite the whiteout marker, breaking RENAME_WHITEOUT's persistence contract that overlayfs relies on. Both impl AND `dirent.tla::FirstInstallSlot` had the bug in lockstep (spec-first didn't catch it). Fixed by gating whiteout install-eligibility on name match — only same-name whiteouts are install candidates; different-name whiteouts are walked past as occupying slots (preserving the marker). New test seam `stm_dirent_install_at_probe_for_test` (gated by STRATUM_BUILD_TESTING_HOOKS, same pattern as R46 P2-1's `stm_snapshot_create_for_test`) bypasses the chain walk to construct exact slot configurations for the regression test. R88 P3-1 (dirent.h on-disk layout doc) updated with WHITEOUT column + bit-1 mutual exclusion documentation. R88 P3-2 (dirent specs not in CI matrix) forward-noted to P11 — pre-existing CI gap not introduced by P9b. R88 P3-3 (dirent_whiteout_drops_name_buggy GREEN-by-design) acknowledged in the cfg's comment block — runtime tests pin readdir-emission directly. R88 P3-4 (directory-inode WHITEOUT regression test added). R88 P3-5 (cross-directory WHITEOUT regression test added). R88 P3-6 (rmdir-drops-whiteouts regression test added). 2 new test_dirent.c regressions (R88 P2-1 whiteout-preserved-under-hash-collision + R88 same-name overwrite under collision); 3 new test_fs_phase8.c regressions (R88 P3-4/P3-5/P3-6). test_dirent 34 → 44; test_fs_phase8 178 → 191. **No format break** — UB v26 preserved. dirent.tla EXTENDED + R88 P2-1 fix lands in lockstep with C impl. 39 ctest suites green default + ASan + TSan in isolation. | **`dirent.tla` EXTENDED** — 1 new action + 1 new slot kind + 2 new invariants + 1 healthy cfg + 2 buggy cfgs. R88 P2-1 spec fix in `FirstInstallSlot` mirrors C impl. | dirent (existing) + fs (existing) — both already on CLAUDE.md trigger list. |
| **P8-POSIX-9b** *(closed; R86 audit run + applied; RENAME_WHITEOUT still deferred)* | **R86 verdict: 0 P0 + 0 P1 + 2 P2 + 4 P3 — GREEN.** All P2 are pre-existing P9 gaps (R86 P2-1 directory-cycle detection on EXCHANGE; R86 P2-2 parent-dir mtime/ctime not stamped) — neither introduced by P9b but reachable via the new dir/file kind-bypass branch; both forward-noted to P11 alongside xfstests prep. **R86 P3-1**: spec-impl gap doc — dirent.tla::Swap requires `~iter_active`; the C impl substitutes the index mutex (readdir takes the same mutex). Doc-comment added to `stm_dirent_swap_two`. **R86 P3-2** + **R86 P3-3**: closed by 2 new regression tests (chain-collision swap with 16 siblings; hardlinks-to-same-ino swap is observational no-op + nlink unchanged). R86 P3-4 (best-effort ctime stamp doc tightening) acknowledged cosmetic. test_fs_phase8 163 → 165. `renameat2(2)` `RENAME_EXCHANGE` flag — atomically swap two dirents. New `STM_FS_RENAME_EXCHANGE = 0x02u` flag (mutually exclusive with `STM_FS_RENAME_NOREPLACE` — combination → STM_EINVAL). New dirent primitive `stm_dirent_swap_two(idx, ds, dir1_ino, name1, dir2_ino, name2)` performs the in-place (ino, gen, type) swap on both slots; chain integrity preserved by construction (slot positions + names + flags unchanged). Same-dir + cross-dir cases unified at both spec + impl levels. **Spec extension**: `dirent.tla` extended with `Swap(d1, name1, d2, name2)` action — atomically swaps the (ino, gen) values at the two slots; preconditions guard `~iter_active` on both dirs (P8-POSIX-4 stable-iteration model) + reject self-swap. No buggy variant for Swap (the chain-integrity invariants the spec already pins are preserved by construction; a half-applied swap doesn't satisfy TLA+'s atomic-action semantics). Wire-up in `stm_fs_rename`: new branch BEFORE the existing dst-replace path; refuses if dst doesn't exist (STM_ENOENT). Stamps ctime on BOTH swapped inodes (POSIX rename ctime semantics). The exchange branch BYPASSES the kind-mismatch check (Linux `renameat2(EXCHANGE)` allows swapping a regular file with a directory because no inode is being deleted — verified via `fs_p9b_rename_exchange_dir_with_file_swaps_kinds`). Composes over inode.tla — no spec extension on the inode side (no nlink change, no gen bump). **No format break** — STM_UB_VERSION = 26 preserved. 6 new tests in test_dirent.c covering: same-dir swap / cross-dir swap / swap is self-inverse / self-swap refused / missing-either returns ENOENT / arg validation. 9 new tests in test_fs.c covering: file/file swap / cross-directory / dir/file kind-bypass / dst-missing or src-missing → ENOENT / NOREPLACE+EXCHANGE refused / self-rename no-op / both ctimes stamped / persists across remount / unknown-flag refused. test_dirent 28 → 34; test_fs 313 → 322. 38 ctest suites green default + ASan. **RENAME_WHITEOUT still deferred** — overlayfs-specific; would need a new STM_DIRENT_FLAG_WHITEOUT distinct from TOMBSTONE. | **`dirent.tla` EXTENDED** — adds `Swap(d1, name1, d2, name2)` action. No new buggy cfg (chain integrity preserved by construction). | dirent (existing) + fs (existing) — both already on CLAUDE.md trigger list. R86 audit pending. |
| **P8-POSIX-9** *(MVP closed; R79 audit run + applied; RENAME_EXCHANGE + RENAME_WHITEOUT deferred)* | `stm_fs_rename(fs, ds, src_parent, src_name, dst_parent, dst_name, flags)` — atomic rename. Composes `dirent_alloc(dst → src_ino) + dirent_unlink(src) + (overwrite path) dirent_unlink(dst) + inode_unlink(dst-prior, &freed)` under fs->lock so the next sync_commit flushes them as a unit. MVP: basic rename + `STM_FS_RENAME_NOREPLACE` flag. Refusals: name validation, ds/parent_ino == 0, unknown flag bits, parent not S_IFDIR (STM_ENOTDIR), src missing (STM_ENOENT), dst exists with NOREPLACE (STM_EEXIST), kind mismatch (file→dir → STM_EISDIR; dir→file → STM_ENOTDIR), dir→non-empty-dir (STM_ENOTEMPTY), wedged/RO (STM_EWEDGED/STM_EROFS). Same-path no-op returns STM_OK. Best-effort rollback on alloc / unlink failure with stm_fs_mark_wedged on rollback failure. **R79 verdict: 0 P0 + 0 P1 + 1 P2 + 2 P3** — GREEN; all addressed inline. R79 P2-1 (hardlink-rename test gap) closed by `fs_p9_r79_p2_1_rename_hardlink_same_inode_consistent` — pins that `rename(a, b)` when `a` and `b` are hardlinks (nlink=2) to the same inode produces the POSIX-correct outcome (a gone, b preserved → ino, nlink=1). R79 P3-1 (defense-in-depth on rollback's freshly-installed-dst-unlink rv) closed by capturing the rv + feeding it into the wedge guard. R79 P3-2 (misleading comment about which slot dirent_alloc reuses) closed by tightening to clarify the chain probes deterministically by hash and either install slot is correct. test_fs 217 → 227 (9 P9 substantive + 1 R79 regression). No format break — STM_UB_VERSION=25 preserved. **RENAME_EXCHANGE + RENAME_WHITEOUT deferred** to follow-up sub-chunks (EXCHANGE needs a new dirent atomic-two-slot-swap primitive; WHITEOUT is rare). | composes dirent.tla — no spec extension for MVP (rename = atomic Unlink+Create composition; existing chain-integrity invariants cover. RENAME_EXCHANGE follow-up will require a new dirent.tla action `Swap(d, n1, n2)` with corresponding `SwapAtomicity` invariant) | fs (existing) — already on CLAUDE.md trigger list |
| **P8-POSIX-10b** *(closed; R84 audit run + applied)* | `stm_fs_copy_file_range(2)` POSIX-shape wrapper over `stm_fs_reflink` + R81 P3-8 dst-stamping fix. MVP scope: WHOLE-FILE copy only; non-zero offsets or partial lengths refuse with `STM_ENOTSUPPORTED` so the binding layer can fall back to read+write loops. `len == 0` is a legal POSIX no-op. **R81 P3-8 fix landed**: on successful `sync_reflink`, `stm_fs_reflink` now stamps dst's mtime + ctime to "now" + aligns dst's si_size to src's si_size + flips dst's si_data_kind to STM_DATA_EXTENT if src had any extent records. Stamping fires only when dst has an inode in the index — legacy direct-extent callers preserve their pre-7a behavior. **R84 verdict: 1 P0 + 0 P1 + 2 P2 + 4 P3** — RED, all addressed inline. **R84 P0-1 (CRITICAL)**: SYMLINK-source reflink silently fabricated zero bytes on dst (sync_reflink found no extents on a SYMLINK src → dst stayed empty + post-success stamping wrote `si_size=src_size` with `si_data_kind=INLINE` + `si_data_len=0`; subsequent read returned `src_size` zero bytes — pure data fabrication, reachable via the public API). Same shape latent for STM_DATA_DEVICE if size > 0. Fixed by generalizing the inline-source guard to refuse ANY non-EXTENT src with non-zero size — catches INLINE, SYMLINK, DEVICE, and any future non-EXTENT kind. Regression test `fs_p10b_r84_p0_1_symlink_source_refused`. **R84 P2-1**: `stm_fs_copy_file_range`'s size-probe TOCTOU: pre-fix, cfp dropped fs->lock between size validation + the inner reflink call, allowing src to grow between probe and reflink (out_copied < dst's actual size). Fixed by introducing static `fs_reflink_locked` helper that runs the entire pipeline under the caller's fs->lock; cfp now holds fs->lock continuously through both size-validation lookup AND fs_reflink_locked. **R84 P2-2**: reflink ignored si_mode — directory / symlink / device dst would receive si_size=src_size + INLINE-empty stamping (orthogonal corruption path). Fixed by adding `S_ISREG` checks on BOTH src and dst inodes (returns STM_EINVAL — matches Linux FICLONE semantics). Regression tests `fs_p10b_r84_p2_2_directory_dst_refused` + `fs_p10b_r84_p2_2_symlink_dst_refused`. **R84 P3-1**: src_has_inode was conditioned on dst_has_inode in the substantive — legacy mixed-state callers (legacy dst + inode-aware src) would skip the inline-source guard. Fixed by hoisting both lookups to be unconditional + applying the non-EXTENT guard regardless of dst_has_inode. **R84 P3-2** forward-noted: when src_has_inode is FALSE (legacy direct-extent src) but sync_reflink installs records on dst, dst's si_data_kind stays at its loaded value (typically INLINE) instead of flipping to EXTENT. Today no production callsite mixes legacy src + indexed dst; future fix would query stm_extent_count_for_ino post-success. **R84 P3-3** acknowledged cosmetic. **R84 P3-4** closed by 4 new coverage tests (symlink-src / dir-dst / symlink-dst / SEAL_FUTURE_WRITE-solo). 12 new tests in test_fs.c (8 substantive + 4 R84 regressions). test_fs 291 → 303. **No format break** — UB v26 preserved. 38 ctest suites green default + ASan. | composes inode.tla — no spec extension (inode-state transitions are already pinned by P8-POSIX-5's `OneWayInlineToExtent` + `DataKindMatchesState`; reflink-stamping is just a metadata write on dst's existing ALLOCATED record). | fs (existing) — already on CLAUDE.md trigger list. |
| **P8-POSIX-10** *(truncate-half closed; R78 audit run + applied; reflink-wrapper deferred)* | `stm_fs_truncate(fs, ds, ino, new_size)` — POSIX-shape truncate(2) wrapper composing the inode kind dispatch (P8-POSIX-5) with `stm_sync_truncate` (P7-9). Six paths: INLINE-shrink (just update si_data_len + si_size); INLINE-grow within inline cap (zero-fill the gap, stays inline); INLINE-grow past cap (transition via combined-buffer write — same shape as fs_write_regular_locked's transition path); EXTENT-shrink (delegate to sync_truncate, requires 4 KiB-aligned new_size, stays EXTENT per ARCH §11.3.3 / inode.tla::OneWayInlineToExtent); EXTENT-grow (just update si_size, sparse extent semantics zero-fill the [old_size, new_size) range on read); no-op (new_size == cur_size). Refusals: directory → STM_EISDIR (POSIX); non-S_IFREG → STM_EINVAL; sub-block EXTENT-mode truncate → STM_EINVAL; oversize transition → STM_ERANGE. R77 P1-1 defense-in-depth: rejects corrupt si_data_len > STM_INODE_INLINE_MAX before any memcpy. **R78 verdict: 0 P0 + 0 P1 + 0 P2 + 3 P3** — GREEN; P3-1 (lock-posture infallibility annotations on the 3 stm_inode_set call-sites) closed inline; P3-2 (truncate-then-write composition coverage gap) closed by 2 new regression tests (`fs_p10_r78_p3_2_truncate_then_write_within_inline` + `fs_p10_r78_p3_2_truncate_extent_then_write_inside_no_size_regress` — the latter pins that overwriting an EXTENT file's earlier blocks via fs_write doesn't regress si_size when prior truncate-grow extended it); P3-3 (no-op path skips ctime/mtime stamp) forward-noted as a P8-wide gap to land at the eventual statx clock integration in P8-POSIX-7. test_fs 207 → 217 (8 P10 substantive + 2 R78 regressions). No format break — STM_UB_VERSION=25 preserved. **`copy_file_range` reflink wrapper deferred** — `stm_fs_reflink` (P7-16) already provides POSIX FICLONE shape at the fs.c layer; the P8-POSIX-10 row's "reflink wrapper" intent was inode-level validation + better integration with the inode-aware fs_write/fs_read paths, which can land as a follow-up sub-chunk once xfstests reveals the gaps. | composes inode.tla — no spec extension (the truncate state-machine is `data_kind` transitions already pinned by inode.tla's P8-POSIX-5 invariants — `OneWayInlineToExtent`, `InlineLenBounded`, `DataKindMatchesState`) | fs (existing) — already on CLAUDE.md trigger list |
| **P8-POSIX-7e** *(closed; R87 audit run + applied)* | posix_fadvise(2) pass-through. New public API `stm_fs_fadvise(fs, ds, ino, off, len, advice)` — Linux POSIX_FADV_*-shape advisory hints. MVP scope: **per-inode granularity** (`off` and `len` accepted but IGNORED — the hint applies to the WHOLE file, an OVERSCOPE relative to POSIX's range semantics; future per-extent fadvise would compose over primitives that don't exist today). 6 known advice values mapped: NORMAL/SEQUENTIAL/RANDOM/NOREUSE → no-op STM_OK (stratum has no userspace page cache to bias by access pattern); WILLNEED → best-effort delegate to `stm_fs_promote_to_hot`, swallow inner status (advisory contract: `posix_fadvise(2)` doesn't return errors for advisory failures); DONTNEED → best-effort delegate to `stm_fs_migrate_to_cold`, swallow inner status. STM_FS_FADV_* constants match the **GENERIC** Linux UAPI — every Linux arch except s390 (R87 P2-1: s390's `arch/s390/include/uapi/asm/fadvise.h` defines POSIX_FADV_DONTNEED=6 + POSIX_FADV_NOREUSE=7, a historic 32-vs-64-bit ABI quirk; an s390 binding MUST translate before calling fadvise). **Refusals (the only paths returning non-OK)**: NULL fs / dataset_id == 0 / ino == 0 → STM_EINVAL; unknown advice → STM_EINVAL (forward-compat lock); wedged at front-check → STM_EWEDGED. **No existence check** — `posix_fadvise(2)` doesn't validate file contents, and stratum's legacy direct-extent files have no inode-index entry but are valid fadvise targets. **RO mounts**: the wedged check passes (FS_GUARD_READ allows RO); WILLNEED/DONTNEED's swallow then converts the inner STM_EROFS to STM_OK transparently. **Cost amplification (R87 P2-2)**: WILLNEED/DONTNEED triggers a full per-extent decrypt+re-encrypt+rewrite cycle — `O(file_size / chunk_len)` AEAD operations + bdev writes per call. Bindings exposing fadvise to unprivileged callers MUST add per-uid rate-limiting; without it, an adversary calling `fadvise(WILLNEED); fadvise(DONTNEED);` in a loop on a large file gets a CPU+IO amplification primitive against the host pool. Documented in fs.h. **R87 verdict: 0 P0 + 0 P1 + 2 P2 + 7 P3** — GREEN; both P2 + every actionable P3 addressed inline. P2-1 (s390 portability): docstring scoped to the generic Linux ABI + s390 quirk documented + binding-layer translation responsibility called out. P2-2 (DoS amplification): docstring tightened to require per-uid throttling at the binding layer. P3-1 (range-overscope): documented. P3-2 (wedged race window): documented (STM_OK means "not wedged at front-check time", not "non-wedged for the duration"). P3-3 (wedged test coverage): extended the wedged test to all 6 advice values. P3-4 (RO + pre-existing COLD): added `fs_p7e_r87_p3_4_fadvise_ro_mount_with_preexisting_cold_swallows`. P3-5 (no fault-injection harness): doc-only acknowledgment near the `(void)rc` lines — the swallow shape is self-evident, future status-dependent refactor MUST land with a fault-injection regression. P3-6 (cross-feature): added `fs_p7e_r87_p3_6_dontneed_on_sealed_file_does_not_violate_seal` (verifies SEAL_WRITE is preserved across migrate, user writes still refuse) + `fs_p7e_r87_p3_6_willneed_preserves_xattrs_and_hardlinks` (nlink + dirent + xattr survive promote). P3-7 (TSan): test_fs_phase8 ran clean under TSan after R87 fixes. test_fs_phase8 175 → 178 (10 substantive + 3 R87 P3 regressions; the wedged test was extended in place, not added). No format break — STM_UB_VERSION = 26 preserved. 39 ctest suites green default + ASan + TSan in isolation. | composes over P7-CAS-7/11's promote/migrate primitives — no spec extension (advisory hints don't change cas.tla's load-bearing invariants). | fs (existing) — already on CLAUDE.md trigger list. |
| **P8-POSIX-11** | Tests + audit + xfstests integration prep. Property-based tests for the file/dir/xattr/ACL layer. Regression suite for ARCH §11 corner cases. | — | full P8 surface |

## Exit criteria (ROADMAP §11.2)

- [ ] All POSIX file/dir ops produce semantically correct results
      matching ext4/XFS/APFS for a representative test corpus.
- [ ] Hash-collision chain works correctly with ≥1k dirents per
      directory.
- [ ] Hard link nlink remains correct under concurrent create/unlink.
- [ ] Inline-to-extent transition recovers correctly across crash
      boundaries.
- [ ] `statx` returns nanosecond timestamps + btime.
- [ ] POSIX ACL roundtrip preserves grants/denies bit-exact.
- [ ] `inode.tla` + `dirent.tla` (+ likely `xattr.tla`) pin the
      load-bearing invariants under TLC.
- [ ] xfstests subset (file/dir/xattr/ACL portion) green on
      Stratum-backed mount once Phase 9 (FUSE) lands; in Phase 8
      the equivalent verification runs against the `stm_fs_*` API
      directly.

## Posture going in

Inheriting from Phase 7 exit (`1759caf` + P8-NS-1 spec at `bea7f82`):

| | |
|---|---|
| STM_UB_VERSION | 23 (last bumped at P7-CAS-16). Phase 8 will likely bump for the inode tree on-disk format addition (probably v23 → v24 at P8-POSIX-1). |
| STM_SEND_VERSION | 3 |
| Spec posture | 22 modules / 26 fixed cfgs / 38 buggy cfgs (after P8-NS-1's namespace.tla landed). Phase 8 adds `inode.tla`, `dirent.tla`, probably `xattr.tla` — projected 24-25 modules at Phase 8 exit. |
| ctest | 35 suites green default + ASan + TSan |
| test_fs | 159 |
| test_send_recv | 40 |
| Audits closed | R0 — R68 |

## Format break expectations

P8-POSIX-1 will add a NEW per-dataset on-disk surface (the inode
tree). This is a format break — STM_UB_VERSION 23 → 24. v23 pools
won't have an inode tree; mounting them on v24 would need either
a migration step or a refusal at mount. Probably refusal at
mount with STM_EBADVERSION (mirroring P7-CAS-16's pattern), since
v23 → v24 represents adding a load-bearing structural layer
rather than extending an existing record shape.

Format-break signoff required per CLAUDE.md before P8-POSIX-1
substantive lands.

## Operational notes

- Spec-first (CLAUDE.md): inode and dirent invariants are
  load-bearing. `inode.tla` lands BEFORE P8-POSIX-1 substantive,
  `dirent.tla` lands BEFORE P8-POSIX-2 substantive.
- Audit-per-change: `src/inode/` and `src/dirent/` join the
  CLAUDE.md trigger list as soon as the first chunk lands. The
  v1 `src/p9/p9.c` audit pattern (R6, R12 specifically) carries
  forward — silent dirent-tree corruption is the canonical
  failure mode here.
- Memory primer roll at every chunk close: MEMORY.md index +
  project_v2_active.md tip / lineage table + project_v2_next_session.md.
