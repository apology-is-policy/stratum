# Phase 5 — status and pickup guide

Authoritative pickup guide for Phase 5 (multi-device + redundancy).
Last update 2026-04-22 after P5-2 landed. Companion to
`phase4-status.md`, which documents the crypto/integrity layer
Phase 5 builds on.

## TL;DR

Phase 5 = ARCH §4 (storage pool model) + §5 (superblock and quorum)
+ ROADMAP §8. Converts Stratum from single-device to N-device pools
with declared redundancy. Commit semantics lift from single-device
four-phase to multi-device three-phase-quorum per ARCH §5.6. First
redundancy profile is `mirror(n)`; RS + LRC erasure coding deferred
to a later sub-chunk.

| Commit | What | Tests |
|---|---|---|
| `25d7c4a` | **R20 (P5-5-α scope) audit close: 0 P0 + 0 P1 + 3 P2 + 7 P3**. Audit clean at critical levels — the pre-audit self-found fix (`86a71ec`, pool.rdlock around scrub's pointer-returning pool reads) absorbed what would have been the P1. R20 batch addresses: P2-1 (`cursor_start_block >= (1<<48)` boundary trips alloc's EINVAL guard, wedging scrub) + P2-2 (`STM_ECORRUPT` from alloc cursor wedges scrub) — unified fix: `stm_scrub_step` now treats any non-OK-non-ENOENT return from `stm_alloc_first_allocated_from` as "skip this device, advance cursor to next"; `STM_ECORRUPT` additionally bumps `ranges_processed` as a symbolic "tree unparseable" signal. P2-3 (spec's `CompletedIffDrained` vs impl's "all ONLINE/EVACUATING swept" semantic drift) — docstring clarified in `scrub.h`: COMPLETED means the scrub finished every ONLINE+EVACUATING device; FAULTED+REMOVED skipped by design, degraded coverage is operator responsibility (β will add a `devices_skipped` counter). P3-5 (lifetime docstring gap) — `scrub.h` now states attached allocs must outlive scrub OR be detached before close. P3-1 (no FAULTED-device skip test) — new `scrub_skips_faulted_devices` test on 2-device mirror(2) pool: reserve_mirror, commit, fail_device(1), scrub → asserts ranges_processed=1 (not 2), blocks_verified=4 (not 8), blocks_failed=0. P3-7 (pause-refuses-from-COMPLETED missing) — extended `scrub_pause_refuses_non_running` with COMPLETED branch. **Deferred (γ-scope / future)**: P3-2 direct unit tests for `stm_alloc_first_allocated_from`, P3-3 concurrent scrub+pool-mutation stress, P3-4 step latency throttling, P3-6 spec extension for device-aware cursor. No spec changes — all fixes are impl/docs/tests. | test_scrub +1 (17 total). 28 suites × default + ASan + TSan green. 13/13 specs clean; 6 buggy configs still fire. |
| `ee22b96` | **P5-5-α: scrub verify-only sweep + in-RAM state machine**. Spec-first: new `v2/specs/scrub.tla` + `scrub.cfg` + `scrub_buggy.cfg`. Four-state machine IDLE / RUNNING / PAUSED / COMPLETED with transitions Start / Restart / Reset / Pause / Resume / StepClean / StepCorrupt / Complete. Invariants: TypeOK, StateMachineValid, CursorBounded, ProcessedCount (verified + failed = cursor), CompletedIffDrained, IdleMeansZero, PauseResumeIdempotent (cursor ≥ snapshot_cursor across Pause/Resume), SnapshotPinnedWhilePaused, NoWorkWhenIdleOrCompleted. Fixed config (BuggyResume=FALSE): 19 states clean at depth 8. Buggy config (BuggyResume=TRUE, models "Resume zeros cursor"): PauseResumeIdempotent VIOLATED at State 5 with 5-step trace (Init → Start → StepClean(1) → Pause → Resume; post-Resume cursor=0 < snapshot_cursor=1). Impl: new `include/stratum/scrub.h` + `src/scrub/scrub.c`. Handle owns internal mutex; state transitions take strict state guards (start from IDLE or COMPLETED; pause from RUNNING only; resume from PAUSED only; reset from COMPLETED only). `stm_scrub_step` processes one alloc-tree range per call: reads each block via `stm_bdev_read`, counts OK/fail into blocks_verified/blocks_failed, advances per-device cursor past the range (inclusive lower bound); when one device drains, advances cursor_device_id. ONLINE + EVACUATING devices scanned; FAULTED + REMOVED skipped (matches mirror_read). Cursor drained → RUNNING → COMPLETED. No repair, no durable cursor (β / γ). **Supporting changes**: `stm_alloc_first_allocated_from(a, min_start_block, ...)` returns first refcount≥1 entry at or after cursor (passes lo_key to btree_mt_scan; inclusive lower bound). `stm_sync_pool(s)` + `stm_sync_alloc(s, dev)` accessors expose borrowed pointers to scrub under sync's lock. Caveats / deferred: no per-block csum check (no bptr layer; P6 unlock) — α is "readability" scrub. Durable pause/resume across mount is P5-5-γ. IO throttling + per-device parallelism (ARCH §7.14.3 / §12.7.1) is post-α. | test_scrub +16 (lifecycle idle + null args; start/pause/resume/reset state-guard refusals; step on empty pool → COMPLETED; step over 3-range single-device pool → 8 blocks verified / 3 ranges processed; pause-resume preserves cursor + counters across a 9-block sweep; Restart zeros counters; Reset → IDLE; truncated backing file yields blocks_failed=2 with scrub continuing; mirror(2) 2-dev pool scrub covers both attached allocs → 6 blocks / 2 ranges). 28 suites × default + ASan + TSan green. 12/12 TLA+ specs clean (new scrub + scrub_buggy both verified; 6 buggy configs all fire their invariants). |
| `9207ae7` | **R19 (P5-4c-α scope) audit close: 0 P0 + 2 P1 (pre-patched) + 5 P2 + 4 P3**. Two P1s (TOCTOU on new_slot + impossible-caller-preset-device_id) were already addressed by `eca9c87`. R19 batch adds: idempotent resume path when `old` is already EVACUATING on disk (carries forward a crashed-mid-replace state without duplicating the add); `replace_rollback_or_wedge` helper wedges the sync handle on rollback-of-rollback failure; STM_REPLACE_DRAIN_MAX_STEPS = 100M cap on the drain loop (returns STM_ECORRUPT on divergence); upfront `s->wedged`/`read_only` snapshot guards before any mutation; `bool old_exists` captured inside rdlock (hygiene vs future pointer-post-unlock dereferences); FAULTED-branch refusal test actually driven via `stm_pool_fail_device` (P5-4d-α); old-alloc close-responsibility documented in the wrapper's docstring. Return codes extended with STM_EWEDGED / STM_ECORRUPT. No spec changes — all fixes are impl/docs. | test_sync_multi FAULTED-branch test now exercises the actual refusal path (previously just argued in a comment). 27 × default + ASan + TSan green; 11 TLA+ specs clean. |
| `fcb2d0d` | **P5-4d-α: `stm_pool_fail_device` / `stm_pool_rejoin_device` + mirror_read FAULTED-skip**. ARCH §4.7.4 device-fail / rejoin state transitions. Pool-layer APIs flip ONLINE ↔ FAULTED; bdev pointer PRESERVED (unlike remove's NULL-clear). Dev 0 guard on fail (R17 P1-1). RO pools refuse. `_locked` variants exposed for sync-layer composition. Sync's `stm_sync_mirror_read` now checks `stm_pool_device_info->state` before attempting I/O; ONLINE + EVACUATING accepted, FAULTED + REMOVED skipped (previously only NULL-bdev filtered, which missed FAULTED with stale bytes). Reconcile (catch-up) deferred to P5-4d-β (needs bptr-layer block iteration). | test_pool +3 (fail/rejoin cycle + state guards + RO refusal); test_sync_multi +1 (mirror_read skips FAULTED; read via dev 0 and FAULTED-first ordering both succeed). 27 × default + ASan + TSan green; 11 specs clean. |
| `eca9c87` | **P5-4c-α addendum: close new_slot TOCTOU** (self-found during β review). Original impl asked caller to pre-set `new_alloc->device_id` to a value only knowable post-add under concurrent callers (impossible under race). Fix: determine new_slot INSIDE a pool.wrlock-held critical section around count-read + add_device_locked; call stm_alloc_set_device_id inside the wrapper. Caller no longer needs to pre-set. R15 F3 EBUSY-on-dirty doesn't fire (fresh alloc is clean). Test updated to not pre-set. R19 later confirmed this was the right fix for its P1-1 and P1-2. | test_sync_multi updated (removed pre-set). 27 × default + ASan + TSan green. |
| `f9f9d77` | **P5-4c-α: `stm_sync_replace_device_online`**. ARCH §4.7.3 ONLINE → ONLINE device replacement. Safe wrapper composing `stm_pool_add_device` + `stm_sync_attach_alloc` + `stm_sync_commit` + `stm_pool_begin_evacuation` + evacuation_step-loop + `stm_sync_finish_evacuation` + intervening commits. No new spec invariants — each step is already verified; composition preserves all. Scope caveats: ONLINE→ONLINE only (FAULTED→new reconstruct returns STM_ENOTSUPPORTED, needs bptr-layer block iteration, deferred to P5-4c-β); `device_id == 0` returns STM_ENOTSUPPORTED (R17 P1-1 metadata-primary guard); new device lands at a fresh roster slot (not old's slot — slot reuse is architecturally distinct). Rollback on attach_alloc failure removes the newly-added slot before returning. `sync.h` now includes `pool.h` directly (was forward-declaring `stm_pool`; new API references `stm_pool_device`). | test_sync_multi +2 (happy-path + unsupported-paths). 27 suites × default + ASan + TSan green. 11 TLA+ specs clean. |
| `3c370c5` | **R18 (P5-4b-ii-β scope) audit close: 0 P0 + 1 P1 + 4 P2 + 2 P3**. 1 P1: misleading post-β docstrings claiming `stm_pool_remove_device` / `stm_pool_finish_evacuation` are safe — fixed with loud WARNINGs directing sync-attached callers to the sync-layer safe wrappers (drain check is sync-only). 4 P2s: Linux writer-preference attr via `PTHREAD_RWLOCK_PREFER_WRITER_NONRECURSIVE_NP` (prevents glibc reader-starvation of writers); pre-lock `device_count` bounds check moved inside pool.rdlock for attach_alloc + evacuation_step (closes TSan data race with concurrent add); `stm_sync_finish_evacuation` docstring now specifies detach-without-close ownership semantics (prevents caller UAF from using the detached alloc after closing its bdev); `stm_pool_close` try-acquires wrlock before destroy + aborts on contract violation (converts silent rwlock_destroy UB into a loud crash). 2 P3s: unified State-N phrasing across evac.tla / buggy cfg / phase5-status; device_bdev / device_info docstring updated with pointer-vs-scalar lock-requirement split. Deferred: P2-2 long-I/O-under-lock (throughput pass, not correctness); P3-3 observational. | No new tests — R18 fixes are docstring / writer-preference / guard-ordering. 27 suites × default + ASan + TSan green. 11 specs clean. |
| `5211249` | **P5-4b-ii-β: per-pool rwlock (R16 F4 discharge)**. Spec-first: `evac.tla` gained a `RemoveDirectly` action modeling `stm_sync_remove_device` / `stm_pool_remove_device`. New `DrainCheckOnRemove` BOOLEAN — TRUE = R17 P1-2 fix (drain check enforced), FALSE = pre-fix (`EvacuationAtomic` violated at State 2, one step from Init — reproduced in `evac_remove_no_drain_buggy.cfg`). Impl: `stm_pool` gains `pthread_rwlock_t lock`. Public mutators (add/remove/begin_evac/finish_evac) acquire exclusive internally and delegate to new `*_locked` variants (exposed in pool.h for sync-layer composition). `stm_pool_lock_shared`/`_unlock_shared`/`_lock_exclusive`/`_unlock_exclusive` public API. Sync's 6 pool-iterating APIs (sync_commit, attach_alloc, reserve_mirror, mirror_write, mirror_read, evacuation_step) wrap their critical sections in shared lock. Safe wrappers `stm_sync_remove_device` / `stm_sync_finish_evacuation` acquire exclusive BEFORE sync mutex, then call `*_locked` primitives — closes the R17 lingering race where concurrent reserve could land on about-to-be-REMOVED slot. Lock order: POOL OUTER, SYNC INNER. Caller-serialization contract in pool.h relaxed for all mutators — no external synchronization needed post-β. | test_sync_multi +2 (concurrent-readers + writer-serializes-with-readers stress under TSan). 27 suites × default + ASan + TSan green. 11 TLA+ specs clean (evac.tla gained RemoveDirectly action, extended configs). Buggy configs: release-before-write (State 3), no-drain-on-remove (State 2), and existing quorum/metadata_nonce/device_lifecycle all fire as expected. |
| `350e144` | **R17 (P5-4b scope) audit close: 0 P0 + 2 P1 + 4 P2 + 1 P3**. 2 P1: device-0 guard on begin_evacuation + remove_device (`STM_ENOTSUPPORTED` until sync_open can pick a dynamic metadata primary post-P5-4c); safe-removal wrappers `stm_sync_remove_device` + `stm_sync_finish_evacuation` that verify drain via `stm_alloc_first_allocated` before delegating to pool primitives (returns STM_EBUSY if data remains; on finish, detaches `s->allocs[X]`). 4 P2: sync_commit skips REMOVED devices (belt to finish's braces); remove_device refuses with STM_EBUSY when any OTHER slot is EVACUATING (mirrors AtMostOneEvacuating to the remove path — prevents three-way transition floor-accounting hazard); evacuation_step caps per-step transfer at 4 MiB + overflow guard (STM_ERANGE/STM_ENOTSUPPORTED for over-large ranges — partial-free streaming is P5-4c); sync_redundancy_decode at mount validates against live device count, not total (symmetric with create-time). 1 P3: STM_ENODATA→STM_ENOENT docstring drift. Deferred: P2-6 survivor-in-replicas (documented as caller-contract; closes with P6 extent manager); RemoveDuringEvacuation spec extension (paired with per-pool lock in P5-4b-ii-β); P3-2/P3-3 design-aware, not bugs. | test_pool +3 (device-0 guard / remove-during-evacuating / updated floor test). test_sync_multi +1 (safe-removal refuses non-drained). 27 suites × default + ASan + TSan green. 11 specs clean. |
| `3132867` | **P5-4b-ii-α: evacuation state machine + `stm_sync_evacuation_step`**. Spec-first: new `v2/specs/evac.tla` + `evac.cfg` + `evac_buggy.cfg` models per-block evacuation atomicity with `AtomicEvacuation` BOOLEAN toggle. Fixed config (atomic step) → 13 states clean at depth 5; buggy config (release-before-write) → `EvacuationAtomic` VIOLATED at depth 3 (Init → BeginEvacuation → ReleaseOnly, `replicas[b] ∩ Live` = 1 < MirrorN=2). Invariants: `EvacuationAtomic`, `AtMostOneEvacuating`, `RedundancyPreservedDuringEvacuation`, `NoTargetReplicasAfterComplete`. Impl: new state `STM_DEV_STATE_EVACUATING=6`; `stm_pool_begin_evacuation` (flips ONLINE/FAULTED→EVACUATING under the redundancy floor + at-most-one guards) and `stm_pool_finish_evacuation` (EVACUATING→REMOVED; UUID burned); `stm_sync_evacuation_step(s, target, survivor, &old_paddr, &new_paddr)` reads lowest-start allocated range on target, reserves same-length range on caller-supplied survivor, writes + fsyncs, frees from target, all under sync's lock for per-step atomicity. Caller picks survivor because at this layer we cannot see which devices hold other replicas (spec's `s \notin replicas[b]` delegated up). Also: `reserve_mirror` now iterates the roster picking first n ONLINE slots (was [0, n) positional — would have landed on an EVACUATING device). `stm_alloc_first_allocated` helper (lowest-start refcount≥1 entry, STM_ENOENT when empty/PENDING-only). `stm_pool_remove_device` refuses EVACUATING (caller must finish). **Deferred to P5-4b-ii-β**: per-pool lock (R16 F4) — cross-cutting refactor kept separate for audit focus; caller-serialization contract carries forward. | test_alloc +3 (first_allocated empty/picks-lowest-live/null-args). test_pool +5 (marks-states / redundancy-floor / at-most-one / finish-and-remove-guards / RO-refuses). test_sync_multi +3 (happy-path mirror(2)×3-dev with 2 blocks migrated dev 0→dev 2, verified via mirror_read post-finish / non-evacuating-refused-ENOINVAL / empty-device-skips-steps). test_pool at 39, total 27 suites ~210 tests. Green on default + ASan + TSan. 11/11 TLA+ specs clean (new evac + evac_buggy still demonstrates the bug). |
| `9b60908` | **P5-4b-i: metadata-half `stm_pool_remove_device`**. Transitions a slot to REMOVED without evacuating its data (evacuation is P5-4b-ii). Slot state=REMOVED + bdev=NULL + UUID preserved for burned-UUID tracking. Refuses if already REMOVED, out-of-range, RO pool (EROFS), or if `(live_count - 1) < redundancy_floor` (enforces `RedundancyPreservedOnRemove` from `device_lifecycle.tla`). New `stm_pool_live_device_count` accessor; sync uses it for quorum denominator, redundancy validation, and `reserve_mirror` check. `attach_alloc` refuses REMOVED slots. R16 F3 (burned-UUID) discharged: `add_device` uniqueness walk iterates all slots including REMOVED → re-add with same UUID returns EEXIST. `stm_pool_open` validation relaxed: NULL bdev allowed iff state=REMOVED. | test_pool +4 (mark_removed + floor enforcement + burn_uuid + double-remove/OOR/RO refusal). 31/31 in test_pool green on default + ASan + TSan. 10/10 TLA+ specs clean — impl now matches the existing `device_lifecycle.tla` AddDevice-guard semantics. |
| `a93ce5d` | **R16 (P5-4a scope) audit close: 0 P0 + 2 P1 + 3 P2 + 2 P3**. F1 (state coercion — add_device now forces ONLINE regardless of caller). F2 (size_bytes derived from bdev caps, not caller input). F3 (burned-UUID documented as P5-4b precondition). F4 (caller-serialization contract strengthened; real per-pool lock deferred to P5-4b). F5 (`read_only` bit on stm_pool; add_device refuses STM_EROFS). F6 (test fixture distinct tmp paths via `open_companion_device`). F7 (new `device_lifecycle_large.cfg` at AllDevices={1..6}, MirrorN=2, MaxGen=6 — 10.6M states clean at depth 21). Three regression tests added. | 27/27 suites green; test_pool grew +3 for R16 fixes. 10/10 TLA+ specs clean (new cfg included). |
| `df27da4` | **P5-4a: device lifecycle — spec + `stm_pool_add_device`**. Follows the spec-first policy end-to-end. New `v2/specs/device_lifecycle.tla` models the full device state machine ({ABSENT, ONLINE, FAULTED, REMOVED}) with actions for add/remove/replace/fail/rejoin/reconcile and invariants: `RosterMonotonic`, `RedundancyPreservedOnRemove` (guarded by `RequireRedundancyCheck` constant; buggy config fails at depth 2 — initial 2-device pool drops to 1 below MirrorN=2), `AddDeviceIdempotent`, `NoOrphanOnRemove`, `ReconcileRestoresState`. Impl: `stm_pool_add_device(pool, new_device)` per ARCH §4.7.1. Validates bdev / UUID / role / class / state; rejects duplicate UUID (EEXIST); rejects at roster cap (ENOSPC); appends, recomputes `roster_hash`. Next sync_commit picks up the expanded membership automatically. Caller-serialization contract documented (per-pool lock is P5-4b work). | test_pool +4 (roundtrip + duplicate UUID refused + bad args + roster cap); test_sync_multi +1 integration (add mid-session, commit, every device's UB gets the expanded roster, remount reconstructs the 3-device pool). 31/31 in affected suites green on default + ASan + TSan. 10/10 TLA+ specs clean. |
| *(pending)* | **R15 metadata_nonce.tla + retroactive spec**. New `v2/specs/metadata_nonce.tla` + `metadata_nonce.cfg` + `metadata_nonce_buggy.cfg`. Models AEGIS-256 metadata-node nonce construction (paddr || gen || pool_uuid) with a `DeviceStampPaddrs` BOOLEAN toggle. `=TRUE` (the R15 F1 fix) → 51939 states clean. `=FALSE` (pre-fix impl) → NonceUniqueness VIOLATED at depth 5 with a 3-step counterexample (two devices write (paddr=1, gen=1) under shared metadata_key). Same pattern as `quorum.tla`'s `IdempotentRetry` constant — demonstrates the bug at spec level and pins the invariant for future per-device-allocator refactors. Aligned with the new CLAUDE.md spec-first policy. | 10 specs total; new spec clean + buggy counterexample confirmed. |
| `dd8a275` | **R15 (P5-3 scope) adversarial audit close**. All 7 findings addressed. F1 (P0) AEAD nonce per-device differentiation — alloc's store_reserve stamps device_id into paddr top-16 so per-device tree-node nonces stay unique; store_write/read/free validate and strip. F2 (P1) alloc_roots_commit frees reserved paddr on post-serialize failure (prevents retry leak). F3 (P1) stm_alloc_set_device_id returns STM_EBUSY when tree_dirty or current_tree_root != 0 (forbids the silent paddr-stream-inconsistency hazard). F4 (P2) reserve_mirror rollback uses auth_gen for free_gen so the next commit's sweep reclaims cleanly. F5 (P2) stm_sync gains read_only + wedged guards; reserve_mirror / mirror_write / commit refuse up-front; mirror_read exempt from RO but wedged-refuses. F6 (P2) STM_UB_VERSION 6→7 bump gates the P5-3c value-layout change cleanly. F7 (P3) mirror_read preserves first-error for diagnostics. | test_sync_multi +3 regression tests (per-device nonce differentiation, set_device_id refuses after reserve, mirror_write refuses RO). test_pool +1 (v6 version rejection). 27/27 green on default + ASan + TSan. |
| `a078a38` | **P5-3c: mirror reserve / write fan-out / read fallback**. Enables mirror(n) redundancy end-to-end. Per-device allocator support: `stm_alloc` gains `device_id`, stamped into paddr top 16 bits on reserve, validated at free/ref/lookup. Public setter `stm_alloc_set_device_id` before any reserve on device > 0. Multi-alloc sync: `stm_sync` holds `stm_alloc *allocs[STM_POOL_DEVICES_MAX]` — primary at create/open; devices > 0 via `stm_sync_attach_alloc` (which installs per-device crypt ctx AND lazy-loads the tree from the roots object if mounted). `sync_commit` iterates every attached alloc, commits each, records `(paddr, csum, tree_gen)` in the roots object. Per-tree gen in roots entries: value layout extended 40 → 48 bytes (+8 for le64 root_gen) — trees can have different AEAD gens when `alloc_commit`'s R7c P2-5 short-circuits; the per-entry gen lets attach-time `load_tree_at` use the right nonce. Bug caught mid-impl by the mirror_survives_unmount_remount test (one clean-tree short-circuit → other tree advanced → AEAD-tag mismatch at remount). Mirror APIs: `stm_sync_reserve_mirror` (reserves one paddr per device 0..n-1 via their attached allocs; rollback on partial failure), `stm_sync_mirror_write` (parallel writes + fsyncs; STM_OK on ⌊n/2⌋+1, STM_EQUORUM below), `stm_sync_mirror_read` (tries replicas in order, BLAKE3-verifies each against caller-supplied expected_csum; first match wins; STM_ECORRUPT if all fail). | test_sync_multi +6: mirror(2) + mirror(3) roundtrip, tampered-replica fallback (three progressively-corrupt scenarios), reserve refuses NONE profile, attach_alloc arg validation (device_id=0 primary + uuid mismatch + double-attach), mount-remount with attach-loads-from-roots. test_alloc_roots signatures updated for 5-arg set / 4-arg get / iter-with-gen callback. 43/43 tests green on default + ASan + TSan. 9/9 TLA+ specs clean. |
| `c5ad8d7` | **P5-3b: allocator-roots object** (ARCH §6.1). New `stm_alloc_roots` module (`include/stratum/alloc_roots.h` + `src/alloc_roots/alloc_roots.c`). `ub_alloc_root` now points at the pool-level roots object (kind = new `STM_BPTR_KIND_ALLOC_ROOTS = 8`) instead of directly at a single alloc tree; the roots object is a small AEAD-encrypted Bε-tree (via `stm_btree_store_serialize`) whose leaves carry each device's alloc-tree root bptr keyed by `device_id`. Merkle chain remains transitively complete. For N=1 pools, one entry points at device 0's tree — identical FS-layer behavior, extra indirection. `stm_sync` gains a `stm_alloc_roots *roots` field; `sync_create` creates it, `sync_commit` drives `alloc_commit → roots_set → roots_commit` in sequence, `sync_open` reads via `alloc_roots_load_at`. Dirty-flag idempotency on both `roots_commit` AND `roots_set` (no-op when new value == existing entry) — preserves byte-identical UBs across retry per quorum.tla's ContentQuorumAtGen. Bug caught + fixed during integration: `alloc_roots_commit` calls `bootstrap_commit` at its tail so roots-paddr reservations become durable (otherwise the bitmap's persisted image didn't cover our new paddrs → STM_ECORRUPT on the next commit's free_tree). Format change: `STM_UB_VERSION 5 → 6`. | New `test_alloc_roots.c` (10 tests): unit coverage for set/get/count/iter/commit/load roundtrip + idempotency + tamper detection (wrong csum/key/gen). `test_sync_multi` +3: ub_points_at_alloc_roots, multi_commit_cycle (5 commits + remount + 1 more to exercise free_tree-on-old-roots), v5 UB refusal. `test_pool` +1: v5 version rejection. `test_sync` reworked: sync_ub_alloc_root_matches_tree now asserts the indirection is real (paddr != tree_root, kind = ALLOC_ROOTS). 40/40 tests green on default + ASan + TSan (27 suites; +10 in new test_alloc_roots, +4 in test_sync_multi & test_pool). 9/9 TLA+ specs clean (no spec change). |
| `a52b532` | **P5-3a: UB redundancy profile plumbing**. Threads a pool-wide `stm_redundancy_profile {kind, mirror_n}` through `stm_sync_create` / `stm_sync_open`. The profile is validated up-front (mirror_n in [1..device_count]; RS/LRC → STM_ENOTSUPPORTED forward-reserved) and stamped into every UB's ub_redundancy_kind + ub_redundancy_params[15] at commit. `stm_sync_open` decodes the canonical UB's profile, re-validates (including "mirror(n) on a pool that has since shrunk below n" → STM_ECORRUPT), exposes it via `stm_sync_redundancy_get`. Params encoding is layout-stable: NONE = all 15 bytes zero; MIRROR = params[0]=n, rest zero. Tail-byte checks reject both accidental drift and tamper even though ub_csum already covers the UB. Existing `stm_sync_create` callers pass NULL (= NONE) — 13 call sites updated via perl. Lays the groundwork for P5-3b (per-device alloc trees + alloc-roots object) and P5-3c (`stm_alloc_reserve_mirror` + write fan-out + read fallback). | `test_sync_multi` grows 4 new: mirror(2) roundtrip on NDEV=3 (byte-identical on-disk + round-trips through remount), null-profile-produces-NONE, create-rejects-malformed (mirror(0), mirror(n>3), NONE+n≠0, unknown kind, RS/LRC), mount-rejects-nonzero-tail-on-NONE (with ub_csum fix-up). 30/30 tests green on default + ASan + TSan. 9/9 TLA+ specs clean (no spec change). |
| `57a37e7` | **P5-0: `quorum.tla` formal spec**. Models multi-device commit (ARCH §5.6) with per-device UB rings, parallel reservation + final writes, quorum threshold = ⌊N/2⌋+1, device FAULTED / ONLINE state, rejoin + reconcile, coordinator crash. Proves `TypeOK`, `QuorumSafety` (phase=Published implies ≥quorum devices at target_gen), `AuthoritativeMono` (committed gens never regress), `CommitAtomic` (auth ≥ 2×commits_done), `OrphansNotAuthoritative` (partial-Phase-3 gens held by <quorum devices are never authoritative), `LiveCoordTargetValid` (coord's in-flight target_gen ≥ current auth), `QuorumDurability`, `MountGenBumpMulti`. Mount requires quorum of ONLINE devices (emergency mount is an explicit opt-in not modeled). TLC-clean under `Devices={1,2,3}, MaxCommits=3, MaxFaults=2`: 36839 distinct states, depth 35. Two iterations: initial pass rejected orphan gens from degraded-mount, then caught an over-strict `LiveCoordMonotonic` (`target > auth`) that Fails in the transient state where Phase 3 WriteFinals hit quorum on disk before CheckFinalQuorum fires. Fixed by weakening to `target_gen >= auth`. | Spec-only; 9 TLA+ specs clean including the new one. |
| `15d2d33` | **R14b P2-1: idempotent `stm_keyschema_commit`**. Dirty-flag on stm_keyschema; commit short-circuits and returns cached (paddr, csum) when clean. Symmetric to alloc's R7c P2-5. Closes the only R14b P2 finding — prevents the 3-way retry divergence scenario where rotating single-device successes across retries produce 3 distinct contents at target_gen, causing content-quorum to fail at mount. Regression test `sync_multi_keyschema_commit_idempotent_on_clean` verifies two consecutive sync_commits produce byte-identical ub_key_schema[512] bytes. 27/27 suites green on default + ASan + TSan. 9/9 TLA+ specs clean. |
| `3f946a2` | **R14 P5-2-scope audit close**. Spec-first fix: extended quorum.tla with content dimension + `ContentQuorumAtGen` invariant + `IdempotentRetry` CONSTANT + `RetryPhase3` action. Bonus spec fixes: Mount-overwrite-all, BeginCommit-visible-quorum gate, Reconcile-copy-peer-content. quorum.cfg (IdempotentRetry=TRUE, MaxFaults=0) clean at 2411 states; quorum_buggy.cfg finds ContentQuorumAtGen counterexample at depth 13 (demonstrates P1 at spec level). Impl: content-quorum agreement check in stm_sync_open replaces strict unanimity (groups visible devs at auth_gen by byte-level shared content; ≥quorum-group is canonical; dissenters are ARCH §5.8 orphans); byte-level `sync_ub_shared_bytes_match` auto-covers all shared fields (closes P2 on `ub_alloc_root_gen`); `stm_ub_decode` rejects ub_gen=0 (P3-3); `write_ub_to_all_devices` propagates last hard err on N=1 (P3-5). New test_sync_multi tests for minority divergence tolerated, no-content-quorum rejected, alloc_root_gen tamper detected; test_sb rejects ub_gen=0. |
| `1414cc4` | **P5-2: Multi-device 2-phase quorum commit**. Implements the commit + mount protocols for N-device pools per quorum.tla. Each commit writes reservation at gen=auth+1 (pre-flush roots; rollback target) and final at gen=auth+2 (post-flush roots + new merkle) to every pool device in parallel, with quorum (⌊N/2⌋+1) fsync confirmations required per phase. Mount scans every device, computes auth_gen = highest gen G such that ≥quorum devices have ub_gen ≥ G (derived by sorting valid gens descending and taking the kth element), validates content agreement among quorum members at gen=auth_gen, then writes a claim UB at auth+1 to every device (quorum required). Fresh-pool first commit is 1-phase (no pre-flush state); subsequent commits uniformly 2-phase. Struct cleanup: `stm_sync` drops cached `bdev`/`self_device_id` (no "self" in multi-device), adds `auth_gen` field. `build_uberblock` takes a `target_device_id` param; shared bytes + per-device fields split via a new `write_ub_to_all_devices` helper. New `STM_EQUORUM` status code for quorum-write failures. `stm_sync_info` exposes `auth_gen` alongside `current_gen`. Alloc tree still lives on device 0 (metadata primary) — P5-3+ mirror/RS will fan it out. | New `test_sync_multi.c` (7 tests): 3-device roundtrip, quorum fault tolerance (wipe 1 of 3 devices' UBs; mount succeeds), sub-quorum refusal (wipe 2 of 3; STM_EQUORUM), every-device UB write verification, orphan-ahead-of-quorum (OrphansNotAuthoritative), inconsistent-quorum-content rejection. Existing tests updated for +2 gen arithmetic (fresh commit at gen=1; subsequent commits advance auth by 2). All 26 suites green on default + ASan + TSan. 9/9 TLA+ specs clean. |
| `567b143` | **P5-1: Pool layer foundation**. Introduces `stm_pool` (`include/stratum/pool.h` + `src/pool/pool.c`) wrapping up to 64 `stm_bdev`s — today's configuration is always N=1 (degenerate). Adds `stm_device_state` enum. Populates the uberblock's previously-zero roster fields (`ub_device_count`, `ub_device_id`, `ub_device_class`, `ub_device_role`, `ub_roster[2048]`, `ub_roster_hash`) on every commit. `ub_roster` layout is 32 bytes per slot: `uuid[16] + size[8] + role[1] + class[1] + state[1] + reserved[5]`; up to 64 slots exactly match `ub_roster[2048]`. `ub_roster_hash` is the le64 truncation of BLAKE3-256 over the encoded 2048-byte roster. Refactors `stm_sync_create` / `stm_sync_open` to take a `stm_pool *` in place of `(stm_bdev*, pool_uuid, device_uuid)` — cleaner lifecycle, ready for P5-2 to iterate the pool's device set during commit. `stm_fs_format` builds a 1-device pool from `stm_fs_format_opts` (role=DATA, class=SSD, state=ONLINE default); `stm_fs_mount` peeks the durable UB via `stm_sb_mount_scan`, decodes the roster, constructs a matching `stm_pool`, and hands it to sync. Sync cross-validates `ub_roster_hash` + `ub_pool_uuid` + `ub_device_uuid` against the pool handle before proceeding — catches programmer error and roster tampering that preserved ub_csum. STM_UB_VERSION 4 → 5. v4 pools refused at mount by version check; no feature-flag allocation needed (version bump alone gates per ARCH §5.9). | New `test_pool.c` (12 tests): open validation, roster encode/decode roundtrip, roster hash determinism + identity-change sensitivity, fs format/mount roundtrip populates every roster field, decode rejects leftover bytes + zero UUID in populated slot, sync_open refuses wrong pool_uuid + wrong device_uuid. All 25 suites (24 prior + 1 new) green on default + ASan + TSan. |

## Remaining Phase 5 work

Rough ordering; dependencies force roughly this sequence:

### P5-1: Pool layer foundation (`src/pool/`) — **LANDED**

Landed above P5-0; specifics in the Landed-chunks table. Summary:
`stm_pool` wraps up to `STM_POOL_DEVICES_MAX = 64` `stm_bdev`s (MVP
exercises N=1 only); roster persisted in every device's uberblock
at `ub_roster[2048]` (32-byte slots: `uuid[16] + size[8] + role +
class + state + reserved[5]`); `ub_roster_hash` is the le64
truncation of BLAKE3-256(`ub_roster[2048]`); STM_UB_VERSION bumped
4→5. Sync + FS lifecycle refactored to thread a `stm_pool *` where
previously `(stm_bdev*, pool_uuid, device_uuid)` lived — P5-2 is
now pure body-change (iterate the pool's device set during commit).

### P5-2: Multi-device commit — **LANDED**

Landed above P5-1; specifics in the Landed-chunks table. Summary:
`stm_sync_commit` / `_open` iterate the pool's device set per
phase; each phase requires quorum of fsync confirmations. Commit
protocol is 2-phase (reservation at auth+1 with pre-flush roots,
final at auth+2 with post-flush roots); each commit advances auth
by 2. Mount scans all devices, computes auth via highest gen with
quorum, writes a claim UB at auth+1 (quorum required) and sets
auth_gen = auth+1 post-claim.

Fresh-pool first commit is 1-phase (no pre-flush state). `STM_EQUORUM`
is the new status for quorum-write failures. Alloc tree is still
single-device (device 0, the metadata primary) — multi-device data
redundancy lands in P5-3.

### P5-3: Mirror redundancy — split into three sub-chunks

First redundancy profile: `mirror(n)`. Deliberately simpler than RS/LRC so we can validate the multi-device commit + redundancy integration end-to-end before taking on EC.

**P5-3a: UB redundancy profile plumbing — LANDED (`a52b532`).** See the Landed-chunks table. Threads `stm_redundancy_profile` through sync, stamps it into every UB, validates + decodes at mount. No behavioral change — groundwork for the two chunks that follow.

**P5-3b: allocator-roots object — LANDED (`c5ad8d7`).** See the Landed-chunks table. Summary: `ub_alloc_root` points at a pool-level roots object (`STM_BPTR_KIND_ALLOC_ROOTS`); single-entry today, multi-entry unlocked for P5-3c. STM_UB_VERSION bumped 5 → 6.

**P5-3c: mirror reserve / write fan-out / read fallback — LANDED (`a078a38`).** See the Landed-chunks table. Summary: sync-level mirror API over N attached per-device allocs, exposed as `stm_sync_reserve_mirror` / `_mirror_write` / `_mirror_read`. Landed the full mirror redundancy MVP. Placement is deterministic (devices 0..n-1); a full free-space-weighted scorer per ARCH §6.7.2 follows in a later sub-chunk once extent-manager integration is live.

### P5-4: Device lifecycle — split into sub-chunks

Spec + first impl chunk landed. Remaining sub-chunks outlined below.

**P5-4a: spec + `stm_pool_add_device` — LANDED (`df27da4`).** See the Landed-chunks table.

**P5-4b: `stm_pool_remove_device` — split into three sub-chunks.**

- **P5-4b-i: metadata half — LANDED (`9b60908`).** Slot transition to REMOVED + burned-UUID tracking + redundancy-floor guard. Refuses on data-bearing devices via caller-enforced alloc-tree-empty precondition.
- **P5-4b-ii-α: evacuation state machine + per-step data movement — LANDED (pending commit above).** New `STM_DEV_STATE_EVACUATING` intermediate state, `stm_pool_begin_evacuation` / `stm_pool_finish_evacuation` + `stm_sync_evacuation_step`. Caller drives the drain with begin → (step, step, …) → STM_ENOENT → finish. Each step is atomic (read + survivor-reserve + write + target-free under sync's lock). `evac.tla` formalizes the per-block atomicity invariant; buggy config demonstrates release-before-write exposes < MirrorN replicas at depth 3. `reserve_mirror` updated to iterate roster picking first-n ONLINE (skips EVACUATING). Caller supplies survivor device_id because at this layer we can't see which devices already hold other replicas (spec's `s \notin replicas[b]` delegated up).
- **P5-4b-ii-β: per-pool lock (R16 F4 discharge) — LANDED (`5211249`).** See the Landed-chunks table.
- **P5-4b-ii-γ (optional): evacuation resume across crash.** Today the EVACUATING state byte persists in the uberblock's roster but there is no auto-resume on remount. If a mount sees EVACUATING, the operator decides whether to continue (call evac_step until ENOENT) or roll back (ONLINE transition — requires cleanup of survivor copies). Ties into P5-4d's reconcile work.

**P5-4c: `stm_pool_replace_device`.** Split into two sub-chunks.
- **P5-4c-α: ONLINE→ONLINE — LANDED (`f9f9d77`).** See the Landed-chunks table. Safe-wrapper composition of add + evacuate primitives. New device at new slot; old becomes REMOVED tombstone.
- **P5-4c-β: FAULTED→new reconstruct.** Reads from surviving replicas via `mirror_read`, writes to new. Needs bptr-layer iteration (which blocks this is which replicas) — unlocks with the extent-manager refactor (P6 scope).

**P5-4d: FailDevice + RejoinDevice + Reconcile wire-up.** FailDevice → DEGRADED pool state. RejoinDevice → Reconcile reads current-auth UB from a quorum peer, writes it to the returning device. Matches the TLA `ReconcileDevice` action.

**P5-4b+: Per-pool lock + sync integration — LANDED as P5-4b-ii-β.** See P5-4b split above.

Rebalance (ARCH §4.8) remains deferred to a distinct sub-chunk (P5-4e or later) — not needed for basic multi-device operation.

### P5-5: Scrub (`src/scrub/`)

- State machine: IDLE / RUNNING / PAUSED / COMPLETED.
- Pause / resume persistence: progress cursor checkpointed in the allocator tree so restart resumes from where it left off.
- IO throttling via `io_weight` knob (1–100).
- Repair from redundancy: on AEAD tag / csum mismatch in a stripe, read the surviving replicas, rewrite the bad block. Leverages P5-3's mirror-read primitive.
- Exposed via `stm_fs_scrub_start / _pause / _resume / _status`.

### P5-6: R13 adversarial audit

Spawned after P5-1 through P5-5 land. Scope: roster persistence, multi-device commit under partial failure, mirror redundancy, device lifecycle ops, scrub's crash-recovery, concurrent commit + device-fail races.

### Deferred — post-Phase-5

- Reed-Solomon (`rs(k,p)`) and LRC (`lrc(k,l,g)`) erasure coding. Requires Intel ISA-L SIMD integration + stripe geometry + full-stripe write discipline. Non-trivial; lands as its own sub-chunk once the core commit+redundancy shape is proven with mirrors.
- Hot spares (ARCH §4.10).
- Metadata ditto blocks (ARCH §4.5.4) for large pools.
- Class-aware placement (ARCH §4.9).

## ROADMAP §8.2 exit criteria status

- [x] 4-device RAID-Z-equivalent pool survives single-device failure without data loss. **Mirror(n)** satisfies this at P5-3c — sync_multi_mirror_read_falls_back_on_tamper demonstrates 2-of-3 replica survival. Full 4-device RAID-Z (Reed-Solomon) lands post-Phase-5.
- [ ] LRC repair 2-3× faster than RS for single-failure scenarios. (Deferred.)
- [ ] Scrub detects + repairs injected corruption. (P5-5.)
- [ ] Rebalance progress persists across restart. (Deferred to P5-4b.)
- [x] `quorum.tla` proves commit-under-partial-failure semantics. (P5-0.)

## Build + verify commands

```bash
cd v2

# Default + sanitizers — 27 suites + mirror additions from P5-3.
cmake -B build -S . -DSTM_ENABLE_IOURING=OFF -DSTM_ENABLE_PQ=OFF
cmake --build build -j
ctest --test-dir build --output-on-failure

cmake -B build-asan -S . -DSTM_SANITIZE=asan \
    -DSTM_ENABLE_IOURING=OFF -DSTM_ENABLE_PQ=OFF
cmake --build build-asan -j
ctest --test-dir build-asan --output-on-failure

cmake -B build-tsan -S . -DSTM_SANITIZE=tsan \
    -DSTM_ENABLE_IOURING=OFF -DSTM_ENABLE_PQ=OFF
cmake --build build-tsan -j
ctest --test-dir build-tsan --output-on-failure --timeout 180

# All 10 TLA+ specs (quorum.tla joins at P5-0; metadata_nonce.tla at R15)
export PATH="/opt/homebrew/opt/openjdk/bin:$PATH"
cd specs
for s in sync concurrency structural balanced merge allocator merkle key_schema quorum metadata_nonce; do
    echo "== $s =="
    java -cp /tmp/tla2tools.jar tlc2.TLC -workers auto -deadlock \
        -config $s.cfg $s.tla 2>&1 | tail -3
done

# Bug-demonstration specs (expected to fail, proving the bug is
# modeled — useful as executable documentation):
java -cp /tmp/tla2tools.jar tlc2.TLC -workers auto -deadlock \
    -config quorum_buggy.cfg quorum.tla 2>&1 | tail -3
java -cp /tmp/tla2tools.jar tlc2.TLC -workers auto -deadlock \
    -config metadata_nonce_buggy.cfg metadata_nonce.tla 2>&1 | tail -3
```

## Trip hazards carrying into Phase 5

Inherited + new:

- **Nonce uniqueness under multi-device** (ARCH §4.4.3). paddr's device_id naturally differentiates. The existing sync.tla nonce argument (uniqueness of `(paddr, gen)`) lifts to `(device_id, offset, gen)` with no additional burden: `gen` is pool-global monotonic per quorum.tla, and `device_id` distinguishes across devices trivially. Verified post-P5-2 that every AEAD call site threads the device-id-bearing paddr through unchanged.
- **Quorum at mount refuses degraded pools** (P5-0 spec finding). `Cardinality(OnlineDevices) >= QuorumN` is a hard precondition for Mount. Emergency mount (ARCH §5.11) is the only way to mount below quorum; it's an explicit admin opt-in and should be a separate code path, not a relaxation of the default Mount.
- **Orphan "ahead of quorum" gens are legal post-crash** (P5-0 spec finding). A partial Phase 3 write can leave one device at `gen+2` while the rest stay at `gen+1`. The impl must handle this: mount picks the highest QUORUM gen (likely `gen+1`), and the next commit overwrites the orphan device's `gen+2` UB via normal ring rotation. Writing a regression test that reproduces this (kill 2/3 devices mid-Phase-3) is a P5-2 deliverable.
- **Device add/remove changes the roster** (ARCH §5.5.4). Quorum is computed against the roster AT COMMIT TIME, not current roster. A commit that started before a `pool remove` completes must still reach quorum of the pre-remove roster. The `roster_hash` in uberblocks is the witness.
- **Format stability**: roster goes in `ub_reserved`, STM_UB_VERSION bumps 4 → 5 at P5-1. Feature-flag gating per ARCH §5.9.

## Known deltas from ARCH §4 + §5 (owed follow-ups)

- **Rebalance tree-embedded progress** (ARCH §4.8). ARCH envisions the rebalance cursor living in the allocator tree so it survives crashes. P5-4 MVP won't implement rebalance at all; cursor design happens in P5-4b.
- **Device class discovery** (ARCH §4.12). ARCH's open question is "probe via `/sys/block/…/rotational` + `queue/zoned` or require admin declaration". P5-1 requires admin declaration (simpler MVP); auto-probe can graft on later.
- **Log device semantics** (ARCH §4.12). ARCH defers this to §9 (block device). Phase 5 MVP doesn't support dedicated LOG-role devices; add later if ZIL-class latency optimization is wanted.

## References

1. `v2/docs/phase5-status.md` — this file.
2. `v2/docs/phase4-status.md` — Phase 4 exit + invariants Phase 5 builds on.
3. `docs/ARCHITECTURE.md §4` — storage pool.
4. `docs/ARCHITECTURE.md §5` — superblock + quorum.
5. `docs/ROADMAP-V2.md §8` — phase deliverables + exit criteria.
6. `memory/audit_v2_r0_closed_list.md` — cumulative closed-list (R0–R12).
7. `v2/specs/quorum.tla` + `quorum.cfg` — Phase 5 commit spec (P5-0).
